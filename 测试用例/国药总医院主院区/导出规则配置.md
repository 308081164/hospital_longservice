# 国药总医院主院区 · S7 导出规则配置

- **客户编码**：`GUOYAO-MAIN`
- **账单策略**：`guoyao_bill`（legacy POI 导出管线）
- **结款函策略**：`standard_settlement`
- **结款独立折扣**：无
- **导出阶段折扣**：无
- **验收**：legacy 模板 + 策略已登记

## 导出包数口径（2026-08）

- **导出包数与导入一致**：不再将 `packCount>1` 拆成多行（已移除 `splitGuoyaoPlatinumRows` 默认调用）。
- **汽轮机核算**（`GuoyaoQuantityAlgorithm`）仍只影响「单包内器械数量/把」列，不改包数。
- 聚合 key 含 `包类别号`，避免同单号不同类别错合并。

详见 [`测试用例/S7导出规则配置说明.md`](../S7导出规则配置说明.md)。