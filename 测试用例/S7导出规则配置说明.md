# S7 导出规则配置说明（优先医院）

> 种子：`backend/src/main/resources/billing-seeds/phase-export-rules-20260723.json`
> marker：`billing_seed_export_rules_20260723_v1`
> **分科室布局增量**：`phase-export-dept-split-20260728.json` · marker `billing_seed_export_dept_split_20260728_v1`

## 架构

- **账单/结款函导出**：`ExportEngineServiceImpl` → legacy POI 模板（`standard_bill` / 客户绑定策略）+ `ColumnTransformPipeline` 列映射。
- **账单布局**：`export_template.column_mapping.billLayout` — `auto`（默认，>1000 行 OOM  guard 合并合计）| `dept_split`（强制分科室多 Sheet）| `combined`（强制单 Sheet 合计）。
- **D8 行显示**：`column_mapping.d8DisplaySource` — `hospitalName` | `ruleName` | `auto`（默认规则名回退医院名）。
- **导出向导标识**：预览/校验步展示 `exportProfileLabel`（特色导出·分科室 / 常规导出等）。
- **结款独立折扣**：`CustomerBillingPolicy` / `CustomerDiscount` 的 `settlement_only` 阶段，由结款函填充器读取。
- **导出阶段折扣**：`ExportStageDiscountApplier` 读取 `export_only` 策略（如太平 2+ 把 75%）。
- **UI**：客户管理 → `CustomerExportTemplatePanel` 绑定模板 + 策略摘要 + 跳转导出向导。

## 种子摘要（按客户编码）

| 编码 | 账单策略 | 结款 | 结款折扣 | 导出阶段折扣 | billLayout | S7 |
|------|----------|------|----------|--------------|------------|:--:|
| `ZYY-D1` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `ZY3-DIANLI` | standard_bill | standard_settlement | 结款7折 | — | auto | ✅ |
| `GUOYAO-MAIN` | guoyao_bill | standard_settlement | — | — | auto | ✅ |
| `GUOYAO-2` | guoyao_bill | standard_settlement | — | — | auto | ✅ |
| `GUOYAO-3` | guoyao_bill | standard_settlement | — | — | auto | ✅ |
| `HRB-2ND` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `HRB-WY` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `HRB-WY-EM` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `XINFA-HSZ` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `SHENG-YY-NG` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `SHENG-YY-XF` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `ZUYAN-NG` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `ZUYAN-SF` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `ZUYAN-XA` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `NG-FUCHAN` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `SHKF-YY` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `DAOWAI-RM` | daowai_bill | standard_settlement | — | — | auto | ✅ |
| `TAIPING-RM` | standard_bill | standard_settlement | — | 2+把75%阶梯（export_only，phase5-batch-c） | auto | ✅ |
| `SANJING-SB` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `VICTORIA` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `JIUZHOU-FK` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `HULAN-HSZ` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `HULAN-TCM` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `ZYY-D2-NG` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `ZYY-D2-HN` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `RENSHENG` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `HRB-HX-EYE` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `BINGCHENG-YM` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `XF-ZYY` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `WJ-HLJ-ZD` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `YUEMEI-FH` | standard_bill | standard_settlement | — | — | auto | ✅ |
| `ERYY-NG` | sheng_er_bill | standard_settlement | — | — | auto | ✅ |
| `ERYY-SB` | standard_bill | standard_settlement | — | — | dept_split | ✅ |
| `HULAN-RM` | standard_bill | standard_settlement | 账单7折（after_base） | — | auto | ✅ |
| `HRB-HSZ` | standard_bill | standard_settlement | 加急/抵扣见 phase1-batch-a | — | auto | ✅ |
| `HRB-HIT` | standard_bill | standard_settlement | — | — | combined | ✅ |
| `HRB-HEU` | standard_bill | standard_settlement | 结款灭菌9折 | — | auto | ⏭ |

## 特殊说明

- **太平（TAIPING-RM）**：`phase5-batch-c` 已写入 `export_only` 阶梯折扣；与登记表「导出 75 折仅记录」并存，S8 导出比对时以处理后表为准。
- **九院（HRB-NGJY）**：不在 36 院看板内，但种子含结款 9 折 + 结款模板绑定（看板外 P0 客户）。
- **全局 template customerCode 别名**（如 `JIUYUAN`）与正式编码不一致时，以 **客户级 export_template 绑定** 为准（本种子已覆盖工程/九院/道外/省二/国药等）。

## 复跑

```bash
python3 scripts/batch_s7_export_config.py
./scripts/verify-billing-seed.sh
```
