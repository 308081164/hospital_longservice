---
description: 禁止整体读取巨型文件，防止上下文爆炸导致会话中断
alwaysApply: true
---

# 巨型文件与目录处理规范（防止会话中断）

本仓库含大量测试材料与历史报告。以下操作曾导致上下文耗尽、会话被强制压缩、任务中途出错，**必须遵守**：

## 禁止

- 禁止用 Read 整体读取 `测试用例/*.json` 严格对账报告（单文件可达 6 万+ 行）
- 禁止对 `测试用例/`、`铂康/`、`docs/进度规划/` 做无 glob 限制的 Grep 全量 content 输出
- 禁止把 Excel/PDF 二进制路径交给 Read（用脚本解析）

## 正确做法

- 读报告 JSON：用 `python3 -c` / `jq` 提取 summary 或指定字段，只输出聚合结果
- 读大文件前先 `wc -l` 或看大小；超过 2000 行的一律用 offset/limit 分段或用脚本摘要
- Grep 测试目录时必须加 `glob` 限制（如 `*.md`）或 `head_limit`
- 需要逐行分析报告内容时，写 Python 脚本聚合，不要把原始行贴进上下文
