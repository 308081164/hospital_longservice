# 内勤规则配置生效核查报告（2026-09-23）

## 总览结论

| 核查项 | 结论 | 一句话 |
|--------|------|--------|
| 账单规则 | **部分生效** | Excel「账单规则」11 家中，多数已落入 `clerk-rules/baseline` 并由 `ClerkBillPriceRuleApplier` / 折扣类编译链路覆盖；**太平人民医院价格表明细未入库**为主要缺口。 |
| 低消保底规则 | **完全配置生效** | Excel「低消保底规则」5 家的 `minCharge` / 基数 / 关联物流与折扣均已配置，且 `ClerkRuleCompiler` 会编译为月度结算策略；存在结款函表与低消表双源重复录入（卫生问题，不阻生效）。 |
| 26 大条结款函规则 | **部分生效** | 26 条医院维度的核心能力绝大多数已有对应 `ruleType`；**附二南岗/哈南「三张汇总表」仅配 1 种**、**电力缺「消毒灭菌费明细表」**为明确缺口；部分类型（国药核算 / 合并结款 / 备包拆行）依赖特化导出或硬编码，非 Compiler 全量转译。 |

**口径说明：** 本报告以「Excel 条目是否已写入 `clerk-rules` 且存在可追踪生效机制」为主；不以 `测试用例/clerk_export_verify_report.md` 的金额对账结果替代「配置是否落地」（该报告显示大量 S8 fail，属导出金额/样式验收，另案）。

---

## Excel 来源与解析口径

| 项 | 值 |
|----|-----|
| 源文件 | `/Users/yangxinghui/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_7qwn4vnuj7xo22_508c/temp/drag/医院内勤规则-20260918.xlsx` |
| 工作区副本 | `测试用例/医院内勤规则-20260918.xlsx` |
| 解析工具 | Python `openpyxl`（`data_only=True`） |
| 仓库声明来源 | `clerk-rules/index.json` → `source`: `docs/source/附一收费标准.xlsx + 铂康/内勤要求/医院内勤规则-20260918.xlsx`；`generated_at`: `2026-09-20T07:35:18Z`；`customer_count`: **39** |

### Sheet 结构

| Sheet | 行规模 | 用途 |
|-------|--------|------|
| **账单规则** | 112×10 | 11 家医院：账单计算规则 + 包类型价格明细；`是否有结款函规则` 标记 |
| **结款函规则** | 48×3 | **26 家**医院结款函条文（多行续写合并为一条「医院」） |
| **低消保底规则** | 6×5 | **5 家**低消标准 + 物流/折扣旁注 |
| 附件清单 / 附件价格详情 | 小表 | PDF 价格单索引与明细 |
| *结款函样式* 等 8 个 sheet | 多为空壳 | 样式图在 `clerk-rules/attachments/settlement-layout/*.png`，Excel 内无结构化规则 |

### 解析规则

1. **账单规则**：`序号`+`医院名称` 起新医院块；其后无序号行归入该院价格明细。
2. **结款函规则**：有医院名起新条；仅有「结款函规则」列的续行并入上一条。
3. **低消保底**：一行一家，字段：`低消标准` / `物流费` / `账单折扣` / `说明`。
4. 医院名 → CODE 映射与 `docs/clerk-rules-excel-mapping.json`、baseline `customerName` 对齐（如「黑龙江省医院（南岗/香坊）」→ `SHENG-YY-NG`+`SHENG-YY-XF`）。

### 生效机制（仓库）

```
Excel(20260918)
  → scripts/clerk_rules_import_v2.py 等生成 baseline/*.json + index.json
  → ClerkRuleIndex 读 classpath
  → ClerkRuleCompiler.compileForCustomer
       ├─ billingPolicies（折扣/低消/物流/加急等）
       ├─ exportLayouts / exportSupplementTypes
       └─ clerkRules（全文保留，供专用 Applier）
  → ClerkBillPriceRuleApplier / ClerkRuleExportService / SettlementTemplateFiller / GuoyaoBillExportStrategy …
```

门禁：`scripts/check_clerk_rules_alignment.py`（对照 `docs/clerk-rules-excel-mapping.json`）当前结果：`expected=38 indexed=39 missing=0 extra=1`，**EXTRA=`ZYY-D1`（附一，不在 20260918 结款函 26 条内）**。

---

## 一、账单规则

### 1.1 Excel vs 仓库覆盖

| # | Excel 医院 | CODE | Excel 要点 | baseline 现状 | 判定 |
|---|------------|------|------------|---------------|------|
| 1 | 道外区人民医院 | DAOWAI-RM | 包类型列表 + 19 行单价；结款函标记「有」 | 20×`BILL_EXPORT_PRICE_RULE`（含 3 元/把表头） | ✅ 生效 |
| 2 | 三精肾病医院 | SANJING-SB | 包类型 + 23 行单价 | 24×`BILL_EXPORT_PRICE_RULE` | ✅ 生效 |
| 3 | 哈尔滨华夏眼科医院 | HUAXIA-EYE | **仅包类型列表，无价格明细** | 1×`BILL_EXPORT_PRICE_RULE`（`acceptedTypes` 覆盖所列类型，`unitPrice=2.75`） | ✅ 覆盖明示项（弱：Excel 未给价表） |
| 4 | 哈尔滨市南岗妇产医院 | NG-FUCHAN | 19 行包名固定价 | 19×`PACK_NAME_PRICE` + 1×`BILL_EXPORT_PRICE_RULE` | ✅ 生效 |
| 5 | 哈尔滨市道外区太平人民医院 | TAIPING-RM | 包类型 + **约 25 行敷料/低温/尺寸单价** + 阶梯折扣语义 | 仅 `PIECE_TIER_DISCOUNT`（legacy 把数阶梯）+ 结款侧 `LOGISTICS_WAIVE`；**无 `BILL_EXPORT_PRICE_RULE`** | ❌ 部分/缺口 |
| 6 | 黑龙江省第二医院（南岗区） | ERYY-NG | 标准价七折校对 | `PRICE_VALIDATE_ONLY` rate=0.7 | ✅ 生效 |
| 7 | 黑龙江省第二医院（松北区） | ERYY-SB | 同上 | `PRICE_VALIDATE_ONLY` + 物流规则 | ✅ 生效 |
| 8 | 黑龙江省医院（南岗/香坊） | SHENG-YY-NG / XF | 标准价六折 | 各 1×`DISCOUNT_OVERLAY` | ✅ 生效 |
| 9 | 黑龙江省社会康复医院 | SHKF-YY | 标准价七五折 | `DISCOUNT_OVERLAY` | ✅ 生效 |
| 10 | 武警黑龙江省总队医院 | WUJING-ZD | 零元行纸塑袋/无纺布/环氧乙烷 | 3×`ZERO_ROW_PACKAGING_FEE` 与 Excel 三行一致 | ✅ 生效 |
| 11 | 中医药大学附属第三医院（电力） | ZY3-DIANLI | **账单 sheet 无计算规则** | 账单侧无价表；结款侧见第三节 | ➖ 本项 N/A |

**覆盖摘要：** 11 家中可评 10 家；**9 家配置对齐，1 家（太平）明显缺口**。

### 1.2 生效机制

| 规则类型 | 运行时 |
|----------|--------|
| `BILL_EXPORT_PRICE_RULE` / `PACK_NAME_PRICE` | `ClerkBillPriceRuleApplier`（经 `ClerkRuleExportService`） |
| `PIECE_TIER_DISCOUNT` / `DISCOUNT_OVERLAY` | `ClerkRuleCompiler` → `billingPolicies` → `BillingPolicyApplier`（export_only） |
| `PRICE_VALIDATE_ONLY` | 保留在 `clerkRules`，校对不改价 |
| `ZERO_ROW_PACKAGING_FEE` | `ClerkBillExportApplier` |

### 1.3 缺口清单

| 医院 | CODE | 缺口 |
|------|------|------|
| 哈尔滨市道外区太平人民医院 | TAIPING-RM | Excel 敷料包/无纺布/棉球纱布/纸塑袋尺寸/低温区间价等 **未** 转为 `BILL_EXPORT_PRICE_RULE`；现有 `PIECE_TIER_DISCOUNT` 仅覆盖把数阶梯（含 3 把×16.5→0.891 等），**不能替代整张客服价表**。附件 PDF 已挂 `attachmentRefs`，但未结构化入库。 |

**备注（非阻断）：**

- 道外账单 sheet「是否有结款函规则=有」，但「结款函规则」26 条中无道外——属 **Excel 两表不一致**，不以账单 sheet 标记计为结款函缺口。
- 华夏 Excel 无明细价；若业务期望等同道外全价表，需补源材料后再录规则。

---

## 二、低消保底规则

### 2.1 Excel vs 仓库覆盖（5/5）

| Excel 医院 | CODE | Excel 低消 | baseline `SETTLEMENT_MIN_CHARGE` | 关联配置 | 判定 |
|------------|------|------------|----------------------------------|----------|------|
| 呼兰中医院 | HULAN-TCM | ≥10000；物流 185/次 | `minCharge=10000`，`baseComponents=[sterilize,logistics]`（×2 重复） | `LOGISTICS_FEE` 185（×2）；`SETTLEMENT_PACK_SPLIT` | ✅ |
| 黑龙江维多利亚妇产医院 | VICTORIA | ≥8000；高温五折/低温七折 | `minCharge=8000`（×2） | `SETTLEMENT_DISCOUNT` ht=0.5/lt=0.7 | ✅ |
| 黑龙江九洲妇科医院 | JIUZHOU-FK | ≥3000；减免四次/月；高低温折 | `minCharge=3000`（×2） | `LOGISTICS_WAIVE` freeCount=4（×2）；`SETTLEMENT_DISCOUNT` | ✅ |
| 呼兰区红十字医院 | HULAN-HSZ | ≥1500 | `minCharge=1500`（×2） | — | ✅ |
| 悦美芳华医疗门诊医院 | YUEMEI-FH | ≥1000 | `minCharge=1000`（×2） | — | ✅ |

### 2.2 生效机制

- `ClerkRuleCompiler` case `SETTLEMENT_MIN_CHARGE` → `MONTHLY_SETTLEMENT` 类 `billingPolicies`。
- 结款函展示补差：`SettlementTemplateFiller` 读 job 月度 breakdown / policy。
- 高低温分列：`SettlementTemplateFiller` 对维多利亚/九洲按温区拆「高温灭菌费用」「低温灭菌费用」；折扣由 `SETTLEMENT_DISCOUNT` 编译为 HT/LT `DISCOUNT` policy。

### 2.3 缺口清单

**功能缺口：无。**

| 类型 | 说明 |
|------|------|
| 数据卫生 | 5 家均存在「结款函 sheet + 低消 sheet」双 `sourceRef` 导致 **成对重复** `SETTLEMENT_MIN_CHARGE`（呼兰中医/九洲另有物流重复）。参数一致，一般不改变结果，但增加维护噪音，建议 import 去重。 |

---

## 三、26 大条结款函规则

### 3.1 逐条覆盖总表

| # | Excel 医院 | CODE(s) | Excel 能力摘要 | baseline 关键规则类型 | 判定 |
|---|------------|---------|----------------|----------------------|------|
| 1 | 黑龙江省第二医院（南岗区） | ERYY-NG | 物流 80.5 | `LOGISTICS_FEE` 80.5 | ✅ |
| 2 | 黑龙江省第二医院（松北区） | ERYY-SB | 普通 80.5 / 加急 98 | 多条 `LOGISTICS_FEE` | ✅ |
| 3 | 国药总医院（主/二院区） | GUOYAO-MAIN, GUOYAO-2 | 手术室分类核算 + /5.5 + /15 公式 + 物流卡 | `GUOYAO_SETTLEMENT_FORMULA` + `LOGISTICS_CARD_DEDUCT`；导出另有 `GuoyaoBillExportStrategy` | ✅ 配置+特化策略 |
| 4 | 国药总医院（三院区） | GUOYAO-3 | 同上（无手术室） | 同上 | ✅ |
| 5 | 哈尔滨市第二医院 | HRB-2ND | 不收物流 + 分科室汇总 | `LOGISTICS_WAIVE` + `MONTHLY_SUPPLEMENT_REPORT` | ✅ |
| 6 | 哈尔滨工业大学医院 | HIT-YY | 结款八折 | `SETTLEMENT_DISCOUNT` | ✅ |
| 7 | 哈尔滨市红十字妇产医院 | HRB-HSZ | 单 sheet + 分科室汇总 | `EXPORT_LAYOUT` single_sheet + `MONTHLY_SUPPLEMENT_REPORT` | ✅ |
| 8 | 哈尔滨市第五医院（二门诊） | HRB-WY-EM | 与五院合并结款 | `MERGED_SETTLEMENT`（**params 为空**） | ⚠️ 配置弱 |
| 9 | 呼兰区红十字医院 | HULAN-HSZ | 低消 1500 | `SETTLEMENT_MIN_CHARGE` | ✅ |
| 10 | 呼兰区第一人民医院 | HULAN-RM | 结款七折 | `DISCOUNT_OVERLAY` | ✅ |
| 11 | 呼兰中医院 | HULAN-TCM | 专用格式/备包行 + 低消 10000 + 物流 185 | `SETTLEMENT_PACK_SPLIT` + 低消 + 物流；结款行填充另有 `SettlementTemplateFiller` 硬编码外科包/阑尾包 | ✅ 配置；拆行部分靠硬编码 |
| 12 | 黑龙江九洲妇科医院 | JIUZHOU-FK | 减免 4 次 + 低消 + 高低温 | `LOGISTICS_WAIVE` + 低消 + `SETTLEMENT_DISCOUNT` | ✅ |
| 13 | 哈尔滨仁胜医院 | RENSHENG | 物流卡 + 八折 | `LOGISTICS_CARD_DEDUCT` + `SETTLEMENT_DISCOUNT` | ✅ |
| 14 | 太平人民医院 | TAIPING-RM | 不收物流 | `LOGISTICS_WAIVE` | ✅（结款函条；账单价表见第一节缺口） |
| 15 | 黑龙江维多利亚妇产医院 | VICTORIA | 高低温 + 低消 8000 | 低消 + `SETTLEMENT_DISCOUNT` | ✅ |
| 16 | 香坊中医院+三辅社区 | XIANGFANG-ZY, SANFU-SQ | 合并结款 + 同日物流 50 | `MERGED_SETTLEMENT` + `LOGISTICS_FEE`（SANFU 含 `mergeWith`/`sharedLogisticsFee`） | ✅ |
| 17 | 悦美芳华 | YUEMEI-FH | 低消 1000 | `SETTLEMENT_MIN_CHARGE` | ✅ |
| 18 | 祖研（南岗/香安/三辅） | ZUYAN-NG/XA/SF | 分科室汇总 + 物流 50/25 + 同日合并 + **按科室消毒费比例分摊** | 各院 `MONTHLY_SUPPLEMENT_REPORT` + `LOGISTICS_FEE`（香安 `mergeSameDayWith=ZUYAN-SF`） | ⚠️ 分摊依赖物流管线，规则 JSON 未显式写 allocate 参数 |
| 19 | 中医药大学附属第三医院（电力） | ZY3-DIANLI | 原价/七折并列 + 器械把数汇总 + **消毒灭菌费明细表** | `EXPORT_LAYOUT` showOriginalAndDiscount + `MONTHLY_SUPPLEMENT_REPORT` instrument_count；附件仅两份「器械把数」xlsx | ❌ 缺明细表规则/附件 |
| 20 | 南岗区人民医院（九院） | HRB-NGJY | 结款九折 | `SETTLEMENT_DISCOUNT` | ✅ |
| 21 | 哈尔滨工程大学医院 | HRB-HEU | 结款九折 | `SETTLEMENT_DISCOUNT` | ✅ |
| 22 | 哈尔滨公安医院 | GONGAN-YY | 不收物流 | `LOGISTICS_WAIVE` | ✅ |
| 23 | 五常市人民医院 | WUCHANG-RM | 无物流费 | `LOGISTICS_WAIVE` | ✅ |
| 24 | 黑龙江东大肛肠医院 | DONGDA-GC | 结款七五折 | `SETTLEMENT_DISCOUNT` | ✅ |
| 25 | 中医药大学附属第二医院（南岗） | ZYY-D2-NG | **三张汇总表**（科室金额+物流；按包类型有金额；按包类型无金额） | 仅 1×`MONTHLY_SUPPLEMENT_REPORT` `dept_sterilize_summary` | ❌ |
| 26 | 中医药大学附属第二医院（哈南分院） | ZYY-D2-HN | 同上 | 同上仅 1 种 | ❌ |

**能力完整：约 22/26；明确缺口：3 家（电力明细 + 附二×2）；弱配置：二门诊合并 params、祖研分摊参数。**

### 3.2 生效机制与接线注意

| 能力 | 机制 | 注意 |
|------|------|------|
| 折扣 / 低消 / 物流费·减免 / 物流卡 | Compiler → `billingPolicies` | 主路径清晰 |
| `EXPORT_LAYOUT` | `ClerkExportLayoutMerger` | 单 sheet / 原价七折并列等 |
| `MONTHLY_SUPPLEMENT_REPORT` | `exportSupplementTypes` → 导出能力；ZY3 另靠 `attachmentRefs` 模板追加 | **reportType  alone 不会自动生成附二「三张」**；缺类型即缺能力入口 |
| `GUOYAO_SETTLEMENT_FORMULA` | 保留在 `clerkRules`；账单侧 `GuoyaoBillExportStrategy` | Compiler **无** 专 case；依赖特化策略，配置仍算已声明 |
| `MERGED_SETTLEMENT` / `SETTLEMENT_PACK_SPLIT` | 保留在 `clerkRules` | Compiler **无** 转译；呼兰拆行主要靠 `SettlementTemplateFiller` 硬编码包名 |

### 3.3 缺口清单（医院 + 规则名）

| 医院 | CODE | 缺口规则 / 能力 |
|------|------|-----------------|
| 中医药大学附属第二医院（南岗） | ZYY-D2-NG | Excel「三张汇总表」：缺「按包类型统计科室包及金额」「按包类型统计（无金额）」对应 `MONTHLY_SUPPLEMENT_REPORT`（或等价 reportType） |
| 中医药大学附属第二医院（哈南分院） | ZYY-D2-HN | 同上 |
| 中医药大学附属第三医院（电力） | ZY3-DIANLI | 缺「消毒灭菌费明细表（按消毒方式/包装材料统计包数把数与七折单价金额）」规则与模板附件；现仅有器械把数汇总 + 原价七折并列 |
| 哈尔滨市第五医院（二门诊） | HRB-WY-EM | `MERGED_SETTLEMENT` **未写 `mergeWith` 目标院 CODE**（相对香坊/三辅不完整）——弱缺口 |
| 祖研三院区 | ZUYAN-* | 「物流费按科室消毒费比例分摊」未在 clerk params 中显式建模——需对照 `LogisticsPipelineService`/`LogisticsAllocationExportStrategy` 做运行时验收 |

---

## 四、附一 ZYY-D1 当前状态（Excel 26 条外，如实记录）

`ZYY-D1` 在 `index.json` 中为 alignment 门禁的 **EXTRA**（相对 `docs/clerk-rules-excel-mapping.json` 的 38 家）。

| 项 | 当前状态（2026-09-23） |
|----|------------------------|
| clerk notes | 电话确认：调价/整院折扣类 clerk 规则已封存；对账叠折改由 `billing-rules/baseline/ZYY-D1.json` |
| 仍 active | `EXPORT_LAYOUT`（附一 11 列分科室）、物流、加急、`MONTHLY_SUPPLEMENT_REPORT`×2、`SETTLEMENT_EXTRA` 洗涤费、材料名标准化 |
| 已封存 `isActive=false` | `DISCOUNT_OVERLAY`「整院八折（封存）」；`PRICE_VALIDATE_ONLY`「标准价叠折校对（封存）」 |
| billing | `DISCOUNT` 附一对账八折 0.8 + 九九折 0.99（`applyStage=bill_detail`） |

**不计入** 上文「26 大条」缺口统计。

---

## 五、证据文件路径

| 类别 | 路径 |
|------|------|
| Excel 源/副本 | 微信 temp 原路径；`测试用例/医院内勤规则-20260918.xlsx` |
| clerk 索引 | `backend/src/main/resources/clerk-rules/index.json` |
| clerk baseline | `backend/src/main/resources/clerk-rules/baseline/*.json`（39 家） |
| schema | `backend/src/main/resources/clerk-rules/baseline.schema.json` |
| 附件 | `backend/src/main/resources/clerk-rules/attachments/` |
| 映射/门禁 | `docs/clerk-rules-excel-mapping.json`；`scripts/check_clerk_rules_alignment.py` |
| 编译/应用 | `ClerkRuleCompiler.java`；`ClerkBillPriceRuleApplier.java`；`ClerkRuleExportService.java`；`SettlementTemplateFiller.java`；`GuoyaoBillExportStrategy.java` |
| 既有导出验收（金额，非配置） | `测试用例/clerk_export_verify_report.md` / `.json` |
| 旧版映射文档（结构已过时，sheet 名不同） | `docs/内勤规则Excel映射表-20260918.md` |
| 附一 billing | `backend/src/main/resources/billing-rules/baseline/ZYY-D1.json` |

---

## 六、建议下一步

1. **补录太平账单价表**：按 Excel「账单规则」块生成 `TAIPING-RM` 的 `BILL_EXPORT_PRICE_RULE`（可参考 `DAOWAI-RM`/`SANJING-SB`），保留现有 `PIECE_TIER_DISCOUNT`；补后跑 `ClerkBillPriceRuleApplier` 单测 + 太平样例导出。
2. **补齐附二三张汇总**：为 `ZYY-D2-NG` / `ZYY-D2-HN` 增加缺失 `reportType`（或确认现有导出策略能否一张表覆盖三张，若不能则分三条配置）。
3. **电力消毒灭菌费明细表**：补 `MONTHLY_SUPPLEMENT_REPORT` + 真实模板附件（勿再重复挂两份器械把数 xlsx）。
4. **低消/物流去重**：import 时对同参 `SETTLEMENT_MIN_CHARGE` / `LOGISTICS_*` 合并 sourceRef。
5. **弱配置加固**：`HRB-WY-EM` 补 `mergeWith`；评估 `GUOYAO_SETTLEMENT_FORMULA` / `MERGED_SETTLEMENT` / `SETTLEMENT_PACK_SPLIT` 是否应在 Compiler 中显式转译，减少「JSON 有、主路径不读」风险。
6. **验收分层**：配置门禁（本报告）与 `clerk_export_verify` 金额对账分开汇报；金额失败优先查计价/导出，勿与「规则未入库」混为一谈。

---

## 七、缺口数量摘要

| 核查项 | 结论 | 关键缺口数 |
|--------|------|------------|
| 账单规则 | 部分生效 | **1 家重大**（TAIPING 价表明细未入库）；华夏为 Excel 无明细下的弱配置（不计重大缺口） |
| 低消保底 | 完全配置生效 | **功能缺口 0**；**卫生问题 5 家**（规则重复） |
| 结款函 26 条 | 部分生效 | **明确能力缺口 3 家**（ZYY-D2-NG、ZYY-D2-HN、ZY3 明细表）；**弱配置 2 类**（二门诊 mergeWith 空、祖研分摊参数未显式） |

*报告生成：对照 Excel 20260918 + 仓库 clerk-rules（含 2026-09-23 ZYY-D1 封存状态），未 commit、未部署。*
