# 内勤规则缺口补齐验收（2026-09-23）

对照 [内勤规则配置生效核查报告-20260923.md](./内勤规则配置生效核查报告-20260923.md) 的缺口项，本次补齐结果如下。

## 总览

| 核查项 | 补齐前 | 补齐后 |
|--------|--------|--------|
| 账单规则（太平价表） | 仅阶梯折扣+不收物流 | **24 条** `BILL_EXPORT_PRICE_RULE` + 保留 `PIECE_TIER_DISCOUNT` |
| 低消重复 | 5 家双源重复 | **去重为 1 条/类型**（九洲等已验证） |
| 附二三张汇总 | 1 条非法 `dept_sterilize_summary` | **3 条**合法 `dept_summary` / `price_summary` / `instrument_audit` |
| 电力消毒明细 | 缺 | 新增 `sterilize_fee_detail` 导出策略 + clerk 规则 |
| 非法 reportType | 8 处 | **0**（`scripts/check_clerk_report_types.py` 通过） |
| 二门诊合并结款 | `mergeWith` 空 | **`mergeWith: HRB-WY`** |
| 祖研分摊 | 未显式 | `allocateBy: sterilize_fee_ratio` + `logistics_allocation` |

## 变更文件

### 脚本
- `scripts/clerk_rules_import_v2.py` — 按 code 合并 rules、去重、三张汇总、太平 75 折跳过
- `scripts/apply_clerk_gap_baselines.py` — 一次性 baseline 补丁
- `scripts/check_clerk_report_types.py` — reportType 门禁

### Baseline（节选）
- `clerk-rules/baseline/TAIPING-RM.json` — 24 条价表
- `clerk-rules/baseline/ZYY-D2-NG.json` / `ZYY-D2-HN.json` — 三条附表
- `clerk-rules/baseline/ZY3-DIANLI.json` — 器械把数 + 消毒明细
- `clerk-rules/baseline/HRB-WY-EM.json` — mergeWith
- `clerk-rules/baseline/ZUYAN-*.json` — dept_summary + logistics_allocation
- 低消 5 家 — 重复规则合并

### 后端
- `ClerkBillPriceRuleApplier` — 支持 `N＞3` 全角区间
- `ClerkRuleCompiler` — reportType 别名清洗
- `ExportType` / `SterilizeFeeDetailExportStrategy` — 电力明细导出
- `hospital-export-capabilities.json`

### 前端
- `hospitalExportCapabilities.ts` — 与后端 capabilities 同步

### 测试
- `ClerkBillPriceRuleApplierTest` — 太平敷料/纸塑袋
- `ClerkRuleCompilerTest` — 附二/电力/九洲去重
- `HospitalExportCapabilityServiceTest` — 附二/电力 capabilities
- `SterilizeFeeDetailExportStrategyTest` — 七折金额

## 门禁命令

```bash
python3 scripts/check_clerk_report_types.py
python3 scripts/check_clerk_rules_alignment.py
cd backend && mvn -q -Dtest=ClerkBillPriceRuleApplierTest,ClerkRuleCompilerTest,HospitalExportCapabilityServiceTest,SterilizeFeeDetailExportStrategyTest test
```

## 未纳入本次

- 全量 37 院 `clerk_export_verify` 金额翻绿（配置补齐后的独立对账）
- 附一 ZYY-D1 叠折（另案已处理）
