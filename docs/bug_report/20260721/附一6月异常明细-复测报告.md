# 附一 6 月异常明细 — 复测报告（2026-07-22）

## 测试材料

| 文件 | 用途 |
|------|------|
| `附一6月账单 未改.xlsx` | 原始账单（客户复测用） |
| `异常明细_-hospital-1784686884766.xlsx` | 客户反馈的 45 条异常/修正 |
| `测试用例/…/6月__附一6月账单__附一6月账单.xlsx` | 处理后账单（ground truth） |

## 环境说明

- 本地 backend（Docker）+ 已加载 **ZYY-D1 P0 规则**（停用宽泛「无纺布按把 4.4」误匹配）
- 客户反馈时使用的是 **未更新规则库的旧环境**（见对话说明）

## 复测结论（摘要）

| 指标 | 旧环境（客户） | 当前系统（本次复测） |
|------|----------------|----------------------|
| warning 总数 | 约 **200** | **45** |
| 无纺布/敷料误报（处理后价未变仍报） | **175** | **0** |
| 与客户异常表同集合的 warning | 45 | **45** |
| 校正价与处理后账单不一致 | 至少 **5**（王树人 118.8、张景欣 39.6、辅料包 4.4 等） | **0**（四舍五入对齐后） |

**结论：客户反馈中的「大量不对修正」主因是旧规则下 `无纺布按把4.4` 误报；在当前规则下该问题已不存在。**  
异常表中的 45 条与「原始 vs 处理后确实需改价」的 ground truth 一致，属于**应复核**行，而非误报。

## 45 条与客户表对照

- 导入 job #356（1850 行），warning **45** 条
- 与客户 `异常明细` 表 **45** 行一一对应（包名/发货单/修正价一致）

## 仍存在的细微差异（已修复于代码）

处理后账单常用 **四舍五入展示价**（如 30.4、17.6、20.0），规则曾用收费规则 Sheet2 精确价（30.38、17.58、19.98），界面「修正单价」差 0.02 易被客户认为「算错」。

已对齐：

- `30°腹腔镜组合价`：30.38 → **30.4**
- `保温杯-1Z2044`：17.58 → **17.6**
- `低温袋15cm`（含膀胱取石钳）：19.98 → **20**

补丁种子：`billing-seeds/phase-zyy-d1-p0-2-price-align.json`（marker `billing_seed_zyy_d1_p0_2_v1`）

## 业务说明（换药包 22.6 → 21.99）

- 处理后 6 月账单目标价为 **21.99**（与收费规则 Sheet2 一致）
- 收费规则 Sheet1 曾写合计 **22**，以医院已确认的处理后 Excel 为准
- 当前 warning 符合 ground truth，**不建议改回 22.6**

## 建议客户验证步骤

1. 确认生产/测试环境已部署含 P0 + `zyy_d1_p0_2` 的后端并重启
2. 重新导入 `附一6月账单 未改.xlsx`
3. 异常明细应约为 **45 条**，且不应再出现洗手服/持物钳罐等 **13.2→8.8** 类误报

## 复现命令（开发）

```bash
# 重启 backend 使种子生效后
python3 - <<'PY'
# 见 scripts/batch_june_system_test.py import_bill + fetch_warnings
PY
```
