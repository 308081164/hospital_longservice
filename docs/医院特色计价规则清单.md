# 医院特色计价规则清单

> 自动生成，供与客户逐项核对。修改 seed 后请运行：
> `python3 scripts/billing_rules_catalog.py --write`

- **生成时间**：2026-09-17 02:10 UTC
- **Manifest hash**：`852cc04b0ded725c…`
- **客户数**：13 · **规则总数**：10（启用 10）
- **清单收录**：7 家启用客户

- **规范名重复审计**：无（启用客户规范名均唯一）

## 计价模式说明

| 模式 | 含义 |
|------|------|
| `special_only` | 仅特色规则；未命中则保留原价 |
| `hybrid` | 混合；未命中则走标准灭菌价 |
| `standard` | 标准灭菌定价表 |

---

## 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`）

- **计价模式**：`special_only` — 仅特色规则：未命中规则时保留原价
- **规则数**：启用 2 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 整形包54.5 | 固定单价 | 包名含「整形包、脂充包」；排除：整形手术包；→ 固定 **54.5 元/包**；（不计包装费）；（不计折扣） |
| 环钻27.5 | 固定单价 | 包名含「环钻包」；排除：环钻头；→ 固定 **27.5 元/包**；（不计包装费）；（不计折扣） |

## 呼兰中医院（`HULAN-TCM`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 2 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 外科包固定价 | 固定单价 | 包名含「外科包」；→ 固定 **249.5 元/包**；（不计包装费）；（不计折扣） |
| 阑尾包固定价 | 固定单价 | 包名含「阑尾包」；→ 固定 **288 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江九洲妇科医院（`JIUZHOU-FK`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 方盘5.5 | 固定单价 | 包名含「方盘」；→ 固定 **5.5 元/包**；（不计包装费）；（不计折扣） |

## 哈尔滨汽轮机医院（`QILUNJI-YY`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 汽轮机10mm30度镜固定价 | 固定单价 | 包名含「30度镜@contains、10mm@contains、10毫米30度镜@contains」；→ 固定 **28 元/包**；（不计包装费）；（不计折扣） |

## 新发红十字医院（`XINFA-HSZ`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 穿刺器帽22 | 固定单价 | 包名含「穿刺器帽」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |

## 祖研-黑龙江省中医医院（南岗院区）（`ZUYAN-NG`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 排针10盘算1件 | 折算规则 | 包名含「排针」；→ **10 件算 5.5 件** |

## 黑龙江省中医药大学附属第三医院（电力）（`ZY3-DIANLI`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 2 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 小件盒25 | 固定单价 | 包名含「小件盒」；→ 固定 **25 元/包**；（不计包装费）；（不计折扣） |
| 等离子镜36 | 固定单价 | 包名含「等离子镜」；→ 固定 **36 元/包**；（不计包装费）；（不计折扣） |

