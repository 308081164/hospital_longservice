# 医院特色计价规则清单

> 自动生成，供与客户逐项核对。修改 seed 后请运行：
> `python3 scripts/billing_rules_catalog.py --write`

- **生成时间**：2026-08-03 08:46 UTC
- **Manifest hash**：`adad0a3317f843c9…`
- **客户数**：53 · **规则总数**：296（启用 272）

## 计价模式说明

| 模式 | 含义 |
|------|------|
| `special_only` | 仅特色规则；未命中则保留原价 |
| `hybrid` | 混合；未命中则走标准灭菌价 |
| `standard` | 标准灭菌定价表 |

---

## 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`）

- **计价模式**：`special_only` — 仅特色规则：未命中规则时保留原价
- **规则数**：启用 5 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 环钻包30.5 | 固定单价 | 包名含「环钻包」；→ 固定 **30.5 元/包**；（不计包装费）；（不计折扣） |
| 整形包58 | 固定单价 | 包名含「整形包、整形手术包」；排除：脂充包；→ 固定 **58 元/包**；（不计包装费）；（不计折扣） |
| 整形包54.5 | 固定单价 | 包名含「整形包、脂充包」；排除：整形手术包；→ 固定 **54.5 元/包**；（不计包装费）；（不计折扣） |
| 环钻27.5 | 固定单价 | 包名含「环钻」；→ 固定 **27.5 元/包**；（不计包装费）；（不计折扣） |
| ≥3件按件5.5元 | 按件计价 | 包名含「（无关键词，靠产品绑定或全局）」；→ **5.5 元/件**；（不计包装费）；（不计折扣） |

## 哈尔滨长健医院（`CHANGJIAN`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 长健结款单行对齐 | 固定单价 | 包名含「刀柄」；→ 固定 **50 元/包**；（不计包装费）；（不计折扣） |

## 道外区人民医院（`DAOWAI-RM`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 14 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价2.0 | 固定单价 | 包名含「棉球」；→ 固定 **2 元/包**；（不计包装费）；（不计折扣） |
| 校正价3.0 | 固定单价 | 包名含「剪刀-1、剪刀小-1、卵圆钳-1、弯盘-1、持针器-1、止血钳-1…」；→ 固定 **3 元/包**；（不计包装费）；（不计折扣） |
| 校正价6.0 | 固定单价 | 包名含「持物钳罐-2/持物钳放外面W6050」；→ 固定 **6 元/包**；（不计包装费）；（不计折扣） |
| 校正价6.5 | 固定单价 | 包名含「敷料、敷料包」；→ 固定 **6.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价9.0 | 固定单价 | 包名含「美容包-3件、美容小包-3件」；→ 固定 **9 元/包**；（不计包装费）；（不计折扣） |
| 校正价12.0 | 固定单价 | 包名含「止血钳-4件」；→ 固定 **12 元/包**；（不计包装费）；（不计折扣） |
| 校正价18.0 | 固定单价 | 包名含「美容包-6件」；→ 固定 **18 元/包**；（不计包装费）；（不计折扣） |
| 校正价30.0 | 固定单价 | 包名含「取环包-10件、外伤包-10件」；→ 固定 **30 元/包**；（不计包装费）；（不计折扣） |
| 校正价33.0 | 固定单价 | 包名含「取环包-11件」；→ 固定 **33 元/包**；（不计包装费）；（不计折扣） |
| 校正价126.0 | 固定单价 | 包名含「阑尾器械-41件（筐1）」；→ 固定 **126 元/包**；（不计包装费）；（不计折扣） |
| PDF外来器械骨电钻12.5 | 固定单价 | 包名含「外来器械（骨电钻）」；→ 固定 **12.5 元/包**；（不计包装费）；（不计折扣） |
| PDF外来器械钉盒37.5 | 固定单价 | 包名含「外来器械（钉盒）」；→ 固定 **37.5 元/包**；（不计包装费）；（不计折扣） |
| PDF/期待价2 | 固定单价 | 包名含「棉球」；→ 固定 **2 元/包**；（不计包装费）；（不计折扣） |
| PDF/期待价6.5 | 固定单价 | 包名含「敷料、敷料包」；→ 固定 **6.5 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江省第二医院（南岗院区）（`ERYY-NG`）

- **计价模式**：`special_only` — 仅特色规则：未命中规则时保留原价
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 省二南岗挖勺每件5.5元 | 按件计价 | 包名含「挖勺」；→ **5.5 元/件**；（不计包装费） |

## ERYY-SB（`ERYY-SB`）

- **计价模式**：`special_only` — 仅特色规则：未命中规则时保留原价
- **规则数**：启用 8 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 手术衣无纺布现价 | 固定单价 | 包名含「手术衣」；→ 固定 **26.6 元/包**；（不计包装费）；（不计折扣） |
| 手术衣纸塑袋现价 | 固定单价 | 包名含「手术衣」；→ 固定 **28 元/包**；（不计包装费）；（不计折扣） |
| 儿童头钉现价 | 固定单价 | 包名含「儿童头钉」；→ 固定 **15.4 元/包**；（不计包装费）；（不计折扣） |
| 取钉包多报价 | 固定单价 | 包名含「取钉包」；→ 固定 **207.9 元/包**；（不计包装费）；（不计折扣） |
| 省二松北宫腔检查镜30度3件 | 固定单价 | 包名含「宫腔检查镜30度」；→ 固定 **30.8 元/包**；（不计包装费）；（不计折扣） |
| 省二松北扩棒7-10 | 固定单价 | 包名含「扩棒（7-10）」；→ 固定 **26.95 元/包**；（不计包装费）；（不计折扣） |
| 省二松北扩棒7.5-10 | 固定单价 | 包名含「扩棒（7.5-10）」；→ 固定 **23.1 元/包**；（不计包装费）；（不计折扣） |
| 省二松北取钉包 | 固定单价 | 包名含「取钉包」；→ 固定 **207.9 元/包**；（不计包装费）；（不计折扣） |

## 国药总医院第二院区（`GUOYAO-2`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价8.0 | 固定单价 | 包名含「缝合针-6、缝合针-8」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |

## 国药总医院第三院区（`GUOYAO-3`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价35.0 | 固定单价 | 包名含「辅料包」；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |

## 国药总医院主院区（`GUOYAO-MAIN`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 3 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 10mm30度镜固定价 | 固定单价 | 包名含「30度镜、10mm」；→ 固定 **28 元/包**；（不计包装费）；（不计折扣） |
| 校正价35.0 | 固定单价 | 包名含「辅料包」；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |
| 驱血带固定价 | 固定单价 | 包名含「驱血带」；→ 固定 **13 元/包**；（不计包装费）；（不计折扣） |

## HLFB-SF（`HLFB-SF`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价13.5 | 固定单价 | 包名含「车针」；→ 固定 **13.5 元/包**；（不计包装费）；（不计折扣） |

## 省监狱管理局医院（`HLJ-JYGLJ-YY`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 微克夹钳35元 | 固定单价 | 包名含「微克夹钳」；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |

## 哈尔滨市第二医院（`HRB-2ND`）

- **计价模式**：`special_only` — 仅特色规则：未命中规则时保留原价
- **规则数**：启用 7 条，已停用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 口腔科正畸车针8元 | 固定单价 | 包名含「正畸去胶车针、正畸去版车针」；科室：口腔科 / 口腔科(内) / 口腔科（正） / 口腔修复诊室；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 口腔科调刀8元 | 固定单价 | 包名含「调刀」；科室：口腔科 / 口腔科(内) / 口腔科（正） / 口腔修复诊室；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 手术室止血带8元 | 固定单价 | 包名含「市二院止血带」；科室：手术室；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 手术室电凝钩22元/件 | 按件计价 | 包名含「电凝钩吸引器」；科室：手术室；→ **22 元/件**；（不计包装费）；（不计折扣） |
| 校正价8.0 | 固定单价 | 包名含「市二院止血带14个（只消毒）、洁牙尖（保护）1抛光杯1」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 校正价16.5 | 固定单价 | 包名含「骨科拉钩-2」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| PDF/期待价8 | 固定单价 | 包名含「市二院止血带14个（只消毒）」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |

<details><summary>已停用规则</summary>

| 规则名 | 说明 |
|--------|------|
| 校正价5.5 | 包名含「正畸去胶车针-1」；→ 固定 **5.5 元/包**；（不计包装费）；（不计折扣） |

</details>

## 哈尔滨长健医院（`HRB-CJ`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 3 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 长健敷料包W12050 | 固定单价 | 包名含「敷料包/W12050」；→ 固定 **35 元/包**；（不计包装费） |
| 长健硅胶珠子22 | 固定单价 | 包名含「硅胶珠子7号」；→ 固定 **22 元/包**；（不计包装费） |
| 手术包5.5元/件 | 按件计价 | 包名含「手术包」；→ **5.5 元/件**；（不计包装费）；（不计折扣） |

## 哈尔滨工业大学医院（`HRB-HIT`）

- **计价模式**：`special_only` — 仅特色规则：未命中规则时保留原价
- **规则数**：启用 9 条，已停用 5 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 哈工大export 胆囊包10件165 | 固定单价 | 包名含「胆囊包10件（盒1）」；仅导出账单生效；→ 固定 **165 元/包**；（不计包装费）；（不计折扣） |
| 哈工大换药碗-1 13 | 固定单价 | 包名含「换药碗-1」；→ 固定 **13 元/包**；（不计包装费）；（不计折扣） |
| 哈工大换药碗-2 16.5 | 固定单价 | 包名含「换药碗-2」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 哈工大棉球不带线2.5 | 固定单价 | 包名含「棉球（不带线）、棉球(不带线)」；→ 固定 **2.5 元/包**；（不计包装费）；（不计折扣） |
| 哈工大种植盒23件5.5/件 | 按件计价 | 包名含「种植盒-23件」；→ **5.5 元/件**；（不计包装费）；（不计折扣） |
| 克氏针5.5 | 固定单价 | 包名含「克氏针-3/z7534」；→ 固定 **5.5 元/包**；（不计包装费）；（不计折扣） |
| 车针多报价 | 固定单价 | 包名含「车针」；→ 固定 **13.5 元/包**；（不计包装费）；（不计折扣） |
| 口腔针类5.5 | 固定单价 | 包名含「洁牙尖、成型片」；排除：泪道探针、探针包；→ 固定 **5.5 元/包**；（不计包装费）；（不计折扣） |
| 口腔小件5件计1 | 折算规则 | 包名含「洁牙尖、成型片」；→ **5 件算 5 件**；（不计包装费）；（不计折扣） |

<details><summary>已停用规则</summary>

| 规则名 | 说明 |
|--------|------|
| 哈工大export 换药碗13 | 包名含「换药碗-1/z2032」；仅导出账单生效；→ 固定 **13 元/包**；（不计包装费）；（不计折扣） |
| 哈工大export 种植盒23件27.5 | 包名含「种植盒-23件 盒1/w6050」；仅导出账单生效；→ 固定 **27.5 元/包**；（不计包装费）；（不计折扣） |
| 哈工大换药碗13 | 包名含「换药碗」；→ 固定 **13 元/包**；（不计包装费）；（不计折扣） |
| 哈工大种植盒23件27.5 | 包名含「种植盒-23件」；→ 固定 **27.5 元/包**；（不计包装费）；（不计折扣） |
| 哈工大胆囊包10件165 | 包名含「胆囊包10件」；→ 固定 **165 元/包**；（不计包装费）；（不计折扣） |

</details>

## 哈尔滨市红十字妇产医院（`HRB-HSZ`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 4 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 低温1件22元 | 固定单价 | 包名含「（无关键词，靠产品绑定或全局）」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 湿化瓶2件22元 | 固定单价 | 包名含「湿化瓶」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 纤维喉镜300 | 固定单价 | 包名含「纤维喉镜、气管镜、软管」；→ 固定 **300 元/包**；（不计包装费）；（不计折扣） |
| T型管25 | 固定单价 | 包名含「T型管」；→ 固定 **25 元/包**；（不计包装费）；（不计折扣） |

## 哈尔滨华夏眼科医院（`HRB-HX-EYE`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| ≥3件器械包单价2.75 | 按件计价 | 包名含「（无关键词，靠产品绑定或全局）」；→ **2.75 元/件**；（不计包装费） |

## HRB-MHM（`HRB-MHM`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 美涵吸脂针20cm以下5件算1件 | 折算规则 | 包名含「型号20cm以下、20cm以下」；→ **5 件算 5 件** |

## 哈尔滨市南岗区人民医院（九院）（`HRB-NGJY`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 九院敷料包0元覆盖15 | ZERO_PRICE_OVERRIDE | 包名含「敷料包」；→ 价格 15；（不计包装费）；（不计折扣） |

## HRB-SD-MB（`HRB-SD-MB`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 2 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 松电车针5件算1件 | 折算规则 | 包名含「车针」；→ **5 件算 5 件** |
| 松电口腔科针类5件算1件 | 折算规则 | 包名含「车针、扩大针、机扩针、根管针、根管锉、镍钛锉…」；科室：口腔科；→ **5 件算 5 件** |

## 哈尔滨森海医院（`HRB-SH`）

- **计价模式**：`special_only` — 仅特色规则：未命中规则时保留原价
- **规则数**：启用 2 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 胶帽22元/件 | 按件计价 | 包名含「胶帽」；→ **22 元/件**；（不计包装费）；（不计折扣） |
| 纱布4元 | 固定单价 | 包名含「纱布」；→ 固定 **4 元/包**；（不计包装费）；（不计折扣） |

## 哈尔滨市第五医院（`HRB-WY`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 12 条，已停用 8 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 市五export DMD光源1号35 | 固定单价 | 包名含「DMD光源1号-1/W905060」；仅导出账单生效；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |
| 市五export DMD光源3号35 | 固定单价 | 包名含「DMD光源3号-1/W905060」；仅导出账单生效；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |
| 市五export 磨头Z102688 | 固定单价 | 包名含「骨四（2）邹天彪磨头-4件/Z1026」；仅导出账单生效；→ 固定 **88 元/包**；（不计包装费）；（不计折扣） |
| 市五export 胸外镜头W90605035 | 固定单价 | 包名含「胸外镜头-1（盒1）/W906050」；仅导出账单生效；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |
| 市五export 胸腔镜99 | 固定单价 | 包名含「胸腔镜-17件（筐1）」；仅导出账单生效；→ 固定 **99 元/包**；（不计包装费）；（不计折扣） |
| 市五export 骨六电钻16.5 | 固定单价 | 包名含「骨六业皋电钻-2件/W705060」；仅导出账单生效；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价16.5 | 固定单价 | 包名含「AR水管-2件/双、德美2号刨刀-1/双、德美4K1号刨刀-1、旧轧皮机（2号）-2件盒1、西山骨动力1号-1/双、试用肩镜-2件/双…」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价22.0 | 固定单价 | 包名含「半月板缝合器械-4」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 校正价44.0 | 固定单价 | 包名含「张长青超声刀线-2件」；→ 固定 **44 元/包**；（不计包装费）；（不计折扣） |
| 校正价88.0 | 固定单价 | 包名含「创伤1号机器人-4件（盒1）」；→ 固定 **88 元/包**；（不计包装费）；（不计折扣） |
| 校正价99.0 | 固定单价 | 包名含「胸腔镜-17件（筐1）」；→ 固定 **99 元/包**；（不计包装费）；（不计折扣） |
| 校正价132.0 | 固定单价 | 包名含「骨六房玉利肩钩常备器械」；→ 固定 **132 元/包**；（不计包装费）；（不计折扣） |

<details><summary>已停用规则</summary>

| 规则名 | 说明 |
|--------|------|
| 市五W605060 export11 | 包名含「W605060」；仅导出账单生效；→ 固定 **11 元/包** |
| 市五W705060 export16.5 | 包名含「W705060」；仅导出账单生效；→ 固定 **16.5 元/包** |
| 市五W905060 export35 | 包名含「W905060」；仅导出账单生效；→ 固定 **35 元/包** |
| 市五W906050 export35 | 包名含「W906050」；仅导出账单生效；→ 固定 **35 元/包** |
| 市五Z1026 export88 | 包名含「Z1026」；仅导出账单生效；→ 固定 **88 元/包** |
| 市五export 胸腔镜121 | 包名含「胸腔镜-17件（筐1）」；仅导出账单生效；→ 固定 **121 元/包**；（不计包装费）；（不计折扣） |
| 市五export 骨四咬钳16.5 | 包名含「骨四1 宋晓辉韧带咬钳 2件/W605060」；仅导出账单生效；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 市五胸腔镜121 | 包名含「胸腔镜-17件」；仅导出账单生效；→ 固定 **121 元/包** |

</details>

## 哈尔滨市第五医院（二门诊）（`HRB-WY-EM`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 6 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价16.5 | 固定单价 | 包名含「AR水管-2件/双、德美2号刨刀-1/双、德美4K1号刨刀-1、旧轧皮机（2号）-2件盒1、西山骨动力1号-1/双、试用肩镜-2件/双…」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价22.0 | 固定单价 | 包名含「半月板缝合器械-4」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 校正价44.0 | 固定单价 | 包名含「张长青超声刀线-2件」；→ 固定 **44 元/包**；（不计包装费）；（不计折扣） |
| 校正价88.0 | 固定单价 | 包名含「创伤1号机器人-4件（盒1）」；→ 固定 **88 元/包**；（不计包装费）；（不计折扣） |
| 校正价99.0 | 固定单价 | 包名含「胸腔镜-17件（筐1）」；→ 固定 **99 元/包**；（不计包装费）；（不计折扣） |
| 校正价132.0 | 固定单价 | 包名含「骨六房玉利肩钩常备器械」；→ 固定 **132 元/包**；（不计包装费）；（不计折扣） |

## 呼兰区红十字医院（`HULAN-HSZ`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价8.0 | 固定单价 | 包名含「车针」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |

## 呼兰区第一人民医院（`HULAN-RM`）

- **计价模式**：`special_only` — 仅特色规则：未命中规则时保留原价
- **规则数**：启用 9 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| PDF敷料小20x20x15 | 固定单价 | 包名含「敷料、20cm*20cm*15cm」；→ 固定 **10.5 元/包**；（不计包装费）；（不计折扣） |
| PDF敷料中20x30x30 | 固定单价 | 包名含「敷料、20cm*30cm*30cm」；→ 固定 **14 元/包**；（不计包装费）；（不计折扣） |
| PDF纱布棉球大20x20x15 | 固定单价 | 包名含「纱布、棉球」；→ 固定 **28 元/包**；（不计包装费）；（不计折扣） |
| PDF纸塑袋15cm | 固定单价 | 包名含「纸塑袋」；→ 固定 **3.85 元/包**；（不计包装费）；（不计折扣） |
| PDF纸塑袋10cm | 固定单价 | 包名含「纸塑袋」；→ 固定 **3.56 元/包**；（不计包装费）；（不计折扣） |
| PDF外来器械骨电钻11.55 | 固定单价 | 包名含「外来器械（骨电钻）」；→ 固定 **11.55 元/包**；（不计包装费）；（不计折扣） |
| PDF外来器械植入物3.85 | 固定单价 | 包名含「外来器械（植入物）」；→ 固定 **3.85 元/包**；（不计包装费）；（不计折扣） |
| PDF高温器械柜保护套3.5 | 固定单价 | 包名含「保护套、器械柜」；→ 固定 **3.5 元/包**；（不计包装费）；（不计折扣） |
| PDF低温器械柜保护套5.6 | 固定单价 | 包名含「保护套、器械柜」；→ 固定 **5.6 元/包**；（不计包装费）；（不计折扣） |

## 呼兰中医院（`HULAN-TCM`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 4 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 呼兰中医export 腹腔镜297 | 固定单价 | 包名含「外科腹腔镜-16件筐1/W12050」；仅导出账单生效；→ 固定 **297 元/包**；（不计包装费）；（不计折扣） |
| 外科包固定价 | 固定单价 | 包名含「外科包」；→ 固定 **249.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价275.0 | 固定单价 | 包名含「外科腹腔镜-16件筐1」；→ 固定 **275 元/包**；（不计包装费）；（不计折扣） |
| 阑尾包固定价 | 固定单价 | 包名含「阑尾包」；→ 固定 **288 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江九洲妇科医院（`JIUZHOU-FK`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 方盘5.5 | 固定单价 | 包名含「方盘」；→ 固定 **5.5 元/包**；（不计包装费）；（不计折扣） |

## 哈尔滨基准生物科技有限公司（`JZSW-BIO`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 氩氦刀（EO）150元 | 固定单价 | 包名含「氩氦刀」；→ 固定 **150 元/包**；（不计包装费）；（不计折扣） |

## NEAU-YY（`NEAU-YY`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 2 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 东北农大车针5件算1件 | 折算规则 | 包名含「车针」；→ **5 件算 5 件** |
| 东北农大口腔科针类5件算1件 | 折算规则 | 包名含「车针、扩大针、机扩针、根管针、根管锉、镍钛锉…」；科室：口腔科；→ **5 件算 5 件** |

## 南岗区妇产医院（`NG-FUCHAN`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 19 条，已停用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价2.3 | 固定单价 | 包名含「纱布」；→ 固定 **2.3 元/包**；（不计包装费）；（不计折扣） |
| 校正价8.0 | 固定单价 | 包名含「取环器-1、宫颈钳-1」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 校正价16.0 | 固定单价 | 包名含「弯盘-2」；→ 固定 **16 元/包**；（不计包装费）；（不计折扣） |
| 校正价24.0 | 固定单价 | 包名含「扩棒（4 4.5 5）-3」；→ 固定 **24 元/包**；（不计包装费）；（不计折扣） |
| 校正价弯盘8 | 固定单价 | 包名含「弯盘」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 校正价扩棒8 | 固定单价 | 包名含「扩棒」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 校正价32.0 | 固定单价 | 包名含「盆2盘1杯1(腹腔镜里的)」；→ 固定 **32 元/包**；（不计包装费）；（不计折扣） |
| 校正价170.5 | 固定单价 | 包名含「宫腔镜」；→ 固定 **170.5 元/包**；（不计包装费）；（不计折扣） |
| PDF带架治疗盘16.5 | 固定单价 | 包名含「带架治疗」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 南岗妇产棉球4 | 固定单价 | 包名含「棉球/Z2032」；→ 固定 **4 元/包**；（不计包装费）；（不计折扣） |
| 校正价扩棒6.5套8 | 固定单价 | 包名含「扩棒（6.5 7 7.5）-3件z7526」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| PDF小治疗16 | 固定单价 | 包名含「小治疗」；→ 固定 **16 元/包**；（不计包装费）；（不计折扣） |
| PDF取环包33 | 固定单价 | 包名含「取环包」；→ 固定 **33 元/包**；（不计包装费）；（不计折扣） |
| PDF上环包33 | 固定单价 | 包名含「上环包」；→ 固定 **33 元/包**；（不计包装费）；（不计折扣） |
| PDF药流包27.5 | 固定单价 | 包名含「药流包」；→ 固定 **27.5 元/包**；（不计包装费）；（不计折扣） |
| PDF人工流产33 | 固定单价 | 包名含「人工流产」；→ 固定 **33 元/包**；（不计包装费）；（不计折扣） |
| PDF人流包33 | 固定单价 | 包名含「人流包」；→ 固定 **33 元/包**；（不计包装费）；（不计折扣） |
| PDF诊刮包8 | 固定单价 | 包名含「诊刮」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| PDF大领子8 | 固定单价 | 包名含「大领子」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |

<details><summary>已停用规则</summary>

| 规则名 | 说明 |
|--------|------|
| 南岗妇产export 扩棒16.5 | 包名含「扩棒（6.5 7 7.5）-3件z7526」；仅导出账单生效；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |

</details>

## 三精肾病医院（`SANJING-SB`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 11 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 三精export 内瘘68件102 | 固定单价 | 包名含「内瘘器械包（一）」；仅导出账单生效；→ 固定 **102 元/包**；（不计包装费）；（不计折扣） |
| 校正价6.4 | 固定单价 | 包名含「哈巴狗止血夹-1」；→ 固定 **6.4 元/包**；（不计包装费）；（不计折扣） |
| 校正价9.0 | 固定单价 | 包名含「开口器1 舌钳子1 压舌板1、持物钳罐-3件」；→ 固定 **9 元/包**；（不计包装费）；（不计折扣） |
| 校正价30.0 | 固定单价 | 包名含「插管包二」；→ 固定 **30 元/包**；（不计包装费）；（不计折扣） |
| 校正价42.0 | 固定单价 | 包名含「插管包」；→ 固定 **42 元/包**；（不计包装费）；（不计折扣） |
| 校正价99.0 | 固定单价 | 包名含「内瘘器械包（一）」；→ 固定 **99 元/包**；（不计包装费）；（不计折扣） |
| 校正价102.0 | 固定单价 | 包名含「腹透包-34件（临时）」；→ 固定 **102 元/包**；（不计包装费）；（不计折扣） |
| 校正价102.0-内瘘68件 | 固定单价 | 包名含「内瘘器械包（一）」；→ 固定 **102 元/包**；（不计包装费）；（不计折扣） |
| PDF外来器械4.4 | 固定单价 | 包名含「外来器械」；排除：钉盒、骨电钻、植入物；→ 固定 **4.4 元/包**；（不计包装费）；（不计折扣） |
| PDF外来器械钉盒40 | 固定单价 | 包名含「外来器械（钉盒）」；→ 固定 **40 元/包**；（不计包装费）；（不计折扣） |
| PDF外来器械骨电钻13.2 | 固定单价 | 包名含「外来器械（骨电钻）」；→ 固定 **13.2 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江省医院（南岗院区）（`SHENG-YY-NG`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 10 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 省医院南岗export 取环勾4.8 | 固定单价 | 包名含「取环勾-1/7530」；仅导出账单生效；→ 固定 **4.8 元/包**；（不计包装费）；（不计折扣） |
| 省医院南岗export 手术衣15 | 固定单价 | 包名含「手术衣-1件/W9050」；仅导出账单生效；→ 固定 **15 元/包**；（不计包装费）；（不计折扣） |
| 省医院南岗export 舌钳子6.6 | 固定单价 | 包名含「舌钳子-1/1526」；仅导出账单生效；→ 固定 **6.6 元/包**；（不计包装费）；（不计折扣） |
| 省医院南岗export 间盘A75.9 | 固定单价 | 包名含「间盘A（52件）2/2」；仅导出账单生效；→ 固定 **75.9 元/包**；（不计包装费）；（不计折扣） |
| W9050纺织现价 | 固定单价 | 包名含「床单/W9050、刷手服/W9050、敷布包/W9050、敷料/W9050、手术衣-1件/W9050」；→ 固定 **18 元/包**；（不计包装费）；（不计折扣） |
| 校正价8.1 | 固定单价 | 包名含「小圆针-10」；→ 固定 **8.1 元/包**；（不计包装费）；（不计折扣） |
| 省医院南岗手术包6件 | 固定单价 | 包名含「手术包-6件、双信封包装」；→ 固定 **22.01 元/包**；（不计包装费）；（不计折扣） |
| 校正价22.01 | 固定单价 | 包名含「手术包-6件（双信封包装、门皮 手术包-6件（双信封包装」；科室：美容整形外科 / 皮肤门诊；→ 固定 **22.01 元/包**；（不计包装费）；（不计折扣） |
| 省医院南岗乐奥2件 | 固定单价 | 包名含「乐奥2件」；→ 固定 **26.4 元/包**；（不计包装费）；（不计折扣） |
| 校正价26.4 | 固定单价 | 包名含「乐奥」；→ 固定 **26.4 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江省医院（香坊院区）（`SHENG-YY-XF`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 11 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 手术室胸腔镜17件178.2 | 固定单价 | 包名含「胸腔镜-17件」；科室：手术室；→ 固定 **178.2 元/包**；（不计包装费）；（不计折扣） |
| 生殖医学科W9050纺织18元 | 固定单价 | 包名含「床单/W9050、敷料包/W9050、敷料包、刷手服/W9050」；科室：生殖医学科；→ 固定 **18 元/包**；（不计包装费）；（不计折扣） |
| 生殖医学科刷手服18元 | 固定单价 | 包名含「刷手服」；科室：生殖医学科；→ 固定 **18 元/包**；（不计包装费）；（不计折扣） |
| 神外一勾刀-2 26.4元 | 固定单价 | 包名含「勾刀-2、钩刀-2」；科室：神外一；→ 固定 **26.4 元/包**；（不计包装费）；（不计折扣） |
| 手术室额外包胸腔镜19.8 | 固定单价 | 包名含「胸腔镜」；科室：手术室；排除：w12050、W12050、17件、21件；→ 固定 **19.8 元/包**；（不计包装费）；（不计折扣） |
| 神外一勾刀13.2元 | 固定单价 | 包名含「勾刀、钩刀」；科室：神外一；排除：-2；→ 固定 **13.2 元/包**；（不计包装费）；（不计折扣） |
| W9050纺织现价 | 固定单价 | 包名含「床单/W9050、刷手服/W9050、敷布包/W9050、敷料/W9050、手术衣-1件/W9050」；→ 固定 **18 元/包**；（不计包装费）；（不计折扣） |
| 校正价8.1 | 固定单价 | 包名含「克氏钳-1/双、克氏钳（尖头）-1/双z1029、克氏钳（平头）-1/双」；→ 固定 **8.1 元/包**；（不计包装费）；（不计折扣） |
| 校正价13.2 | 固定单价 | 包名含「勾刀-2、钩刀-2」；→ 固定 **13.2 元/包**；（不计包装费）；（不计折扣） |
| 校正价39.6 | 固定单价 | 包名含「4K镜头1气腹管1光源1」；→ 固定 **39.6 元/包**；（不计包装费）；（不计折扣） |
| 校正价180.0 | 固定单价 | 包名含「4K-、外一27件筐1、外三、外二26筐1、好镜-26筐1、妇科-…」；→ 固定 **180 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江省社会康复医院（`SHKF-YY`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 9 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 社会康复ITI种植盒44 | 固定单价 | 包名含「ITI种植盒」；→ 固定 **44 元/包**；（不计包装费）；（不计折扣） |
| 社会康复登腾种植盒44 | 固定单价 | 包名含「登腾种植盒、DT种植盒」；→ 固定 **44 元/包**；（不计包装费）；（不计折扣） |
| 社会康复梁光强器械盒粉16.5 | 固定单价 | 包名含「梁光强器械盒（粉）、梁光强器械盒(粉)、梁光强器械盒粉」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 社会康复梁光强器械盒22 | 固定单价 | 包名含「梁光强器械盒」；排除：粉；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 社会康复环切器械盒16.5 | 固定单价 | 包名含「环切器械盒」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价2.5 | 固定单价 | 包名含「棉签」；→ 固定 **2.5 元/包**；（不计包装费）；（不计折扣） |
| 社会康复抛光车针盒16.5 | 固定单价 | 包名含「抛光车针盒」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 社会康复大车针盒22 | 固定单价 | 包名含「大车针盒」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 社会康复种植盒16.5 | 固定单价 | 包名含「种植盒」；排除：ITI、登腾、DT；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |

## 太平人民医院（`TAIPING-RM`）

- **计价模式**：`special_only` — 仅特色规则：未命中规则时保留原价
- **规则数**：启用 11 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 太平腔镜器械 | 固定单价 | 包名含「腔镜器械」；→ 固定 **119.63 元/包**；（不计包装费）；（不计折扣） |
| 太平开腹包 | 固定单价 | 包名含「开腹包」；→ 固定 **119.63 元/包**；（不计包装费）；（不计折扣） |
| 太平缝合包 | 固定单价 | 包名含「缝合包」；→ 固定 **20.63 元/包**；（不计包装费）；（不计折扣） |
| 太平皮肤拉钩 | 固定单价 | 包名含「皮肤拉钩」；→ 固定 **10.13 元/包**；（不计包装费）；（不计折扣） |
| 校正价8.3 | 固定单价 | 包名含「凿子-1、卵圆钳-1、取环器-1、宫颈钳-1、活检钳-1、窥器-1」；→ 固定 **8.3 元/包**；（不计包装费）；（不计折扣） |
| 校正价8.91 | 固定单价 | 包名含「产钳-2、剪刀1持针器1钩镊1、剪刀1窥器1宫颈钳1、吸头5号.6号.7号-3件、吸头5号6号7号-3件、吸头6号.7号.8号-3件…」；→ 固定 **8.91 元/包**；（不计包装费）；（不计折扣） |
| 校正价9.8 | 固定单价 | 包名含「咬骨钳-1、弯盘-1」；→ 固定 **9.8 元/包**；（不计包装费）；（不计折扣） |
| 校正价18.8 | 固定单价 | 包名含「取环钳（低温）-1件、窥器-1」；→ 固定 **18.8 元/包**；（不计包装费）；（不计折扣） |
| 校正价26.3 | 固定单价 | 包名含「水管-1」；→ 固定 **26.3 元/包**；（不计包装费）；（不计折扣） |
| 太平肌钩 | 固定单价 | 包名含「肌钩」；→ 固定 **10.13 元/包**；（不计包装费）；（不计折扣） |
| 太平拇外翻13件 | 固定单价 | 包名含「拇外翻」；→ 固定 **53.63 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江维多利亚妇产医院（`VICTORIA`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 2 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价16.0 | 固定单价 | 包名含「带架方盘-1」；→ 固定 **16 元/包**；（不计包装费）；（不计折扣） |
| 校正价25.0 | 固定单价 | 包名含「手柄-1」；→ 固定 **25 元/包**；（不计包装费）；（不计折扣） |

## WCSRMYY（`WCSRMYY`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 9 条，已停用 2 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 优贝特空心钉4.0固定233 | 固定单价 | 包名含「优贝特空心钉4.0」；→ 固定 **233 元/包**；（不计包装费）；（不计折扣） |
| 空心钉4.0固定233 | 固定单价 | 包名含「空心钉4.0」；排除：优贝特；→ 固定 **233 元/包**；（不计包装费）；（不计折扣） |
| 上肢锁定长钉固定370.5 | 固定单价 | 包名含「上肢锁定长钉」；→ 固定 **370.5 元/包**；（不计包装费）；（不计折扣） |
| 上肢锁定固定332 | 固定单价 | 包名含「上肢锁定」；排除：长钉；→ 固定 **332 元/包**；（不计包装费）；（不计折扣） |
| 墨希钉固定50 | 固定单价 | 包名含「墨希钉」；→ 固定 **50 元/包**；（不计包装费）；（不计折扣） |
| 额外包75纸塑袋10.5 | 固定单价 | 包名含「75/双、/z7526、/z7530、/z7534」；排除：墨希钉；→ 固定 **10.5 元/包**；（不计包装费）；（不计折扣） |
| 额外包10纸塑袋13.5 | 固定单价 | 包名含「10/双、/z1026、/z1029、/z1035」；→ 固定 **13.5 元/包**；（不计包装费）；（不计折扣） |
| 手术室操作器22元/件 | 按件计价 | 包名含「操作器」；科室：手术室；→ **22 元/件**；（不计包装费）；（不计折扣） |
| 手术室骨元针5件算1件 | 折算规则 | 包名含「骨元针」；科室：手术室；→ **5 件算 5 件** |

<details><summary>已停用规则</summary>

| 规则名 | 说明 |
|--------|------|
| 手术室腹腔镜器械(三号)17元/件 | 包名含「腹腔镜器械」；科室：手术室；→ **17 元/件**；（不计包装费）；（不计折扣） |
| 手术室腹腔镜器械包187元/包 | 包名含「腹腔镜器械」；科室：手术室；→ 固定 **187 元/包**；（不计包装费）；（不计折扣） |

</details>

## 武警黑龙江省总队医院（`WJ-HLJ-ZD`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 4 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价8.0 | 固定单价 | 包名含「器械包（纸塑袋）、敷料包（纸塑袋）」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 校正价16.5 | 固定单价 | 包名含「克氏针折弯钳-1」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价20.0 | 固定单价 | 包名含「刷手包、器械包（无纺布）、敷料包（无纺布）」；→ 固定 **20 元/包**；（不计包装费）；（不计折扣） |
| 校正价35.0 | 固定单价 | 包名含「环氧乙烷（自行打包）-1包」；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |

## 新发红十字医院（`XINFA-HSZ`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 2 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价66.0 | 固定单价 | 包名含「膀胱镜器械-3件」；→ 固定 **66 元/包**；（不计包装费）；（不计折扣） |
| 穿刺器帽22 | 固定单价 | 包名含「穿刺器帽」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |

## 悦美芳华医疗门诊医院（`YUEMEI-FH`）

- **计价模式**：`standard` — 标准模式：按通用灭菌定价表计费
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 眼包5.5元/件 | 按件计价 | 包名含「眼包」；→ **5.5 元/件**；（不计包装费）；（不计折扣） |

## 祖研-黑龙江省中医医院（南岗院区）（`ZUYAN-NG`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 6 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 祖研南岗export 弯盘13 | 固定单价 | 包名含「弯盘-1/Z2032」；仅导出账单生效；→ 固定 **13 元/包**；（不计包装费）；（不计折扣） |
| 排针10盘算1件 | 折算规则 | 包名含「排针」；→ **10 件算 5.5 件** |
| 校正价13.5 | 固定单价 | 包名含「排针-11、排针-12、排针-14、排针-15、排针20、排针17…」；→ 固定 **13.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价16.5 | 固定单价 | 包名含「排针-23、排针-28、排针-30、排针21、排针22件、排针30件…」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价22.0 | 固定单价 | 包名含「排针34件、排针35件、排针-34、排针-35、排针-47」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 校正价8.0 | 固定单价 | 包名含「排针-6/Z、排针9件」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |

## 祖研-黑龙江省中医医院（三辅院区）（`ZUYAN-SF`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 7 条，已停用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 祖研三辅export 探针刨刀16.5 | 固定单价 | 包名含「探针1瘘管1刨刀1/Z1026」；仅导出账单生效；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 针线包现价 | 固定单价 | 包名含「针线包」；→ 固定 **2.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价22.0 | 固定单价 | 包名含「器械包-4件」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 校正价30.0 | 固定单价 | 包名含「纱布条」；→ 固定 **30 元/包**；（不计包装费）；（不计折扣） |
| 校正价33.0 | 固定单价 | 包名含「排针包48件盘1、美容科排针包50件盘1」；→ 固定 **33 元/包**；（不计包装费）；（不计折扣） |
| 校正价38.5 | 固定单价 | 包名含「排针包51件盘1」；→ 固定 **38.5 元/包**；（不计包装费）；（不计折扣） |
| PDF/期待价30 | 固定单价 | 包名含「纱布条」；→ 固定 **30 元/包**；（不计包装费）；（不计折扣） |

<details><summary>已停用规则</summary>

| 规则名 | 说明 |
|--------|------|
| 三辅探针瘘管刨刀包 | 包名含「探针1瘘管1刨刀1」；→ 固定 **13.5 元/包**；（不计包装费）；（不计折扣） |

</details>

## 祖研-黑龙江省中医医院（香安院区）（`ZUYAN-XA`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 3 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 排针10盘算1件 | 折算规则 | 包名含「排针」；→ **10 件算 5.5 件** |
| 校正价16.5 | 固定单价 | 包名含「剪刀2止血钳1」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价22.0 | 固定单价 | 包名含「剪刀2止血钳1探针1」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江省中医药大学附属第三医院（电力）（`ZY3-DIANLI`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 6 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 小件盒25 | 固定单价 | 包名含「小件盒」；→ 固定 **25 元/包**；（不计包装费）；（不计折扣） |
| 校正价8.0 | 固定单价 | 包名含「洁牙尖-5件」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 校正价13.5 | 固定单价 | 包名含「成型片-7、洁牙尖-7」；→ 固定 **13.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价35.0 | 固定单价 | 包名含「洗手服」；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |
| 校正价88.0 | 固定单价 | 包名含「激光镜-4件 盒1」；→ 固定 **88 元/包**；（不计包装费）；（不计折扣） |
| 等离子镜36 | 固定单价 | 包名含「等离子镜」；→ 固定 **36 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江中医药大学附属第一医院（`ZYY-D1`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 43 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 外二换药包按包21.99 | 固定单价 | 包名含「换药包」；科室：外二；→ 固定 **21.99 元/包**；（不计包装费）；（不计折扣） |
| 宫腔镜镜头-3件按包52.74 | 固定单价 | 包名含「镜头-3件」；科室：宫腔镜；→ 固定 **52.74 元/包**；（不计包装费）；（不计折扣） |
| 30°腹腔镜组合价 | 固定单价 | 包名含「30°腹腔镜、30度腹腔镜」；→ 固定 **30.4 元/包**；（不计包装费）；（不计折扣） |
| 张景欣特器w12050 | 固定单价 | 包名含「张景欣特器」；→ 固定 **132 元/包**；（不计包装费）；（不计折扣） |
| 王树人特器w12050 | 固定单价 | 包名含「王树人特器-26」；→ 固定 **328 元/包**；（不计包装费）；（不计折扣） |
| 换药包120布组合价 | 固定单价 | 包名含「换药包(120布)、换药包120」；→ 固定 **21.99 元/包**；（不计包装费）；（不计折扣） |
| 孔巾包整包价 | 固定单价 | 包名含「孔巾包」；→ 固定 **28 元/包**；（不计包装费）；（不计折扣） |
| 腔镜包整包价 | 固定单价 | 包名含「腔镜包」；排除：腹腔镜；→ 固定 **28 元/包**；（不计包装费）；（不计折扣） |
| 辅料包整包价 | 固定单价 | 包名含「辅料包」；→ 固定 **28 元/包**；（不计包装费）；（不计折扣） |
| 球内注药3件最低把价 | 固定单价 | 包名含「球内注药」；→ 固定 **13.2 元/包**；（不计包装费）；（不计折扣） |
| 冲洗头-120固定价 | 固定单价 | 包名含「冲洗头-120」；→ 固定 **310.4 元/包**；（不计包装费）；（不计折扣） |
| 冲洗头-79固定价 | 固定单价 | 包名含「冲洗头-79」；→ 固定 **220 元/包**；（不计包装费）；（不计折扣） |
| 冲洗头-82固定价 | 固定单价 | 包名含「冲洗头-82」；→ 固定 **237.6 元/包**；（不计包装费）；（不计折扣） |
| 橄榄头-102固定价 | 固定单价 | 包名含「橄榄头-102」；→ 固定 **257.6 元/包**；（不计包装费）；（不计折扣） |
| 橄榄头5件算1件 | 折算规则 | 包名含「橄榄头」；→ **5 件算 5 件**；（不计包装费）；（不计折扣） |
| 冲洗头5件算1件 | 折算规则 | 包名含「冲洗头」；→ **5 件算 5 件**；（不计包装费）；（不计折扣） |
| 低温器械保护盒 | 固定单价 | 包名含「低温器械保护盒、保护盒」；→ 固定 **8 元/包**；（不计包装费）；（不计折扣） |
| 低温套5件 | 固定单价 | 包名含「低温灭菌（套）5件、低温灭菌(套)5件」；→ 固定 **70.33 元/包**；（不计包装费）；（不计折扣） |
| 低温套10件 | 固定单价 | 包名含「低温灭菌（套）10件、低温灭菌 (套)10件」；→ 固定 **131.87 元/包**；（不计包装费）；（不计折扣） |
| 低温套20件 | 固定单价 | 包名含「低温灭菌（套）20件、低温灭菌 (套)20件」；→ 固定 **239.76 元/包**；（不计包装费）；（不计折扣） |
| 保温杯-1Z2044 | 固定单价 | 包名含「保温杯-1Z2044」；→ 固定 **17.6 元/包**；（不计包装费）；（不计折扣） |
| 低温袋10cm | 固定单价 | 包名含「低温灭菌 10cm」；→ 固定 **17.58 元/包**；（不计包装费）；（不计折扣） |
| 低温袋15cm | 固定单价 | 包名含「低温灭菌 15cm、膀胱取石钳」；→ 固定 **20 元/包**；（不计包装费）；（不计折扣） |
| 低温袋20cm | 固定单价 | 包名含「低温灭菌 20cm」；→ 固定 **22.38 元/包**；（不计包装费）；（不计折扣） |
| 低温袋25cm | 固定单价 | 包名含「低温灭菌 25cm」；→ 固定 **23.98 元/包**；（不计包装费）；（不计折扣） |
| 低温袋30cm | 固定单价 | 包名含「低温灭菌 30cm」；→ 固定 **27.97 元/包**；（不计包装费）；（不计折扣） |
| 纸塑袋10cm单把 | 固定单价 | 包名含「纸塑袋 10cm、纸塑袋10cm」；→ 固定 **6.39 元/包**；（不计包装费）；（不计折扣） |
| 纸塑袋15cm单把 | 固定单价 | 包名含「纸塑袋 15cm、纸塑袋15cm」；→ 固定 **8.79 元/包**；（不计包装费）；（不计折扣） |
| 纸塑袋20cm单把 | 固定单价 | 包名含「纸塑袋20cm、纸塑袋 20cm」；→ 固定 **10.39 元/包**；（不计包装费）；（不计折扣） |
| 纸塑袋25cm单把 | 固定单价 | 包名含「纸塑袋 25cm、纸塑袋25cm」；→ 固定 **12.79 元/包**；（不计包装费）；（不计折扣） |
| 纸塑袋15cm两件套 | 固定单价 | 包名含「纸塑袋 15cm、纸塑袋15cm」；→ 固定 **13.19 元/包**；（不计包装费）；（不计折扣） |
| 纸塑袋10cm两件套 | 固定单价 | 包名含「纸塑袋 10cm、纸塑袋10cm」；→ 固定 **10.79 元/包**；（不计包装费）；（不计折扣） |
| 敷料20x20 | 固定单价 | 包名含「20cm*20cm、20×20」；→ 固定 **3.2 元/包**；（不计包装费）；（不计折扣） |
| 敷料大20x20x15 | 固定单价 | 包名含「大 （20cm*20cm*15cm）、20cm*20cm*15cm」；→ 固定 **2 元/包**；（不计包装费）；（不计折扣） |
| 敷料大30x30x50 | 固定单价 | 包名含「敷料大（30cm*30cm*50cm）」；→ 固定 **27.97 元/包**；（不计包装费）；（不计折扣） |
| 敷料小20x20x15 | 固定单价 | 包名含「敷料小（20cm*20cm*15cm）」；→ 固定 **19.98 元/包**；（不计包装费）；（不计折扣） |
| 敷料中20x20x30 | 固定单价 | 包名含「敷料中（20cm*20cm*30cm）」；→ 固定 **23.98 元/包**；（不计包装费）；（不计折扣） |
| 外来器械4.4每件 | 按件计价 | 包名含「外来」；→ **4.4 元/件**；（不计包装费）；（不计折扣） |
| 外来钉盒40 | 固定单价 | 包名含「钉盒」；→ 固定 **40 元/包**；（不计包装费）；（不计折扣） |
| 外来生物监测120 | 固定单价 | 包名含「生物监测」；→ 固定 **120 元/包**；（不计包装费）；（不计折扣） |
| 30度腹腔镜组合价golden30.4 | 固定单价 | 包名含「30°腹腔镜、30度腹腔镜」；→ 固定 **30.4 元/包**；（不计包装费）；（不计折扣） |
| 外二换药包120布golden21.99 | 固定单价 | 包名含「换药包(120布)、换药包120」；科室：外二；→ 固定 **21.99 元/包**；（不计包装费）；（不计折扣） |
| 王树人特器26golden328 | 固定单价 | 包名含「王树人特器-26」；→ 固定 **328 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江中医药大学附属第二医院（哈南分院）（`ZYY-D2-HN`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 1 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 校正价16.5 | 固定单价 | 包名含「粉刺针-3」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |

## 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`）

- **计价模式**：`hybrid` — 混合模式：未命中规则时走标准灭菌阶梯价
- **规则数**：启用 15 条，已停用 6 条

| 规则名 | 类型 | 说明 |
|--------|------|------|
| 附二南岗大衣无纺布35 | 固定单价 | 包名含「大衣-无纺布」；仅导出账单生效；→ 固定 **35 元/包**；（不计包装费） |
| 附二南岗大衣W9050 | 固定单价 | 包名含「大衣」；→ 固定 **30 元/包**；（不计包装费） |
| 附二南岗小单w1250 | 固定单价 | 包名含「小单」；→ 固定 **35 元/包**；（不计包装费） |
| 附二南岗刮勺探针5.5元/把 | 按件计价 | 包名含「刮勺探针」；→ **5.5 元/件**；（不计包装费）；（不计折扣） |
| 附二南岗机扩针7盒 | 固定单价 | 包名含「机扩针-7盒1」；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价22.0 | 固定单价 | 包名含「剪刀-4、车针-17」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 附二南岗弯针2 | 固定单价 | 包名含「弯针-2」；→ 固定 **13.5 元/包**；（不计包装费）；（不计折扣） |
| 校正价30.0 | 固定单价 | 包名含「大衣」；→ 固定 **30 元/包**；（不计包装费）；（不计折扣） |
| 附二南岗椎间孔镜镜头 | 固定单价 | 包名含「椎间孔镜镜头」；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |
| 校正价35.0 | 固定单价 | 包名含「小单」；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |
| 附二南岗关节镜30度 | 固定单价 | 包名含「关节镜30°」；→ 固定 **36 元/包**；（不计包装费）；（不计折扣） |
| 校正价38.5 | 固定单价 | 包名含「机扩针-6盒1」；→ 固定 **38.5 元/包**；（不计包装费）；（不计折扣） |
| 附二南岗关节镜70度 | 固定单价 | 包名含「关节镜70°」；→ 固定 **28 元/包**；（不计包装费）；（不计折扣） |
| PDF/期待价30 | 固定单价 | 包名含「大衣」；→ 固定 **30 元/包**；（不计包装费）；（不计折扣） |
| PDF/期待价35 | 固定单价 | 包名含「小单」；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |

<details><summary>已停用规则</summary>

| 规则名 | 说明 |
|--------|------|
| 附二南岗export 关节镜30度44 | 包名含「关节镜30°/z2060」；仅导出账单生效；→ 固定 **44 元/包**；（不计包装费）；（不计折扣） |
| 附二南岗export 关节镜70度44 | 包名含「关节镜70°-1/z2060」；仅导出账单生效；→ 固定 **44 元/包**；（不计包装费）；（不计折扣） |
| 附二南岗export 刮勺探针22 | 包名含「刮勺探针4/z1035」；仅导出账单生效；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |
| 附二南岗export 机扩针7盒16.5 | 包名含「机扩针-7盒1/z1526」；仅导出账单生效；→ 固定 **16.5 元/包**；（不计包装费）；（不计折扣） |
| 附二南岗export 椎间孔镜35 | 包名含「椎间孔镜镜头-1（双层）/z2060」；仅导出账单生效；→ 固定 **35 元/包**；（不计包装费）；（不计折扣） |
| 附二南岗刮勺探针4 | 包名含「刮勺探针4」；→ 固定 **22 元/包**；（不计包装费）；（不计折扣） |

</details>

