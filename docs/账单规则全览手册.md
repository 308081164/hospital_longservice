# 账单规则全览手册

> 本文档说明系统内**全部计价规则**：标准灭菌默认表、全局内置特色规则、以及各客户 `CustomerProductRule` 落库规则。
> 数据来自 [`billing-rules-manifest.json`](../backend/src/main/resources/billing-seeds/billing-rules-manifest.json)（hash `a54feef3a484…`），生成时间 2026-08-15 02:36 UTC。
> 重新生成：`python3 scripts/generate_billing_rules_handbook.py`

---

## 目录

| 章节 | 内容 | 条数 |
|------|------|------|
| [第 0 章](#第-0-章--规则总量一览) | 规则总量 | — |
| [第 1 章](#第-1-章--计价模式与执行顺序) | 计价模式与顺序 | 3 节 |
| [第 2 章](#第-2-章--标准灭菌规则全局默认-std-) | STD-01～STD-12 | **12** |
| [第 3 章](#第-3-章--特色规则类型说明type-) | TYPE-01～TYPE-06 | **6** |
| [第 4 章](#第-4-章--全局内置特色规则glb-) | GLB-01 | **1** |
| [第 5 章](#第-5-章--客户特色规则完整索引cr-001cr-300) | CR-001～CR-300 | **300** |
| [第 6 章](#第-6-章--特殊收费11-业务分类sc11-) | SC11-T01～T16 | **18** |
| [附录 A](#附录-a--已停用客户规则) | 已停用 CR | 55 |

**一眼总数**：启用客户规则 **300** + 标准规则 **12** + 全局特色 **1** = **313 条可执行计价规则**；另 SC11 业务分类 **18** 种。

---

## 第 0 章 · 规则总量一览

| 层级 | 数量 | 说明 |
|------|------|------|
| **客户特色规则（启用）** | **300** | 本文 **CR-001～CR-300** |
| 客户特色规则（已停用） | 55 | 附录 A |
| 涉及客户（有规则） | 55 | 其中启用特色计价 34 家 |
| **标准灭菌规则（全局默认）** | **12 项** | 第 2 章 STD-01～STD-12 |
| **全局内置特色规则** | **1 条** | 第 4 章 GLB-01 软镜 300 元 |
| **特色规则类型（机制）** | **6 种** | 第 3 章 TYPE-01～TYPE-06 |
| **SC11 业务分类** | **18 种** | 第 6 章（Excel 语义索引） |

**启用规则按类型分布**

| 类型编号 | ruleType | 中文 | 启用条数 |
|----------|----------|------|----------|
| TYPE-01 | `FIXED_PRICE` | 固定单价 | 244 |
| TYPE-02 | `PRICE_PER_INSTRUMENT` | 按件计价 | 15 |
| TYPE-03 | `FOLD` | 件数折算 | 35 |
| TYPE-04 | `EXTRA_FEE` | 附加费 | 5 |
| TYPE-05 | `ZERO_PRICE_OVERRIDE` | 零价覆盖 | 1 |
| TYPE-06 | `MULTIPLIER` | 倍数加价 | 0 |

---

## 第 1 章 · 计价模式与执行顺序

### 1.1 三种客户计价模式

| 模式 | 编号 | 未命中特色规则时 | 典型客户 |
|------|------|------------------|----------|
| `special_only` | MODE-01 | **保留账单单价**，status 多为 unchanged | 冰城医美 |
| `hybrid` | MODE-02 | **走标准灭菌价**（第 2 章 STD-*，含 override） | 电机厂、祖研南岗 |
| `standard` | MODE-03 | **走标准灭菌价**（第 2 章 STD-*） | 多数普通医院 |

### 1.2 单行账单处理顺序（简化）

```
1. billingEnabled=false → 保留原价（特色账单已关闭）
2. 匹配客户 ZERO_PRICE_OVERRIDE / 固定价 / 折算 / 附加费
3. 若 special_only 且未命中 → 保留原价
4. 若 hybrid / standard 且未命中 → 标准高温/低温/敷料路径（STD-*）
5. 比较账单价与建议价 → warning / unchanged
```

### 1.3 规则匹配通用条件

每条客户规则（CR-*）在匹配时还检查：

- 医院名与客户规范名/别名一致
- 包名 + 类型 + 包材 合并文本含关键词（无关键词则靠产品绑定）
- 器械件数、袋宽、温度（HT/LT）区间
- 科室、原价等 `conditionsJson` 附加条件

---

## 第 2 章 · 标准灭菌规则（全局默认 STD-*）

当客户为 **`standard` / `hybrid`** 且未命中特色规则时生效（走高温/低温/敷料标准路径，含 `standardPricingOverride` 深合并）。
**`special_only` 未命中时不走本章**（引擎保留账单单价）。
源码：[`DefaultPricingTemplate.java`](../backend/src/main/java/com/hospital/backend/service/DefaultPricingTemplate.java)。

### STD-01 · 高温纸塑袋按件 + 袋材费

**作用**：额外包/纸塑袋高温灭菌的默认计价。

**生效**：类型或包材含「纸塑袋」且非无纺布、非敷料特例。

**公式**：`ceil(计费件数/5)×5.5 + 袋宽对应袋价`；≥3 件时可按 `件数×5.5+袋价`。

**袋宽价**：10cm→2.5，15cm→5.5，20cm→7.5，25cm→10.5 元。

**示例**：
```
包名：缝合针-1/Z7520，纸塑 15cm，1 件
→ 5.5 + 5.5 = 11.0 元（或 minCharge 16.5 封顶场景另计）
```

### STD-02 · 高温纸塑最低消费 16.5

**作用**：单包纸塑高温最低收费。

**生效**：STD-01 路径下，折算后单价低于 16.5 时抬到 16.5。

**示例**：2 件小件纸塑包 → 常落到 **16.5 元/包**。

### STD-03 · 高温无纺布按件

**作用**：器械包(ZSD)/无纺布高温灭菌。

**生效**：包材或类型含「无纺布」。

**公式**：≤2 件 often 16.5；≥3 件 `件数×5.5`（见引擎 `computeHighTempNonWoven`）。

### STD-04 · 低温纸塑袋阶梯（按件）

**作用**：低温/ETO/EO 纸塑袋灭菌。

**袋宽底价**：10cm→22，15→25，20→28，25→30，30→35 元。

**件数阶梯**：5 件→88，10 件→165，20 件→300；中间按 `remainderPerPiecePrice=22` 补差。

**示例**：
```
单包装 5 件/20cm 低温纸塑 → 88 元
```

### STD-05 · 低温无纺布阶梯

同 STD-04 件数阶梯；单件最低 35 元。

### STD-06 · 敷料包（无纺布）按 W 码分档

**作用**：棉球/纱布/驱血带等敷料包。

**生效**：类型含「敷料包」且识别 W60/W90/W120 等规格。

| W 码 | 单价 |
|------|------|
| W50/W60/W70 | 25 元 |
| W90 | 30 元 |
| W120/W150 | 35 元 |

**示例**：驱血带 W90 → **30 元/包**。

### STD-07 · 敷料包（纸塑）棉球/孔巾小袋

**作用**：纸塑敷料小袋固定价。

**生效**：敷料包+纸塑袋，袋宽 15cm→2.5，20cm→4 元。

### STD-08 · 全局针类/小件 5 合 1 折算

**作用**：包名含「针/小件/车针…」时，5 件算 1 计费件（全局 needle 配置）。

**生效**：无客户 FOLD 优先命中时，匹配 `DefaultPricingTemplate.needle.keywords`。

**示例**：12 件机扩针 → ceil(12/5)=3 计费件 → 3×5.5+袋费。

### STD-09 · 包名「针N」拆分规则

**作用**：包名形如「AR水管-2件/双/Z1530」中「针N」参与折算。

### STD-10 · 物流费（结款函，非单行单价）

**作用**：按趟 50 元物流（`logistics.enabled`）。

### STD-11 · 结款函模板

结算单导出格式，不参与 `processRow` 单价。

### STD-12 · 账单导入清洗

删除合计行、修剪包材列等；不参与单价计算。

---

## 第 3 章 · 特色规则类型说明（TYPE-*）

### TYPE-01 · FIXED_PRICE（固定单价）

**作用**：无论件数多少，命中后建议单价为固定值（元/包）。

**生效**：关键词 + 条件匹配；客户规则优先于全局规则。

**示例**（电机厂 CR 类）：
```
缝合针-8/Z7520 → 固定 8.0 元
```

### TYPE-02 · PRICE_PER_INSTRUMENT（按件计价）

**作用**：`建议单价 = 计费件数 × 单价`；常配合 `skipPackaging=true`。

**示例**（冰城医美）：
```
环钻包 10 件 → 10×5.5=55 + EXTRA_FEE 加价 → 见 CR 条目
```

### TYPE-03 · FOLD（件数折算）

**作用**：将器械数按 threshold/foldRatio 折算成更少「计费件」后再乘 5.5/5.6。

**公式**：件数 ≤ threshold → 1 计费件；否则 `ceil(件数/foldRatio)`。

**示例**（祖研排针）：
```
排针 15 件 → ceil(15/10)=2 计费件 → 2×5.5+袋费
```

### TYPE-04 · EXTRA_FEE / ADD_FEE（附加费）

**作用**：在按件价或固定价基础上 **+fee 元**（如无纺布加价、包装加价）。

**示例**（冰城环钻）：
```
环钻包：5.5×件数 + 3（无纺布）+ 3（小件包装，2-5 件）
```

### TYPE-05 · ZERO_PRICE_OVERRIDE（零价覆盖）

**作用**：对特定包强制按规则价覆盖零价/异常导入。

### TYPE-06 · MULTIPLIER（倍数加价）

**作用**：在基础价上乘 multiplier（当前 manifest 中较少使用）。

---

## 第 4 章 · 全局内置特色规则（GLB-*）

### GLB-01 · 软镜固定 300 元

| 属性 | 值 |
|------|-----|
| 来源 | `DefaultPricingTemplate.specialRules.fixedPrices` |
| 关键词 | 软镜 |
| 价格 | **300 元/包** |

**示例**：
```
包名：软镜包-5，任意件数 → 300 元
```

---

## 第 5 章 · 客户特色规则完整索引（CR-001～CR-300）

以下 **300** 条为当前 **启用** 的客户规则，按客户编码字母序、同客户内按优先级排列。

### 5.0 客户速查表

| 客户 | 编码 | 模式 | 启用条数 | 首条 CR |
|------|------|------|----------|---------|
| 哈尔滨冰城医疗美容医院 | `BINGCHENG-YM` | `special_only` | 7 | [CR-001](#cr-001-冰城整形手术包按件5.5) |
| 博尚医院 | `BOSHANG-YY` | `special_only` | 4 | [CR-008](#cr-008-博尚旋切器戳卡66) |
| 春语医疗美容医院 | `CHUNYU-YL` | `hybrid` | 2 | [CR-012](#cr-012-春语塑料管≤10按1件) |
| 道外区人民医院 | `DAOWAI-RM` | `standard` | 14 | [CR-014](#cr-014-校正价2.0) |
| 哈尔滨市道里区妇幼保健院 | `DL-FUCHAN` | `hybrid` | 4 | [CR-028](#cr-028-道里妇幼棉花针5合1含包材) |
| 黑龙江省第二医院（南岗院区） | `ERYY-NG` | `special_only` | 1 | [CR-032](#cr-032-省二南岗挖勺每件5.5元) |
| ERYY-SB | `ERYY-SB` | `special_only` | 8 | [CR-033](#cr-033-手术衣无纺布现价) |
| 方南南医院 | `FNN-YY` | `standard` | 2 | [CR-041](#cr-041-方南南小件5合1含包材) |
| 国药总医院第二院区 | `GUOYAO-2` | `hybrid` | 5 | [CR-043](#cr-043-电机厂缝合针8元) |
| 国药总医院第三院区 | `GUOYAO-3` | `hybrid` | 1 | [CR-048](#cr-048-校正价35.0) |
| 国药总医院主院区 | `GUOYAO-MAIN` | `special_only` | 3 | [CR-049](#cr-049-10mm30度镜固定价) |
| 黑龙江省海员总医院（松北） | `HAIYUAN-SB` | `hybrid` | 2 | [CR-052](#cr-052-海员胶帽≤5按1件) |
| 黑龙江总工会医院 | `HL-ZGH` | `hybrid` | 1 | [CR-054](#cr-054-总工会镜头铂康盒加收8) |
| HLFB-SF | `HLFB-SF` | `standard` | 1 | [CR-055](#cr-055-校正价13.5) |
| 黑龙江省妇幼保健院（人口） | `HLJ-FY-RK` | `hybrid` | 2 | [CR-056](#cr-056-妇幼人口密封件≤5按1件) |
| 省监狱管理局医院 | `HLJ-JYGLJ-YY` | `standard` | 3 | [CR-058](#cr-058-微克夹钳35元) |
| 哈尔滨市第二医院 | `HRB-2ND` | `special_only` | 7 | [CR-061](#cr-061-口腔科正畸车针8元) |
| 哈尔滨长健医院 | `HRB-CJ` | `standard` | 4 | [CR-068](#cr-068-长健敷料包W12050) |
| 哈尔滨工业大学医院 | `HRB-HIT` | `special_only` | 9 | [CR-072](#cr-072-哈工大export-胆囊包10件165) |
| 哈尔滨市红十字妇产医院 | `HRB-HSZ` | `special_only` | 4 | [CR-081](#cr-081-低温1件22元) |
| HRB-HTFH | `HRB-HTFH` | `standard` | 2 | [CR-085](#cr-085-航天风华镍钛锉5合1含包材) |
| 哈尔滨华夏眼科医院 | `HRB-HX-EYE` | `standard` | 1 | [CR-087](#cr-087-≥3件器械包单价2.75) |
| HRB-MHM | `HRB-MHM` | `standard` | 1 | [CR-088](#cr-088-美涵吸脂针20cm以下5件算1件) |
| 哈尔滨市南岗区人民医院（九院） | `HRB-NGJY` | `standard` | 1 | [CR-089](#cr-089-九院敷料包0元覆盖15) |
| HRB-SD-MB | `HRB-SD-MB` | `standard` | 2 | [CR-090](#cr-090-松电机扩针5合1含包材) |
| 哈尔滨森海医院 | `HRB-SH` | `special_only` | 2 | [CR-092](#cr-092-胶帽22元/件) |
| 哈尔滨市第五医院 | `HRB-WY` | `hybrid` | 12 | [CR-094](#cr-094-市五export-DMD光源1号35) |
| 呼兰区红十字医院 | `HULAN-HSZ` | `special_only` | 1 | [CR-106](#cr-106-校正价8.0) |
| 呼兰区第一人民医院 | `HULAN-RM` | `special_only` | 9 | [CR-107](#cr-107-PDF敷料小20x20x15) |
| 呼兰中医院 | `HULAN-TCM` | `special_only` | 4 | [CR-116](#cr-116-呼兰中医export-腹腔镜297) |
| 佳医医疗 | `JIAYI-YL` | `standard` | 1 | [CR-120](#cr-120-佳医敷料纸塑4元) |
| 黑龙江九洲妇科医院 | `JIUZHOU-FK` | `standard` | 1 | [CR-121](#cr-121-方盘5.5) |
| 哈尔滨基准生物科技有限公司 | `JZSW-BIO` | `standard` | 1 | [CR-122](#cr-122-氩氦刀（EO）150元) |
| 美意医疗 | `MEIYI-YL` | `standard` | 1 | [CR-123](#cr-123-美意敷料纸塑4元) |
| NEAU-YY | `NEAU-YY` | `standard` | 4 | [CR-124](#cr-124-东北农大根管锉5.6含包材) |
| 南岗区妇产医院 | `NG-FUCHAN` | `special_only` | 19 | [CR-128](#cr-128-校正价2.3) |
| 三精肾病医院 | `SANJING-SB` | `standard` | 11 | [CR-147](#cr-147-三精export-内瘘68件102) |
| 黑龙江省医院（南岗院区） | `SHENG-YY-NG` | `hybrid` | 10 | [CR-158](#cr-158-省医院南岗export-取环勾4.8) |
| 黑龙江省医院（香坊院区） | `SHENG-YY-XF` | `hybrid` | 11 | [CR-168](#cr-168-手术室胸腔镜17件178.2) |
| 黑龙江省社会康复医院 | `SHKF-YY` | `special_only` | 11 | [CR-179](#cr-179-社会康复内提工具盒44) |
| 太平人民医院 | `TAIPING-RM` | `special_only` | 11 | [CR-190](#cr-190-太平腔镜器械) |
| 黑龙江维多利亚妇产医院 | `VICTORIA` | `standard` | 2 | [CR-201](#cr-201-校正价16.0) |
| WCSRMYY | `WCSRMYY` | `standard` | 9 | [CR-203](#cr-203-优贝特空心钉4.0固定233) |
| 武警黑龙江省总队医院 | `WJ-HLJ-ZD` | `special_only` | 4 | [CR-212](#cr-212-校正价8.0) |
| 新发红十字医院 | `XINFA-HSZ` | `hybrid` | 6 | [CR-216](#cr-216-新发镜头0度35) |
| 易丽医疗 | `YILI-YL` | `standard` | 1 | [CR-222](#cr-222-易丽敷料纸塑4元) |
| 悦美芳华医疗门诊医院 | `YUEMEI-FH` | `standard` | 1 | [CR-223](#cr-223-眼包5.5元/件) |
| 祖研-黑龙江省中医医院（南岗院区） | `ZUYAN-NG` | `hybrid` | 5 | [CR-224](#cr-224-校正价13.5) |
| 祖研-黑龙江省中医医院（三辅院区） | `ZUYAN-SF` | `special_only` | 7 | [CR-229](#cr-229-祖研三辅export-探针刨刀16.5) |
| 祖研-黑龙江省中医医院（香安院区） | `ZUYAN-XA` | `hybrid` | 3 | [CR-236](#cr-236-排针10盘算1件) |
| 黑龙江省中医药大学附属第三医院（电力） | `ZY3-DIANLI` | `special_only` | 6 | [CR-239](#cr-239-小件盒25) |
| 黑龙江中医药大学附属第一医院 | `ZYY-D1` | `hybrid` | 40 | [CR-245](#cr-245-30度腹腔镜ground30.38) |
| 黑龙江中医药大学附属第二医院（哈南分院） | `ZYY-D2-HN` | `hybrid` | 1 | [CR-285](#cr-285-校正价16.5) |
| 黑龙江中医药大学附属第二医院（南岗） | `ZYY-D2-NG` | `special_only` | 15 | [CR-286](#cr-286-附二南岗大衣无纺布35) |

---

### 5.1 客户 · 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：7 条（CR-001～CR-007）

#### CR-001 · 冰城整形手术包按件5.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 6（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：整形手术包、整形包
- **排除**关键词：脂充包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨冰城医疗美容医院
包名：整形手术包-5/Z7520，器械 5 件
账单价：22.5 元
→ 引擎建议：5×5.5=27.5 元（规则配置不计包装费），status=warning
```

#### CR-002 · 冰城环钻包按件5.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 6（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：环钻包、环钻
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨冰城医疗美容医院
包名：环钻包-5/Z7520，器械 5 件
账单价：22.5 元
→ 引擎建议：5×5.5=27.5 元（规则配置不计包装费），status=warning
```

#### CR-003 · 冰城脂充包按件5.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 6（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：脂充包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨冰城医疗美容医院
包名：脂充包-5/Z7520，器械 5 件
账单价：22.5 元
→ 引擎建议：5×5.5=27.5 元（规则配置不计包装费），status=warning
```

#### CR-004 · 冰城整形手术包无纺布加价3

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`） |
| 规则类型 | `EXTRA_FEE` — 附加费 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 7（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后在基础价上 **+3 元**（附加费）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：整形手术包、整形包
- **排除**关键词：脂充包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨冰城医疗美容医院
包名：整形手术包
基础价 8.5 元 + 附加费 3 元 → 合计 11.5 元
```

#### CR-005 · 冰城环钻包无纺布加价3

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`） |
| 规则类型 | `EXTRA_FEE` — 附加费 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 7（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后在基础价上 **+3 元**（附加费）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：环钻包、环钻
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨冰城医疗美容医院
包名：环钻包
基础价 8.5 元 + 附加费 3 元 → 合计 11.5 元
```

#### CR-006 · 冰城脂充包无纺布加价5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`） |
| 规则类型 | `EXTRA_FEE` — 附加费 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 7（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后在基础价上 **+5 元**（附加费）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：脂充包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨冰城医疗美容医院
包名：脂充包
基础价 8.5 元 + 附加费 5 元 → 合计 13.5 元
```

#### CR-007 · 冰城环钻包小件包装加价3

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨冰城医疗美容医院（`BINGCHENG-YM`） |
| 规则类型 | `EXTRA_FEE` — 附加费 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后在基础价上 **+3 元**（附加费）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：环钻包、环钻
- 器械件数 ≥ 2
- 器械件数 ≤ 5
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨冰城医疗美容医院
包名：环钻包
基础价 8.5 元 + 附加费 3 元 → 合计 11.5 元
```

### 5.2 客户 · 博尚医院（`BOSHANG-YY`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：4 条（CR-008～CR-011）

#### CR-008 · 博尚旋切器戳卡66

| 属性 | 值 |
|------|-----|
| 客户 | 博尚医院（`BOSHANG-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **66 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：旋切器1戳卡1胶帽4/Z2045、旋切器1戳卡1胶帽4
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：博尚医院
包名：旋切器1戳卡1胶帽4/Z2045-示例/Z7520
账单价：76 元（与规则价不符）
→ 引擎建议：66 元/包，status=warning
```

#### CR-009 · 博尚旋切器44

| 属性 | 值 |
|------|-----|
| 客户 | 博尚医院（`BOSHANG-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **44 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：旋切器1胶帽4/Z2045、旋切器1胶帽4
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：博尚医院
包名：旋切器1胶帽4/Z2045-示例/Z7520
账单价：54 元（与规则价不符）
→ 引擎建议：44 元/包，status=warning
```

#### CR-010 · 博尚旋切器线66

| 属性 | 值 |
|------|-----|
| 客户 | 博尚医院（`BOSHANG-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **66 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：旋切器1线1胶帽4/Z2045、旋切器1线1胶帽4
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：博尚医院
包名：旋切器1线1胶帽4/Z2045-示例/Z7520
账单价：76 元（与规则价不符）
→ 引擎建议：66 元/包，status=warning
```

#### CR-011 · 博尚胶帽5合1

| 属性 | 值 |
|------|-----|
| 客户 | 博尚医院（`BOSHANG-YY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胶帽
- 灭菌类型 = `LT`（HT=高温，LT=低温）

**示例**

```
医院：博尚医院
包名：胶帽-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.3 客户 · 春语医疗美容医院（`CHUNYU-YL`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：2 条（CR-012～CR-013）

#### CR-012 · 春语塑料管≤10按1件

| 属性 | 值 |
|------|-----|
| 客户 | 春语医疗美容医院（`CHUNYU-YL`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤10 件按 1 计费件；>10 件按 ceil(件数÷10) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：塑料管、管子
- 器械件数 ≤ 10
- 灭菌类型 = `LT`（HT=高温，LT=低温）

**示例**

```
医院：春语医疗美容医院
包名：塑料管-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-013 · 春语塑料管>10折算22

| 属性 | 值 |
|------|-----|
| 客户 | 春语医疗美容医院（`CHUNYU-YL`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤10 件按 1 计费件；>10 件按 ceil(件数÷10) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：塑料管、管子
- 器械件数 ≥ 11
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**

**示例**

```
医院：春语医疗美容医院
包名：塑料管-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.4 客户 · 道外区人民医院（`DAOWAI-RM`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：14 条（CR-014～CR-027）

#### CR-014 · 校正价2.0

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **2 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：棉球
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：棉球-示例/Z7520
账单价：12 元（与规则价不符）
→ 引擎建议：2 元/包，status=warning
```

#### CR-015 · 校正价3.0

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **3 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：剪刀-1、剪刀小-1、卵圆钳-1、弯盘-1、持针器-1、止血钳-1、盆-1、镊子-1、长弯-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：剪刀-1-示例/Z7520
账单价：13 元（与规则价不符）
→ 引擎建议：3 元/包，status=warning
```

#### CR-016 · 校正价6.0

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **6 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：持物钳罐-2/持物钳放外面W6050
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：持物钳罐-2/持物钳放外面W6050-示例/Z7520
账单价：16 元（与规则价不符）
→ 引擎建议：6 元/包，status=warning
```

#### CR-017 · 校正价6.5

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **6.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：敷料、敷料包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：敷料-示例/Z7520
账单价：16.5 元（与规则价不符）
→ 引擎建议：6.5 元/包，status=warning
```

#### CR-018 · 校正价9.0

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 14（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **9 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：美容包-3件、美容小包-3件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：美容包-3件-示例/Z7520
账单价：19 元（与规则价不符）
→ 引擎建议：9 元/包，status=warning
```

#### CR-019 · 校正价12.0

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **12 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：止血钳-4件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：止血钳-4件-示例/Z7520
账单价：22 元（与规则价不符）
→ 引擎建议：12 元/包，status=warning
```

#### CR-020 · 校正价18.0

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 16（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **18 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：美容包-6件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：美容包-6件-示例/Z7520
账单价：28 元（与规则价不符）
→ 引擎建议：18 元/包，status=warning
```

#### CR-021 · 校正价30.0

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 17（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：取环包-10件、外伤包-10件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：取环包-10件-示例/Z7520
账单价：40 元（与规则价不符）
→ 引擎建议：30 元/包，status=warning
```

#### CR-022 · 校正价33.0

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 18（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **33 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：取环包-11件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：取环包-11件-示例/Z7520
账单价：43 元（与规则价不符）
→ 引擎建议：33 元/包，status=warning
```

#### CR-023 · 校正价126.0

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 19（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **126 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：阑尾器械-41件（筐1）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：阑尾器械-41件（筐1）-示例/Z7520
账单价：136 元（与规则价不符）
→ 引擎建议：126 元/包，status=warning
```

#### CR-024 · PDF外来器械骨电钻12.5

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 40（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **12.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外来器械（骨电钻）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：外来器械（骨电钻）-示例/Z7520
账单价：22.5 元（与规则价不符）
→ 引擎建议：12.5 元/包，status=warning
```

#### CR-025 · PDF外来器械钉盒37.5

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 41（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **37.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外来器械（钉盒）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：外来器械（钉盒）-示例/Z7520
账单价：47.5 元（与规则价不符）
→ 引擎建议：37.5 元/包，status=warning
```

#### CR-026 · PDF/期待价2

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 200（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **2 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：棉球
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：棉球-示例/Z7520
账单价：12 元（与规则价不符）
→ 引擎建议：2 元/包，status=warning
```

#### CR-027 · PDF/期待价6.5

| 属性 | 值 |
|------|-----|
| 客户 | 道外区人民医院（`DAOWAI-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 201（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **6.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：敷料、敷料包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：道外区人民医院
包名：敷料-示例/Z7520
账单价：16.5 元（与规则价不符）
→ 引擎建议：6.5 元/包，status=warning
```

### 5.5 客户 · 哈尔滨市道里区妇幼保健院（`DL-FUCHAN`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：4 条（CR-028～CR-031）

#### CR-028 · 道里妇幼棉花针5合1含包材

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市道里区妇幼保健院（`DL-FUCHAN`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：棉花针
- 器械件数 ≤ 10

**示例**

```
医院：哈尔滨市道里区妇幼保健院
包名：棉花针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-029 · 道里妇幼棉花针5合1免包材

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市道里区妇幼保健院（`DL-FUCHAN`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：棉花针
- 器械件数 ≥ 11
- 命中后 **不计包装/袋材费**

**示例**

```
医院：哈尔滨市道里区妇幼保健院
包名：棉花针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-030 · 道里妇幼洗髓针5合1含包材

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市道里区妇幼保健院（`DL-FUCHAN`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：洗髓针
- 器械件数 ≤ 10

**示例**

```
医院：哈尔滨市道里区妇幼保健院
包名：洗髓针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-031 · 道里妇幼洗髓针5合1免包材

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市道里区妇幼保健院（`DL-FUCHAN`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：洗髓针
- 器械件数 ≥ 11
- 命中后 **不计包装/袋材费**

**示例**

```
医院：哈尔滨市道里区妇幼保健院
包名：洗髓针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.6 客户 · 黑龙江省第二医院（南岗院区）（`ERYY-NG`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：1 条（CR-032～CR-032）

#### CR-032 · 省二南岗挖勺每件5.5元

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省第二医院（南岗院区）（`ERYY-NG`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：挖勺
- 命中后 **不计包装/袋材费**

**示例**

```
医院：黑龙江省第二医院（南岗院区）
包名：挖勺-5/Z7520，器械 5 件
账单价：22.5 元
→ 引擎建议：5×5.5=27.5 元（规则配置不计包装费），status=warning
```

### 5.7 客户 · ERYY-SB（`ERYY-SB`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：8 条（CR-033～CR-040）

#### CR-033 · 手术衣无纺布现价

| 属性 | 值 |
|------|-----|
| 客户 | ERYY-SB（`ERYY-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **26.6 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：手术衣
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：ERYY-SB
包名：手术衣-示例/Z7520
账单价：36.6 元（与规则价不符）
→ 引擎建议：26.6 元/包，status=warning
```

#### CR-034 · 手术衣纸塑袋现价

| 属性 | 值 |
|------|-----|
| 客户 | ERYY-SB（`ERYY-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **28 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：手术衣
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：ERYY-SB
包名：手术衣-示例/Z7520
账单价：38 元（与规则价不符）
→ 引擎建议：28 元/包，status=warning
```

#### CR-035 · 儿童头钉现价

| 属性 | 值 |
|------|-----|
| 客户 | ERYY-SB（`ERYY-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **15.4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：儿童头钉
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：ERYY-SB
包名：儿童头钉-示例/Z7520
账单价：25.4 元（与规则价不符）
→ 引擎建议：15.4 元/包，status=warning
```

#### CR-036 · 取钉包多报价

| 属性 | 值 |
|------|-----|
| 客户 | ERYY-SB（`ERYY-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **207.9 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：取钉包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：ERYY-SB
包名：取钉包-示例/Z7520
账单价：217.9 元（与规则价不符）
→ 引擎建议：207.9 元/包，status=warning
```

#### CR-037 · 省二松北宫腔检查镜30度3件

| 属性 | 值 |
|------|-----|
| 客户 | ERYY-SB（`ERYY-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 9（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30.8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：宫腔检查镜30度
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：ERYY-SB
包名：宫腔检查镜30度-示例/Z7520
账单价：40.8 元（与规则价不符）
→ 引擎建议：30.8 元/包，status=warning
```

#### CR-038 · 省二松北扩棒7-10

| 属性 | 值 |
|------|-----|
| 客户 | ERYY-SB（`ERYY-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **26.95 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：扩棒（7-10）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：ERYY-SB
包名：扩棒（7-10）-示例/Z7520
账单价：36.95 元（与规则价不符）
→ 引擎建议：26.95 元/包，status=warning
```

#### CR-039 · 省二松北扩棒7.5-10

| 属性 | 值 |
|------|-----|
| 客户 | ERYY-SB（`ERYY-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **23.1 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：扩棒（7.5-10）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：ERYY-SB
包名：扩棒（7.5-10）-示例/Z7520
账单价：33.1 元（与规则价不符）
→ 引擎建议：23.1 元/包，status=warning
```

#### CR-040 · 省二松北取钉包

| 属性 | 值 |
|------|-----|
| 客户 | ERYY-SB（`ERYY-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **207.9 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：取钉包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：ERYY-SB
包名：取钉包-示例/Z7520
账单价：217.9 元（与规则价不符）
→ 引擎建议：207.9 元/包，status=warning
```

### 5.8 客户 · 方南南医院（`FNN-YY`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：2 条（CR-041～CR-042）

#### CR-041 · 方南南小件5合1含包材

| 属性 | 值 |
|------|-----|
| 客户 | 方南南医院（`FNN-YY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：P钻、根管锉、光滑针、机扩针
- 器械件数 ≤ 10

**示例**

```
医院：方南南医院
包名：P钻-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-042 · 方南南小件5合1免包材

| 属性 | 值 |
|------|-----|
| 客户 | 方南南医院（`FNN-YY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：P钻、根管锉、光滑针、机扩针
- 器械件数 ≥ 11
- 命中后 **不计包装/袋材费**

**示例**

```
医院：方南南医院
包名：P钻-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.9 客户 · 国药总医院第二院区（`GUOYAO-2`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：5 条（CR-043～CR-047）

#### CR-043 · 电机厂缝合针8元

| 属性 | 值 |
|------|-----|
| 客户 | 国药总医院第二院区（`GUOYAO-2`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：缝合针
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：国药总医院第二院区
包名：缝合针-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-044 · 电机厂双按件5.5外层袋

| 属性 | 值 |
|------|-----|
| 客户 | 国药总医院第二院区（`GUOYAO-2`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：双
- **排除**关键词：(双)、/(双)、双极、双极线
- 器械件数 ≤ 2
- 灭菌类型 = `HT`（HT=高温，LT=低温）
- 命中后 **不计客户折扣**

**示例**

```
医院：国药总医院第二院区
包名：双-5/Z7520，器械 5 件
账单价：22.5 元
→ 引擎建议：5×5.5=27.5 元，status=warning
```

#### CR-045 · 电机厂双按件5.5内层袋

| 属性 | 值 |
|------|-----|
| 客户 | 国药总医院第二院区（`GUOYAO-2`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：双
- **排除**关键词：(双)、/(双)、双极、双极线
- 器械件数 ≥ 3
- 灭菌类型 = `HT`（HT=高温，LT=低温）
- 命中后 **不计客户折扣**

**示例**

```
医院：国药总医院第二院区
包名：双-3/Z7520，器械 3 件
账单价：11.5 元
→ 引擎建议：3×5.5=16.5 元，status=warning
```

#### CR-046 · 电机厂指针5合1含包材

| 属性 | 值 |
|------|-----|
| 客户 | 国药总医院第二院区（`GUOYAO-2`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 14（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：指针
- 器械件数 ≤ 10

**示例**

```
医院：国药总医院第二院区
包名：指针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-047 · 电机厂指针5合1免包材

| 属性 | 值 |
|------|-----|
| 客户 | 国药总医院第二院区（`GUOYAO-2`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：指针
- 器械件数 ≥ 11
- 命中后 **不计包装/袋材费**

**示例**

```
医院：国药总医院第二院区
包名：指针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.10 客户 · 国药总医院第三院区（`GUOYAO-3`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：1 条（CR-048～CR-048）

#### CR-048 · 校正价35.0

| 属性 | 值 |
|------|-----|
| 客户 | 国药总医院第三院区（`GUOYAO-3`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：辅料包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：国药总医院第三院区
包名：辅料包-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

### 5.11 客户 · 国药总医院主院区（`GUOYAO-MAIN`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：3 条（CR-049～CR-051）

#### CR-049 · 10mm30度镜固定价

| 属性 | 值 |
|------|-----|
| 客户 | 国药总医院主院区（`GUOYAO-MAIN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **28 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：30度镜、10mm
- 灭菌类型 = `HT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：国药总医院主院区
包名：30度镜-示例/Z7520
账单价：38 元（与规则价不符）
→ 引擎建议：28 元/包，status=warning
```

#### CR-050 · 校正价35.0

| 属性 | 值 |
|------|-----|
| 客户 | 国药总医院主院区（`GUOYAO-MAIN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：辅料包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：国药总医院主院区
包名：辅料包-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-051 · 驱血带固定价

| 属性 | 值 |
|------|-----|
| 客户 | 国药总医院主院区（`GUOYAO-MAIN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：驱血带
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：国药总医院主院区
包名：驱血带-示例/Z7520
账单价：23 元（与规则价不符）
→ 引擎建议：13 元/包，status=warning
```

### 5.12 客户 · 黑龙江省海员总医院（松北）（`HAIYUAN-SB`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：2 条（CR-052～CR-053）

#### CR-052 · 海员胶帽≤5按1件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省海员总医院（松北）（`HAIYUAN-SB`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胶帽
- 器械件数 ≤ 5
- 灭菌类型 = `LT`（HT=高温，LT=低温）

**示例**

```
医院：黑龙江省海员总医院（松北）
包名：胶帽-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-053 · 海员胶帽>5折算22

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省海员总医院（松北）（`HAIYUAN-SB`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胶帽
- 器械件数 ≥ 6
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**

**示例**

```
医院：黑龙江省海员总医院（松北）
包名：胶帽-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.13 客户 · 黑龙江总工会医院（`HL-ZGH`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：1 条（CR-054～CR-054）

#### CR-054 · 总工会镜头铂康盒加收8

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江总工会医院（`HL-ZGH`） |
| 规则类型 | `EXTRA_FEE` — 附加费 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后在基础价上 **+8 元**（附加费）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：12°镜头、12度镜头、镜头
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江总工会医院
包名：12°镜头
基础价 8.5 元 + 附加费 8 元 → 合计 16.5 元
```

### 5.14 客户 · HLFB-SF（`HLFB-SF`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：1 条（CR-055～CR-055）

#### CR-055 · 校正价13.5

| 属性 | 值 |
|------|-----|
| 客户 | HLFB-SF（`HLFB-SF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：车针
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：HLFB-SF
包名：车针-示例/Z7520
账单价：23.5 元（与规则价不符）
→ 引擎建议：13.5 元/包，status=warning
```

### 5.15 客户 · 黑龙江省妇幼保健院（人口）（`HLJ-FY-RK`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：2 条（CR-056～CR-057）

#### CR-056 · 妇幼人口密封件≤5按1件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省妇幼保健院（人口）（`HLJ-FY-RK`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：密封件、密封胶圈
- 器械件数 ≤ 5
- 灭菌类型 = `LT`（HT=高温，LT=低温）

**示例**

```
医院：黑龙江省妇幼保健院（人口）
包名：密封件-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-057 · 妇幼人口密封件>5折算22

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省妇幼保健院（人口）（`HLJ-FY-RK`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：密封件、密封胶圈
- 器械件数 ≥ 6
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**

**示例**

```
医院：黑龙江省妇幼保健院（人口）
包名：密封件-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.16 客户 · 省监狱管理局医院（`HLJ-JYGLJ-YY`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：3 条（CR-058～CR-060）

#### CR-058 · 微克夹钳35元

| 属性 | 值 |
|------|-----|
| 客户 | 省监狱管理局医院（`HLJ-JYGLJ-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：微克夹钳
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：省监狱管理局医院
包名：微克夹钳-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-059 · 监狱密封件≤5按1件

| 属性 | 值 |
|------|-----|
| 客户 | 省监狱管理局医院（`HLJ-JYGLJ-YY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：密封件、密封胶圈
- 器械件数 ≤ 5
- 灭菌类型 = `LT`（HT=高温，LT=低温）

**示例**

```
医院：省监狱管理局医院
包名：密封件-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-060 · 监狱密封件>5折算22

| 属性 | 值 |
|------|-----|
| 客户 | 省监狱管理局医院（`HLJ-JYGLJ-YY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：密封件、密封胶圈
- 器械件数 ≥ 6
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**

**示例**

```
医院：省监狱管理局医院
包名：密封件-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.17 客户 · 哈尔滨市第二医院（`HRB-2ND`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：7 条（CR-061～CR-067）

#### CR-061 · 口腔科正畸车针8元

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第二医院（`HRB-2ND`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 7（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：正畸去胶车针、正畸去版车针
- 科室 ∈ {口腔科、口腔科(内)、口腔科（正）、口腔修复诊室}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第二医院
包名：正畸去胶车针-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-062 · 口腔科调刀8元

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第二医院（`HRB-2ND`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：调刀
- 科室 ∈ {口腔科、口腔科(内)、口腔科（正）、口腔修复诊室}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第二医院
包名：调刀-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-063 · 手术室止血带8元

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第二医院（`HRB-2ND`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：市二院止血带
- 科室 ∈ {手术室}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第二医院
包名：市二院止血带-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-064 · 手术室电凝钩22元/件

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第二医院（`HRB-2ND`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 9（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 22 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：电凝钩吸引器
- 科室 ∈ {手术室}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第二医院
包名：电凝钩吸引器-5/Z7520，器械 5 件
账单价：105 元
→ 引擎建议：5×22=110 元（规则配置不计包装费），status=warning
```

#### CR-065 · 校正价8.0

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第二医院（`HRB-2ND`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：市二院止血带14个（只消毒）、洁牙尖（保护）1抛光杯1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第二医院
包名：市二院止血带14个（只消毒）-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-066 · 校正价16.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第二医院（`HRB-2ND`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：骨科拉钩-2
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第二医院
包名：骨科拉钩-2-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-067 · PDF/期待价8

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第二医院（`HRB-2ND`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 200（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：市二院止血带14个（只消毒）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第二医院
包名：市二院止血带14个（只消毒）-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

### 5.18 客户 · 哈尔滨长健医院（`HRB-CJ`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：4 条（CR-068～CR-071）

#### CR-068 · 长健敷料包W12050

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨长健医院（`HRB-CJ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 2（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：敷料包/W12050
- 命中后 **不计包装/袋材费**

**示例**

```
医院：哈尔滨长健医院
包名：敷料包/W12050-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-069 · 长健硅胶珠子22

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨长健医院（`HRB-CJ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 2（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：硅胶珠子7号
- 命中后 **不计包装/袋材费**

**示例**

```
医院：哈尔滨长健医院
包名：硅胶珠子7号-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

#### CR-070 · 长健结款单行对齐

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨长健医院（`HRB-CJ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **50 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：刀柄
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨长健医院
包名：刀柄-示例/Z7520
账单价：60 元（与规则价不符）
→ 引擎建议：50 元/包，status=warning
```

#### CR-071 · 手术包5.5元/件

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨长健医院（`HRB-CJ`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：手术包
- 灭菌类型 = `HT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨长健医院
包名：手术包-5/Z7520，器械 5 件
账单价：22.5 元
→ 引擎建议：5×5.5=27.5 元（规则配置不计包装费），status=warning
```

### 5.19 客户 · 哈尔滨工业大学医院（`HRB-HIT`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：9 条（CR-072～CR-080）

#### CR-072 · 哈工大export 胆囊包10件165

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨工业大学医院（`HRB-HIT`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **165 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胆囊包10件（盒1）
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨工业大学医院
包名：胆囊包10件（盒1）-示例/Z7520
账单价：175 元（与规则价不符）
→ 引擎建议：165 元/包，status=warning
```

#### CR-073 · 哈工大换药碗-1 13

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨工业大学医院（`HRB-HIT`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：换药碗-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨工业大学医院
包名：换药碗-1-示例/Z7520
账单价：23 元（与规则价不符）
→ 引擎建议：13 元/包，status=warning
```

#### CR-074 · 哈工大换药碗-2 16.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨工业大学医院（`HRB-HIT`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：换药碗-2
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨工业大学医院
包名：换药碗-2-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-075 · 哈工大棉球不带线2.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨工业大学医院（`HRB-HIT`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **2.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：棉球（不带线）、棉球(不带线)
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨工业大学医院
包名：棉球（不带线）-示例/Z7520
账单价：12.5 元（与规则价不符）
→ 引擎建议：2.5 元/包，status=warning
```

#### CR-076 · 哈工大种植盒23件5.5/件

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨工业大学医院（`HRB-HIT`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：种植盒-23件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨工业大学医院
包名：种植盒-23件-5/Z7520，器械 5 件
账单价：22.5 元
→ 引擎建议：5×5.5=27.5 元（规则配置不计包装费），status=warning
```

#### CR-077 · 克氏针5.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨工业大学医院（`HRB-HIT`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **5.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：克氏针-3/z7534
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨工业大学医院
包名：克氏针-3/z7534-示例/Z7520
账单价：15.5 元（与规则价不符）
→ 引擎建议：5.5 元/包，status=warning
```

#### CR-078 · 车针多报价

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨工业大学医院（`HRB-HIT`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 9（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：车针
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨工业大学医院
包名：车针-示例/Z7520
账单价：23.5 元（与规则价不符）
→ 引擎建议：13.5 元/包，status=warning
```

#### CR-079 · 口腔针类5.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨工业大学医院（`HRB-HIT`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **5.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：洁牙尖、成型片
- **排除**关键词：泪道探针、探针包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨工业大学医院
包名：洁牙尖-示例/Z7520
账单价：15.5 元（与规则价不符）
→ 引擎建议：5.5 元/包，status=warning
```

#### CR-080 · 口腔小件5件计1

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨工业大学医院（`HRB-HIT`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：洁牙尖、成型片
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨工业大学医院
包名：洁牙尖-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.20 客户 · 哈尔滨市红十字妇产医院（`HRB-HSZ`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：4 条（CR-081～CR-084）

#### CR-081 · 低温1件22元

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市红十字妇产医院（`HRB-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 无关键词限制（通常绑定产品 ID 或全局）
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市红十字妇产医院
包名：某包-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

#### CR-082 · 湿化瓶2件22元

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市红十字妇产医院（`HRB-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：湿化瓶
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市红十字妇产医院
包名：湿化瓶-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

#### CR-083 · 纤维喉镜300

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市红十字妇产医院（`HRB-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 30（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **300 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纤维喉镜、气管镜、软管
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市红十字妇产医院
包名：纤维喉镜-示例/Z7520
账单价：310 元（与规则价不符）
→ 引擎建议：300 元/包，status=warning
```

#### CR-084 · T型管25

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市红十字妇产医院（`HRB-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 40（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **25 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：T型管
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市红十字妇产医院
包名：T型管-示例/Z7520
账单价：35 元（与规则价不符）
→ 引擎建议：25 元/包，status=warning
```

### 5.21 客户 · HRB-HTFH（`HRB-HTFH`）

- 计价模式：`standard` — 标准灭菌表（默认）
- 启用规则：2 条（CR-085～CR-086）

#### CR-085 · 航天风华镍钛锉5合1含包材

| 属性 | 值 |
|------|-----|
| 客户 | HRB-HTFH（`HRB-HTFH`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表（默认） |
| 优先级 | 18（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：镍钛锉
- 器械件数 ≤ 10

**示例**

```
医院：HRB-HTFH
包名：镍钛锉-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-086 · 航天风华镍钛锉5合1免包材

| 属性 | 值 |
|------|-----|
| 客户 | HRB-HTFH（`HRB-HTFH`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表（默认） |
| 优先级 | 19（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：镍钛锉
- 器械件数 ≥ 11
- 命中后 **不计包装/袋材费**

**示例**

```
医院：HRB-HTFH
包名：镍钛锉-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.22 客户 · 哈尔滨华夏眼科医院（`HRB-HX-EYE`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：1 条（CR-087～CR-087）

#### CR-087 · ≥3件器械包单价2.75

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨华夏眼科医院（`HRB-HX-EYE`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 2.75 元/件**

**生效条件**（须全部满足）

- 无关键词限制（通常绑定产品 ID 或全局）
- 器械件数 ≥ 3
- 灭菌类型 = `HT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**

**示例**

```
医院：哈尔滨华夏眼科医院
包名：某包-3/Z7520，器械 3 件
账单价：3.25 元
→ 引擎建议：3×2.75=8.25 元（规则配置不计包装费），status=warning
```

### 5.23 客户 · HRB-MHM（`HRB-MHM`）

- 计价模式：`standard` — 标准灭菌表（默认）
- 启用规则：1 条（CR-088～CR-088）

#### CR-088 · 美涵吸脂针20cm以下5件算1件

| 属性 | 值 |
|------|-----|
| 客户 | HRB-MHM（`HRB-MHM`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表（默认） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：型号20cm以下、20cm以下

**示例**

```
医院：HRB-MHM
包名：型号20cm以下-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.24 客户 · 哈尔滨市南岗区人民医院（九院）（`HRB-NGJY`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：1 条（CR-089～CR-089）

#### CR-089 · 九院敷料包0元覆盖15

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市南岗区人民医院（九院）（`HRB-NGJY`） |
| 规则类型 | `ZERO_PRICE_OVERRIDE` — 零价覆盖 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后强制按零价导入规则处理（覆盖原价）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：敷料包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

（示例：包名含「敷料包」时命中 `九院敷料包0元覆盖15`）

### 5.25 客户 · HRB-SD-MB（`HRB-SD-MB`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：2 条（CR-090～CR-091）

#### CR-090 · 松电机扩针5合1含包材

| 属性 | 值 |
|------|-----|
| 客户 | HRB-SD-MB（`HRB-SD-MB`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：机扩针、车针、扩大针、根管针、根管锉、镍钛锉、探针、球钻、拔髓针
- 器械件数 ≤ 10

**示例**

```
医院：HRB-SD-MB
包名：机扩针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-091 · 松电机扩针5合1免包材

| 属性 | 值 |
|------|-----|
| 客户 | HRB-SD-MB（`HRB-SD-MB`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：机扩针、车针、扩大针、根管针、根管锉、镍钛锉、探针、球钻、拔髓针
- 器械件数 ≥ 11
- 命中后 **不计包装/袋材费**

**示例**

```
医院：HRB-SD-MB
包名：机扩针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.26 客户 · 哈尔滨森海医院（`HRB-SH`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：2 条（CR-092～CR-093）

#### CR-092 · 胶帽22元/件

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨森海医院（`HRB-SH`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 22 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胶帽
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨森海医院
包名：胶帽-5/Z7520，器械 5 件
账单价：105 元
→ 引擎建议：5×22=110 元（规则配置不计包装费），status=warning
```

#### CR-093 · 纱布4元

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨森海医院（`HRB-SH`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纱布
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨森海医院
包名：纱布-示例/Z7520
账单价：14 元（与规则价不符）
→ 引擎建议：4 元/包，status=warning
```

### 5.27 客户 · 哈尔滨市第五医院（`HRB-WY`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：12 条（CR-094～CR-105）

#### CR-094 · 市五export DMD光源1号35

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：DMD光源1号-1/W905060
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：DMD光源1号-1/W905060-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-095 · 市五export DMD光源3号35

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：DMD光源3号-1/W905060
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：DMD光源3号-1/W905060-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-096 · 市五export 磨头Z102688

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **88 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：骨四（2）邹天彪磨头-4件/Z1026
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：骨四（2）邹天彪磨头-4件/Z1026-示例/Z7520
账单价：98 元（与规则价不符）
→ 引擎建议：88 元/包，status=warning
```

#### CR-097 · 市五export 胸外镜头W90605035

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胸外镜头-1（盒1）/W906050
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：胸外镜头-1（盒1）/W906050-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-098 · 市五export 胸腔镜99

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **99 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胸腔镜-17件（筐1）
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：胸腔镜-17件（筐1）-示例/Z7520
账单价：109 元（与规则价不符）
→ 引擎建议：99 元/包，status=warning
```

#### CR-099 · 市五export 骨六电钻16.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：骨六业皋电钻-2件/W705060
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：骨六业皋电钻-2件/W705060-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-100 · 校正价16.5

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：AR水管-2件/双、德美2号刨刀-1/双、德美4K1号刨刀-1、旧轧皮机（2号）-2件盒1、西山骨动力1号-1/双、试用肩镜-2件/双、骨四1 宋晓辉韧带咬钳 2件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：AR水管-2件/双-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-101 · 校正价22.0

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：半月板缝合器械-4
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：半月板缝合器械-4-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

#### CR-102 · 校正价44.0

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **44 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：张长青超声刀线-2件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：张长青超声刀线-2件-示例/Z7520
账单价：54 元（与规则价不符）
→ 引擎建议：44 元/包，status=warning
```

#### CR-103 · 校正价88.0

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **88 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：创伤1号机器人-4件（盒1）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：创伤1号机器人-4件（盒1）-示例/Z7520
账单价：98 元（与规则价不符）
→ 引擎建议：88 元/包，status=warning
```

#### CR-104 · 校正价99.0

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 14（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **99 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胸腔镜-17件（筐1）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：胸腔镜-17件（筐1）-示例/Z7520
账单价：109 元（与规则价不符）
→ 引擎建议：99 元/包，status=warning
```

#### CR-105 · 校正价132.0

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨市第五医院（`HRB-WY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **132 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：骨六房玉利肩钩常备器械
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨市第五医院
包名：骨六房玉利肩钩常备器械-示例/Z7520
账单价：142 元（与规则价不符）
→ 引擎建议：132 元/包，status=warning
```

### 5.28 客户 · 呼兰区红十字医院（`HULAN-HSZ`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：1 条（CR-106～CR-106）

#### CR-106 · 校正价8.0

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区红十字医院（`HULAN-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：车针
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区红十字医院
包名：车针-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

### 5.29 客户 · 呼兰区第一人民医院（`HULAN-RM`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：9 条（CR-107～CR-115）

#### CR-107 · PDF敷料小20x20x15

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区第一人民医院（`HULAN-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **10.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：敷料、20cm*20cm*15cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区第一人民医院
包名：敷料-示例/Z7520
账单价：20.5 元（与规则价不符）
→ 引擎建议：10.5 元/包，status=warning
```

#### CR-108 · PDF敷料中20x30x30

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区第一人民医院（`HULAN-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **14 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：敷料、20cm*30cm*30cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区第一人民医院
包名：敷料-示例/Z7520
账单价：24 元（与规则价不符）
→ 引擎建议：14 元/包，status=warning
```

#### CR-109 · PDF纱布棉球大20x20x15

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区第一人民医院（`HULAN-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **28 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纱布、棉球
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区第一人民医院
包名：纱布-示例/Z7520
账单价：38 元（与规则价不符）
→ 引擎建议：28 元/包，status=warning
```

#### CR-110 · PDF纸塑袋15cm

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区第一人民医院（`HULAN-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **3.85 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纸塑袋
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区第一人民医院
包名：纸塑袋-示例/Z7520
账单价：13.85 元（与规则价不符）
→ 引擎建议：3.85 元/包，status=warning
```

#### CR-111 · PDF纸塑袋10cm

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区第一人民医院（`HULAN-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 21（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **3.56 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纸塑袋
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区第一人民医院
包名：纸塑袋-示例/Z7520
账单价：13.56 元（与规则价不符）
→ 引擎建议：3.56 元/包，status=warning
```

#### CR-112 · PDF外来器械骨电钻11.55

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区第一人民医院（`HULAN-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 25（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **11.55 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外来器械（骨电钻）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区第一人民医院
包名：外来器械（骨电钻）-示例/Z7520
账单价：21.55 元（与规则价不符）
→ 引擎建议：11.55 元/包，status=warning
```

#### CR-113 · PDF外来器械植入物3.85

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区第一人民医院（`HULAN-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 26（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **3.85 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外来器械（植入物）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区第一人民医院
包名：外来器械（植入物）-示例/Z7520
账单价：13.85 元（与规则价不符）
→ 引擎建议：3.85 元/包，status=warning
```

#### CR-114 · PDF高温器械柜保护套3.5

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区第一人民医院（`HULAN-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 30（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **3.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：保护套、器械柜
- 灭菌类型 = `HT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区第一人民医院
包名：保护套-示例/Z7520
账单价：13.5 元（与规则价不符）
→ 引擎建议：3.5 元/包，status=warning
```

#### CR-115 · PDF低温器械柜保护套5.6

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰区第一人民医院（`HULAN-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 31（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **5.6 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：保护套、器械柜
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰区第一人民医院
包名：保护套-示例/Z7520
账单价：15.6 元（与规则价不符）
→ 引擎建议：5.6 元/包，status=warning
```

### 5.30 客户 · 呼兰中医院（`HULAN-TCM`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：4 条（CR-116～CR-119）

#### CR-116 · 呼兰中医export 腹腔镜297

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰中医院（`HULAN-TCM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **297 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外科腹腔镜-16件筐1/W12050
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰中医院
包名：外科腹腔镜-16件筐1/W12050-示例/Z7520
账单价：307 元（与规则价不符）
→ 引擎建议：297 元/包，status=warning
```

#### CR-117 · 外科包固定价

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰中医院（`HULAN-TCM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **249.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外科包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰中医院
包名：外科包-示例/Z7520
账单价：259.5 元（与规则价不符）
→ 引擎建议：249.5 元/包，status=warning
```

#### CR-118 · 校正价275.0

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰中医院（`HULAN-TCM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **275 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外科腹腔镜-16件筐1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰中医院
包名：外科腹腔镜-16件筐1-示例/Z7520
账单价：285 元（与规则价不符）
→ 引擎建议：275 元/包，status=warning
```

#### CR-119 · 阑尾包固定价

| 属性 | 值 |
|------|-----|
| 客户 | 呼兰中医院（`HULAN-TCM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **288 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：阑尾包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：呼兰中医院
包名：阑尾包-示例/Z7520
账单价：298 元（与规则价不符）
→ 引擎建议：288 元/包，status=warning
```

### 5.31 客户 · 佳医医疗（`JIAYI-YL`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：1 条（CR-120～CR-120）

#### CR-120 · 佳医敷料纸塑4元

| 属性 | 值 |
|------|-----|
| 客户 | 佳医医疗（`JIAYI-YL`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：孔巾、眼包敷料
- 纸塑袋宽 ≥ 20 cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：佳医医疗
包名：孔巾-示例/Z7520
账单价：14 元（与规则价不符）
→ 引擎建议：4 元/包，status=warning
```

### 5.32 客户 · 黑龙江九洲妇科医院（`JIUZHOU-FK`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：1 条（CR-121～CR-121）

#### CR-121 · 方盘5.5

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江九洲妇科医院（`JIUZHOU-FK`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **5.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：方盘
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江九洲妇科医院
包名：方盘-示例/Z7520
账单价：15.5 元（与规则价不符）
→ 引擎建议：5.5 元/包，status=warning
```

### 5.33 客户 · 哈尔滨基准生物科技有限公司（`JZSW-BIO`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：1 条（CR-122～CR-122）

#### CR-122 · 氩氦刀（EO）150元

| 属性 | 值 |
|------|-----|
| 客户 | 哈尔滨基准生物科技有限公司（`JZSW-BIO`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **150 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：氩氦刀
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：哈尔滨基准生物科技有限公司
包名：氩氦刀-示例/Z7520
账单价：160 元（与规则价不符）
→ 引擎建议：150 元/包，status=warning
```

### 5.34 客户 · 美意医疗（`MEIYI-YL`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：1 条（CR-123～CR-123）

#### CR-123 · 美意敷料纸塑4元

| 属性 | 值 |
|------|-----|
| 客户 | 美意医疗（`MEIYI-YL`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：洞巾、治疗巾
- 纸塑袋宽 ≥ 20 cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：美意医疗
包名：洞巾-示例/Z7520
账单价：14 元（与规则价不符）
→ 引擎建议：4 元/包，status=warning
```

### 5.35 客户 · NEAU-YY（`NEAU-YY`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：4 条（CR-124～CR-127）

#### CR-124 · 东北农大根管锉5.6含包材

| 属性 | 值 |
|------|-----|
| 客户 | NEAU-YY（`NEAU-YY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：根管锉
- 器械件数 ≤ 10

**示例**

```
医院：NEAU-YY
包名：根管锉-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-125 · 东北农大根管锉5.6免包材

| 属性 | 值 |
|------|-----|
| 客户 | NEAU-YY（`NEAU-YY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 9（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：根管锉
- 器械件数 ≥ 11
- 命中后 **不计包装/袋材费**

**示例**

```
医院：NEAU-YY
包名：根管锉-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-126 · 东北农大小件5合1含包材

| 属性 | 值 |
|------|-----|
| 客户 | NEAU-YY（`NEAU-YY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：根管针、机锉、牙探针、机扩针、车针、扩大针、拔髓针、探针、球钻
- 器械件数 ≤ 10

**示例**

```
医院：NEAU-YY
包名：根管针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-127 · 东北农大小件5合1免包材

| 属性 | 值 |
|------|-----|
| 客户 | NEAU-YY（`NEAU-YY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：根管针、机锉、牙探针、机扩针、车针、扩大针、拔髓针、探针、球钻
- 器械件数 ≥ 11
- 命中后 **不计包装/袋材费**

**示例**

```
医院：NEAU-YY
包名：根管针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.36 客户 · 南岗区妇产医院（`NG-FUCHAN`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：19 条（CR-128～CR-146）

#### CR-128 · 校正价2.3

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **2.3 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纱布
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：纱布-示例/Z7520
账单价：12.3 元（与规则价不符）
→ 引擎建议：2.3 元/包，status=warning
```

#### CR-129 · 校正价8.0

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：取环器-1、宫颈钳-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：取环器-1-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-130 · 校正价16.0

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：弯盘-2
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：弯盘-2-示例/Z7520
账单价：26 元（与规则价不符）
→ 引擎建议：16 元/包，status=warning
```

#### CR-131 · 校正价24.0

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **24 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：扩棒（4 4.5 5）-3
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：扩棒（4 4.5 5）-3-示例/Z7520
账单价：34 元（与规则价不符）
→ 引擎建议：24 元/包，status=warning
```

#### CR-132 · 校正价弯盘8

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：弯盘
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：弯盘-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-133 · 校正价扩棒8

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：扩棒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：扩棒-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-134 · 校正价32.0

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 14（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **32 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：盆2盘1杯1(腹腔镜里的)
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：盆2盘1杯1(腹腔镜里的)-示例/Z7520
账单价：42 元（与规则价不符）
→ 引擎建议：32 元/包，status=warning
```

#### CR-135 · 校正价170.5

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **170.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：宫腔镜
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：宫腔镜-示例/Z7520
账单价：180.5 元（与规则价不符）
→ 引擎建议：170.5 元/包，status=warning
```

#### CR-136 · PDF带架治疗盘16.5

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：带架治疗
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：带架治疗-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-137 · 南岗妇产棉球4

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：棉球/Z2032
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：棉球/Z2032-示例/Z7520
账单价：14 元（与规则价不符）
→ 引擎建议：4 元/包，status=warning
```

#### CR-138 · 校正价扩棒6.5套8

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：扩棒（6.5 7 7.5）-3件z7526
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：扩棒（6.5 7 7.5）-3件z7526-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-139 · PDF小治疗16

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 21（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：小治疗
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：小治疗-示例/Z7520
账单价：26 元（与规则价不符）
→ 引擎建议：16 元/包，status=warning
```

#### CR-140 · PDF取环包33

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 22（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **33 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：取环包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：取环包-示例/Z7520
账单价：43 元（与规则价不符）
→ 引擎建议：33 元/包，status=warning
```

#### CR-141 · PDF上环包33

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 23（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **33 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：上环包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：上环包-示例/Z7520
账单价：43 元（与规则价不符）
→ 引擎建议：33 元/包，status=warning
```

#### CR-142 · PDF药流包27.5

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 24（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **27.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：药流包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：药流包-示例/Z7520
账单价：37.5 元（与规则价不符）
→ 引擎建议：27.5 元/包，status=warning
```

#### CR-143 · PDF人工流产33

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 25（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **33 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：人工流产
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：人工流产-示例/Z7520
账单价：43 元（与规则价不符）
→ 引擎建议：33 元/包，status=warning
```

#### CR-144 · PDF人流包33

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 26（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **33 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：人流包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：人流包-示例/Z7520
账单价：43 元（与规则价不符）
→ 引擎建议：33 元/包，status=warning
```

#### CR-145 · PDF诊刮包8

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 27（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：诊刮
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：诊刮-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-146 · PDF大领子8

| 属性 | 值 |
|------|-----|
| 客户 | 南岗区妇产医院（`NG-FUCHAN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 28（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：大领子
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：南岗区妇产医院
包名：大领子-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

### 5.37 客户 · 三精肾病医院（`SANJING-SB`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：11 条（CR-147～CR-157）

#### CR-147 · 三精export 内瘘68件102

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **102 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：内瘘器械包（一）
- 器械件数 ≥ 68
- 器械件数 ≤ 68
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：内瘘器械包（一）-示例/Z7520
账单价：112 元（与规则价不符）
→ 引擎建议：102 元/包，status=warning
```

#### CR-148 · 校正价6.4

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **6.4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：哈巴狗止血夹-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：哈巴狗止血夹-1-示例/Z7520
账单价：16.4 元（与规则价不符）
→ 引擎建议：6.4 元/包，status=warning
```

#### CR-149 · 校正价9.0

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **9 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：开口器1 舌钳子1 压舌板1、持物钳罐-3件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：开口器1 舌钳子1 压舌板1-示例/Z7520
账单价：19 元（与规则价不符）
→ 引擎建议：9 元/包，status=warning
```

#### CR-150 · 校正价30.0

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：插管包二
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：插管包二-示例/Z7520
账单价：40 元（与规则价不符）
→ 引擎建议：30 元/包，status=warning
```

#### CR-151 · 校正价42.0

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **42 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：插管包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：插管包-示例/Z7520
账单价：52 元（与规则价不符）
→ 引擎建议：42 元/包，status=warning
```

#### CR-152 · 校正价99.0

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 14（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **99 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：内瘘器械包（一）
- 器械件数 ≥ 66
- 器械件数 ≤ 66
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：内瘘器械包（一）-示例/Z7520
账单价：109 元（与规则价不符）
→ 引擎建议：99 元/包，status=warning
```

#### CR-153 · 校正价102.0

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **102 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：腹透包-34件（临时）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：腹透包-34件（临时）-示例/Z7520
账单价：112 元（与规则价不符）
→ 引擎建议：102 元/包，status=warning
```

#### CR-154 · 校正价102.0-内瘘68件

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **102 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：内瘘器械包（一）
- 器械件数 ≥ 68
- 器械件数 ≤ 68
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：内瘘器械包（一）-示例/Z7520
账单价：112 元（与规则价不符）
→ 引擎建议：102 元/包，status=warning
```

#### CR-155 · PDF外来器械4.4

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 40（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **4.4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外来器械
- **排除**关键词：钉盒、骨电钻、植入物
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：外来器械-示例/Z7520
账单价：14.4 元（与规则价不符）
→ 引擎建议：4.4 元/包，status=warning
```

#### CR-156 · PDF外来器械钉盒40

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 41（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **40 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外来器械（钉盒）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：外来器械（钉盒）-示例/Z7520
账单价：50 元（与规则价不符）
→ 引擎建议：40 元/包，status=warning
```

#### CR-157 · PDF外来器械骨电钻13.2

| 属性 | 值 |
|------|-----|
| 客户 | 三精肾病医院（`SANJING-SB`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 42（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.2 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外来器械（骨电钻）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：三精肾病医院
包名：外来器械（骨电钻）-示例/Z7520
账单价：23.2 元（与规则价不符）
→ 引擎建议：13.2 元/包，status=warning
```

### 5.38 客户 · 黑龙江省医院（南岗院区）（`SHENG-YY-NG`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：10 条（CR-158～CR-167）

#### CR-158 · 省医院南岗export 取环勾4.8

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **4.8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：取环勾-1/7530
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：取环勾-1/7530-示例/Z7520
账单价：14.8 元（与规则价不符）
→ 引擎建议：4.8 元/包，status=warning
```

#### CR-159 · 省医院南岗export 手术衣15

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **15 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：手术衣-1件/W9050
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：手术衣-1件/W9050-示例/Z7520
账单价：25 元（与规则价不符）
→ 引擎建议：15 元/包，status=warning
```

#### CR-160 · 省医院南岗export 舌钳子6.6

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **6.6 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：舌钳子-1/1526
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：舌钳子-1/1526-示例/Z7520
账单价：16.6 元（与规则价不符）
→ 引擎建议：6.6 元/包，status=warning
```

#### CR-161 · 省医院南岗export 间盘A75.9

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **75.9 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：间盘A（52件）2/2
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：间盘A（52件）2/2-示例/Z7520
账单价：85.9 元（与规则价不符）
→ 引擎建议：75.9 元/包，status=warning
```

#### CR-162 · W9050纺织现价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **18 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：床单/W9050、刷手服/W9050、敷布包/W9050、敷料/W9050、手术衣-1件/W9050
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：床单/W9050-示例/Z7520
账单价：28 元（与规则价不符）
→ 引擎建议：18 元/包，status=warning
```

#### CR-163 · 校正价8.1

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8.1 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：小圆针-10
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：小圆针-10-示例/Z7520
账单价：18.1 元（与规则价不符）
→ 引擎建议：8.1 元/包，status=warning
```

#### CR-164 · 省医院南岗手术包6件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22.01 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：手术包-6件、双信封包装
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：手术包-6件-示例/Z7520
账单价：32.01 元（与规则价不符）
→ 引擎建议：22.01 元/包，status=warning
```

#### CR-165 · 校正价22.01

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22.01 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：手术包-6件（双信封包装、门皮 手术包-6件（双信封包装
- 科室 ∈ {美容整形外科、皮肤门诊}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：手术包-6件（双信封包装-示例/Z7520
账单价：32.01 元（与规则价不符）
→ 引擎建议：22.01 元/包，status=warning
```

#### CR-166 · 省医院南岗乐奥2件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **26.4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：乐奥2件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：乐奥2件-示例/Z7520
账单价：36.4 元（与规则价不符）
→ 引擎建议：26.4 元/包，status=warning
```

#### CR-167 · 校正价26.4

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（南岗院区）（`SHENG-YY-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **26.4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：乐奥
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（南岗院区）
包名：乐奥-示例/Z7520
账单价：36.4 元（与规则价不符）
→ 引擎建议：26.4 元/包，status=warning
```

### 5.39 客户 · 黑龙江省医院（香坊院区）（`SHENG-YY-XF`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：11 条（CR-168～CR-178）

#### CR-168 · 手术室胸腔镜17件178.2

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **178.2 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胸腔镜-17件
- 器械件数 ≥ 17
- 器械件数 ≤ 17
- 科室 ∈ {手术室}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：胸腔镜-17件-示例/Z7520
账单价：188.2 元（与规则价不符）
→ 引擎建议：178.2 元/包，status=warning
```

#### CR-169 · 生殖医学科W9050纺织18元

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **18 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：床单/W9050、敷料包/W9050、敷料包、刷手服/W9050
- 科室 ∈ {生殖医学科}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：床单/W9050-示例/Z7520
账单价：28 元（与规则价不符）
→ 引擎建议：18 元/包，status=warning
```

#### CR-170 · 生殖医学科刷手服18元

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **18 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：刷手服
- 科室 ∈ {生殖医学科}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：刷手服-示例/Z7520
账单价：28 元（与规则价不符）
→ 引擎建议：18 元/包，status=warning
```

#### CR-171 · 神外一勾刀-2 26.4元

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **26.4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：勾刀-2、钩刀-2
- 科室 ∈ {神外一}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：勾刀-2-示例/Z7520
账单价：36.4 元（与规则价不符）
→ 引擎建议：26.4 元/包，status=warning
```

#### CR-172 · 手术室额外包胸腔镜19.8

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **19.8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：胸腔镜
- **排除**关键词：w12050、W12050、17件、21件、22件、32件
- 科室 ∈ {手术室}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：胸腔镜-示例/Z7520
账单价：29.8 元（与规则价不符）
→ 引擎建议：19.8 元/包，status=warning
```

#### CR-173 · 神外一勾刀13.2元

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.2 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：勾刀、钩刀
- **排除**关键词：-2
- 科室 ∈ {神外一}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：勾刀-示例/Z7520
账单价：23.2 元（与规则价不符）
→ 引擎建议：13.2 元/包，status=warning
```

#### CR-174 · W9050纺织现价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **18 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：床单/W9050、刷手服/W9050、敷布包/W9050、敷料/W9050、手术衣-1件/W9050
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：床单/W9050-示例/Z7520
账单价：28 元（与规则价不符）
→ 引擎建议：18 元/包，status=warning
```

#### CR-175 · 校正价8.1

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8.1 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：克氏钳-1/双、克氏钳（尖头）-1/双z1029、克氏钳（平头）-1/双
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：克氏钳-1/双-示例/Z7520
账单价：18.1 元（与规则价不符）
→ 引擎建议：8.1 元/包，status=warning
```

#### CR-176 · 校正价13.2

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.2 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：勾刀-2、钩刀-2
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：勾刀-2-示例/Z7520
账单价：23.2 元（与规则价不符）
→ 引擎建议：13.2 元/包，status=warning
```

#### CR-177 · 校正价39.6

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **39.6 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：4K镜头1气腹管1光源1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：4K镜头1气腹管1光源1-示例/Z7520
账单价：49.6 元（与规则价不符）
→ 引擎建议：39.6 元/包，status=warning
```

#### CR-178 · 校正价180.0

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省医院（香坊院区）（`SHENG-YY-XF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **180 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：4K-、外一27件筐1、外三、外二26筐1、好镜-26筐1、妇科-、普镜-、泌腹腔镜-、胸腔镜-
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省医院（香坊院区）
包名：4K--示例/Z7520
账单价：190 元（与规则价不符）
→ 引擎建议：180 元/包，status=warning
```

### 5.40 客户 · 黑龙江省社会康复医院（`SHKF-YY`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：11 条（CR-179～CR-189）

#### CR-179 · 社会康复内提工具盒44

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **44 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：内提工具盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：内提工具盒-示例/Z7520
账单价：54 元（与规则价不符）
→ 引擎建议：44 元/包，status=warning
```

#### CR-180 · 社会康复ITI种植盒44

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **44 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：ITI种植盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：ITI种植盒-示例/Z7520
账单价：54 元（与规则价不符）
→ 引擎建议：44 元/包，status=warning
```

#### CR-181 · 社会康复百诺工具盒44

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **44 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：百诺工具盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：百诺工具盒-示例/Z7520
账单价：54 元（与规则价不符）
→ 引擎建议：44 元/包，status=warning
```

#### CR-182 · 社会康复登腾种植盒44

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 6（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **44 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：登腾种植盒、DT种植盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：登腾种植盒-示例/Z7520
账单价：54 元（与规则价不符）
→ 引擎建议：44 元/包，status=warning
```

#### CR-183 · 社会康复梁光强器械盒粉16.5

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 7（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：梁光强器械盒（粉）、梁光强器械盒(粉)、梁光强器械盒粉
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：梁光强器械盒（粉）-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-184 · 社会康复梁光强器械盒22

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：梁光强器械盒
- **排除**关键词：粉
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：梁光强器械盒-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

#### CR-185 · 社会康复环切器械盒16.5

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 9（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：环切器械盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：环切器械盒-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-186 · 校正价2.5

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **2.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：棉签
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：棉签-示例/Z7520
账单价：12.5 元（与规则价不符）
→ 引擎建议：2.5 元/包，status=warning
```

#### CR-187 · 社会康复抛光车针盒16.5

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：抛光车针盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：抛光车针盒-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-188 · 社会康复大车针盒22

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：大车针盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：大车针盒-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

#### CR-189 · 社会康复种植盒16.5

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省社会康复医院（`SHKF-YY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：种植盒
- **排除**关键词：ITI、登腾、DT
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省社会康复医院
包名：种植盒-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

### 5.41 客户 · 太平人民医院（`TAIPING-RM`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：11 条（CR-190～CR-200）

#### CR-190 · 太平腔镜器械

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 6（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **119.63 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：腔镜器械
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：腔镜器械-示例/Z7520
账单价：129.63 元（与规则价不符）
→ 引擎建议：119.63 元/包，status=warning
```

#### CR-191 · 太平开腹包

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 7（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **119.63 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：开腹包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：开腹包-示例/Z7520
账单价：129.63 元（与规则价不符）
→ 引擎建议：119.63 元/包，status=warning
```

#### CR-192 · 太平缝合包

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **20.63 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：缝合包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：缝合包-示例/Z7520
账单价：30.63 元（与规则价不符）
→ 引擎建议：20.63 元/包，status=warning
```

#### CR-193 · 太平皮肤拉钩

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 9（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **10.13 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：皮肤拉钩
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：皮肤拉钩-示例/Z7520
账单价：20.13 元（与规则价不符）
→ 引擎建议：10.13 元/包，status=warning
```

#### CR-194 · 校正价8.3

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8.3 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：凿子-1、卵圆钳-1、取环器-1、宫颈钳-1、活检钳-1、窥器-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：凿子-1-示例/Z7520
账单价：18.3 元（与规则价不符）
→ 引擎建议：8.3 元/包，status=warning
```

#### CR-195 · 校正价8.91

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8.91 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：产钳-2、剪刀1持针器1钩镊1、剪刀1窥器1宫颈钳1、吸头5号.6号.7号-3件、吸头5号6号7号-3件、吸头6号.7号.8号-3件、小S拉钩-1、巾钳子-2、拆线剪1止血钳1、拆线剪1止血钳小2、持针器1剪刀1止血钳1、探针1窥器1宫颈钳1、止血钳 中-3、活检钳1宫颈钳1、活检钳1宫颈钳1镊子1、电钻-3、舌钳1开口器1、锤子-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：产钳-2-示例/Z7520
账单价：18.91 元（与规则价不符）
→ 引擎建议：8.91 元/包，status=warning
```

#### CR-196 · 校正价9.8

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **9.8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：咬骨钳-1、弯盘-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：咬骨钳-1-示例/Z7520
账单价：19.8 元（与规则价不符）
→ 引擎建议：9.8 元/包，status=warning
```

#### CR-197 · 校正价18.8

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **18.8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：取环钳（低温）-1件、窥器-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：取环钳（低温）-1件-示例/Z7520
账单价：28.8 元（与规则价不符）
→ 引擎建议：18.8 元/包，status=warning
```

#### CR-198 · 校正价26.3

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 14（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **26.3 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：水管-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：水管-1-示例/Z7520
账单价：36.3 元（与规则价不符）
→ 引擎建议：26.3 元/包，status=warning
```

#### CR-199 · 太平肌钩

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **10.13 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：肌钩
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：肌钩-示例/Z7520
账单价：20.13 元（与规则价不符）
→ 引擎建议：10.13 元/包，status=warning
```

#### CR-200 · 太平拇外翻13件

| 属性 | 值 |
|------|-----|
| 客户 | 太平人民医院（`TAIPING-RM`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 16（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **53.63 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：拇外翻
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：太平人民医院
包名：拇外翻-示例/Z7520
账单价：63.63 元（与规则价不符）
→ 引擎建议：53.63 元/包，status=warning
```

### 5.42 客户 · 黑龙江维多利亚妇产医院（`VICTORIA`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：2 条（CR-201～CR-202）

#### CR-201 · 校正价16.0

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江维多利亚妇产医院（`VICTORIA`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：带架方盘-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江维多利亚妇产医院
包名：带架方盘-1-示例/Z7520
账单价：26 元（与规则价不符）
→ 引擎建议：16 元/包，status=warning
```

#### CR-202 · 校正价25.0

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江维多利亚妇产医院（`VICTORIA`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **25 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：手柄-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江维多利亚妇产医院
包名：手柄-1-示例/Z7520
账单价：35 元（与规则价不符）
→ 引擎建议：25 元/包，status=warning
```

### 5.43 客户 · WCSRMYY（`WCSRMYY`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：9 条（CR-203～CR-211）

#### CR-203 · 优贝特空心钉4.0固定233

| 属性 | 值 |
|------|-----|
| 客户 | WCSRMYY（`WCSRMYY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 1（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **233 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：优贝特空心钉4.0
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：WCSRMYY
包名：优贝特空心钉4.0-示例/Z7520
账单价：243 元（与规则价不符）
→ 引擎建议：233 元/包，status=warning
```

#### CR-204 · 空心钉4.0固定233

| 属性 | 值 |
|------|-----|
| 客户 | WCSRMYY（`WCSRMYY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 2（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **233 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：空心钉4.0
- **排除**关键词：优贝特
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：WCSRMYY
包名：空心钉4.0-示例/Z7520
账单价：243 元（与规则价不符）
→ 引擎建议：233 元/包，status=warning
```

#### CR-205 · 上肢锁定长钉固定370.5

| 属性 | 值 |
|------|-----|
| 客户 | WCSRMYY（`WCSRMYY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **370.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：上肢锁定长钉
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：WCSRMYY
包名：上肢锁定长钉-示例/Z7520
账单价：380.5 元（与规则价不符）
→ 引擎建议：370.5 元/包，status=warning
```

#### CR-206 · 上肢锁定固定332

| 属性 | 值 |
|------|-----|
| 客户 | WCSRMYY（`WCSRMYY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **332 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：上肢锁定
- **排除**关键词：长钉
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：WCSRMYY
包名：上肢锁定-示例/Z7520
账单价：342 元（与规则价不符）
→ 引擎建议：332 元/包，status=warning
```

#### CR-207 · 墨希钉固定50

| 属性 | 值 |
|------|-----|
| 客户 | WCSRMYY（`WCSRMYY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **50 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：墨希钉
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：WCSRMYY
包名：墨希钉-示例/Z7520
账单价：60 元（与规则价不符）
→ 引擎建议：50 元/包，status=warning
```

#### CR-208 · 额外包75纸塑袋10.5

| 属性 | 值 |
|------|-----|
| 客户 | WCSRMYY（`WCSRMYY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **10.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：75/双、/z7526、/z7530、/z7534
- **排除**关键词：墨希钉
- 器械件数 ≥ 1
- 器械件数 ≤ 1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：WCSRMYY
包名：75/双-示例/Z7520
账单价：20.5 元（与规则价不符）
→ 引擎建议：10.5 元/包，status=warning
```

#### CR-209 · 额外包10纸塑袋13.5

| 属性 | 值 |
|------|-----|
| 客户 | WCSRMYY（`WCSRMYY`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 6（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：10/双、/z1026、/z1029、/z1035
- 器械件数 ≥ 1
- 器械件数 ≤ 1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：WCSRMYY
包名：10/双-示例/Z7520
账单价：23.5 元（与规则价不符）
→ 引擎建议：13.5 元/包，status=warning
```

#### CR-210 · 手术室操作器22元/件

| 属性 | 值 |
|------|-----|
| 客户 | WCSRMYY（`WCSRMYY`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 22 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：操作器
- 科室 ∈ {手术室}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：WCSRMYY
包名：操作器-5/Z7520，器械 5 件
账单价：105 元
→ 引擎建议：5×22=110 元（规则配置不计包装费），status=warning
```

#### CR-211 · 手术室骨元针5件算1件

| 属性 | 值 |
|------|-----|
| 客户 | WCSRMYY（`WCSRMYY`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：骨元针
- 科室 ∈ {手术室}

**示例**

```
医院：WCSRMYY
包名：骨元针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

### 5.44 客户 · 武警黑龙江省总队医院（`WJ-HLJ-ZD`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：4 条（CR-212～CR-215）

#### CR-212 · 校正价8.0

| 属性 | 值 |
|------|-----|
| 客户 | 武警黑龙江省总队医院（`WJ-HLJ-ZD`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：器械包（纸塑袋）、敷料包（纸塑袋）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：武警黑龙江省总队医院
包名：器械包（纸塑袋）-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-213 · 校正价16.5

| 属性 | 值 |
|------|-----|
| 客户 | 武警黑龙江省总队医院（`WJ-HLJ-ZD`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：克氏针折弯钳-1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：武警黑龙江省总队医院
包名：克氏针折弯钳-1-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-214 · 校正价20.0

| 属性 | 值 |
|------|-----|
| 客户 | 武警黑龙江省总队医院（`WJ-HLJ-ZD`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **20 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：刷手包、器械包（无纺布）、敷料包（无纺布）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：武警黑龙江省总队医院
包名：刷手包-示例/Z7520
账单价：30 元（与规则价不符）
→ 引擎建议：20 元/包，status=warning
```

#### CR-215 · 校正价35.0

| 属性 | 值 |
|------|-----|
| 客户 | 武警黑龙江省总队医院（`WJ-HLJ-ZD`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：环氧乙烷（自行打包）-1包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：武警黑龙江省总队医院
包名：环氧乙烷（自行打包）-1包-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

### 5.45 客户 · 新发红十字医院（`XINFA-HSZ`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：6 条（CR-216～CR-221）

#### CR-216 · 新发镜头0度35

| 属性 | 值 |
|------|-----|
| 客户 | 新发红十字医院（`XINFA-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：0度镜头
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：新发红十字医院
包名：0度镜头-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-217 · 新发镜头12度28

| 属性 | 值 |
|------|-----|
| 客户 | 新发红十字医院（`XINFA-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **28 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：12度镜头、镜头-1/双
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：新发红十字医院
包名：12度镜头-示例/Z7520
账单价：38 元（与规则价不符）
→ 引擎建议：28 元/包，status=warning
```

#### CR-218 · 新发镜头30度35

| 属性 | 值 |
|------|-----|
| 客户 | 新发红十字医院（`XINFA-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：30度镜头、30°镜头
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：新发红十字医院
包名：30度镜头-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-219 · 新发棉球纸塑袋

| 属性 | 值 |
|------|-----|
| 客户 | 新发红十字医院（`XINFA-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：棉球/Z、带线棉球
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：新发红十字医院
包名：棉球/Z-示例/Z7520
账单价：14 元（与规则价不符）
→ 引擎建议：4 元/包，status=warning
```

#### CR-220 · 校正价66.0

| 属性 | 值 |
|------|-----|
| 客户 | 新发红十字医院（`XINFA-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **66 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：膀胱镜器械-3件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：新发红十字医院
包名：膀胱镜器械-3件-示例/Z7520
账单价：76 元（与规则价不符）
→ 引擎建议：66 元/包，status=warning
```

#### CR-221 · 穿刺器帽22

| 属性 | 值 |
|------|-----|
| 客户 | 新发红十字医院（`XINFA-HSZ`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：穿刺器帽
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：新发红十字医院
包名：穿刺器帽-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

### 5.46 客户 · 易丽医疗（`YILI-YL`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：1 条（CR-222～CR-222）

#### CR-222 · 易丽敷料纸塑4元

| 属性 | 值 |
|------|-----|
| 客户 | 易丽医疗（`YILI-YL`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：孔巾、包布、大单、治疗巾
- 纸塑袋宽 ≥ 20 cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：易丽医疗
包名：孔巾-示例/Z7520
账单价：14 元（与规则价不符）
→ 引擎建议：4 元/包，status=warning
```

### 5.47 客户 · 悦美芳华医疗门诊医院（`YUEMEI-FH`）

- 计价模式：`standard` — 标准灭菌表
- 启用规则：1 条（CR-223～CR-223）

#### CR-223 · 眼包5.5元/件

| 属性 | 值 |
|------|-----|
| 客户 | 悦美芳华医疗门诊医院（`YUEMEI-FH`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `standard` — 标准灭菌表 |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：眼包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：悦美芳华医疗门诊医院
包名：眼包-5/Z7520，器械 5 件
账单价：22.5 元
→ 引擎建议：5×5.5=27.5 元（规则配置不计包装费），status=warning
```

### 5.48 客户 · 祖研-黑龙江省中医医院（南岗院区）（`ZUYAN-NG`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：5 条（CR-224～CR-228）

#### CR-224 · 校正价13.5

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（南岗院区）（`ZUYAN-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：排针-11、排针-12、排针-14、排针-15、排针20、排针17、排针15、排针13、排针12、排针9件、排针-6、排针-10
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（南岗院区）
包名：排针-11-示例/Z7520
账单价：23.5 元（与规则价不符）
→ 引擎建议：13.5 元/包，status=warning
```

#### CR-225 · 祖研排针10合1含包材

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（南岗院区）（`ZUYAN-NG`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤10 件按 1 计费件；>10 件按 ceil(件数÷10) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：排针
- 器械件数 ≤ 20

**示例**

```
医院：祖研-黑龙江省中医医院（南岗院区）
包名：排针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-226 · 校正价16.5

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（南岗院区）（`ZUYAN-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：排针-23、排针-28、排针-30、排针21、排针22件、排针30件、排针21件、排针30件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（南岗院区）
包名：排针-23-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-227 · 祖研排针10合1免包材

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（南岗院区）（`ZUYAN-NG`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤10 件按 1 计费件；>10 件按 ceil(件数÷10) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：排针
- 器械件数 ≥ 21
- 命中后 **不计包装/袋材费**

**示例**

```
医院：祖研-黑龙江省中医医院（南岗院区）
包名：排针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-228 · 校正价22.0

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（南岗院区）（`ZUYAN-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：排针34件、排针35件、排针-34、排针-35、排针-47
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（南岗院区）
包名：排针34件-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

### 5.49 客户 · 祖研-黑龙江省中医医院（三辅院区）（`ZUYAN-SF`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：7 条（CR-229～CR-235）

#### CR-229 · 祖研三辅export 探针刨刀16.5

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（三辅院区）（`ZUYAN-SF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：探针1瘘管1刨刀1/Z1026
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（三辅院区）
包名：探针1瘘管1刨刀1/Z1026-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-230 · 针线包现价

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（三辅院区）（`ZUYAN-SF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **2.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：针线包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（三辅院区）
包名：针线包-示例/Z7520
账单价：12.5 元（与规则价不符）
→ 引擎建议：2.5 元/包，status=warning
```

#### CR-231 · 校正价22.0

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（三辅院区）（`ZUYAN-SF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：器械包-4件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（三辅院区）
包名：器械包-4件-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

#### CR-232 · 校正价30.0

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（三辅院区）（`ZUYAN-SF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纱布条
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（三辅院区）
包名：纱布条-示例/Z7520
账单价：40 元（与规则价不符）
→ 引擎建议：30 元/包，status=warning
```

#### CR-233 · 校正价33.0

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（三辅院区）（`ZUYAN-SF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **33 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：排针包48件盘1、美容科排针包50件盘1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（三辅院区）
包名：排针包48件盘1-示例/Z7520
账单价：43 元（与规则价不符）
→ 引擎建议：33 元/包，status=warning
```

#### CR-234 · 校正价38.5

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（三辅院区）（`ZUYAN-SF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **38.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：排针包51件盘1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（三辅院区）
包名：排针包51件盘1-示例/Z7520
账单价：48.5 元（与规则价不符）
→ 引擎建议：38.5 元/包，status=warning
```

#### CR-235 · PDF/期待价30

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（三辅院区）（`ZUYAN-SF`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 200（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纱布条
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（三辅院区）
包名：纱布条-示例/Z7520
账单价：40 元（与规则价不符）
→ 引擎建议：30 元/包，status=warning
```

### 5.50 客户 · 祖研-黑龙江省中医医院（香安院区）（`ZUYAN-XA`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：3 条（CR-236～CR-238）

#### CR-236 · 排针10盘算1件

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（香安院区）（`ZUYAN-XA`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤10 件按 1 计费件；>10 件按 ceil(件数÷5.5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：排针

**示例**

```
医院：祖研-黑龙江省中医医院（香安院区）
包名：排针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-237 · 校正价16.5

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（香安院区）（`ZUYAN-XA`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：剪刀2止血钳1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（香安院区）
包名：剪刀2止血钳1-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-238 · 校正价22.0

| 属性 | 值 |
|------|-----|
| 客户 | 祖研-黑龙江省中医医院（香安院区）（`ZUYAN-XA`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：剪刀2止血钳1探针1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：祖研-黑龙江省中医医院（香安院区）
包名：剪刀2止血钳1探针1-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

### 5.51 客户 · 黑龙江省中医药大学附属第三医院（电力）（`ZY3-DIANLI`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：6 条（CR-239～CR-244）

#### CR-239 · 小件盒25

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省中医药大学附属第三医院（电力）（`ZY3-DIANLI`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **25 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：小件盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省中医药大学附属第三医院（电力）
包名：小件盒-示例/Z7520
账单价：35 元（与规则价不符）
→ 引擎建议：25 元/包，status=warning
```

#### CR-240 · 校正价8.0

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省中医药大学附属第三医院（电力）（`ZY3-DIANLI`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：洁牙尖-5件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省中医药大学附属第三医院（电力）
包名：洁牙尖-5件-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-241 · 校正价13.5

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省中医药大学附属第三医院（电力）（`ZY3-DIANLI`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：成型片-7、洁牙尖-7
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省中医药大学附属第三医院（电力）
包名：成型片-7-示例/Z7520
账单价：23.5 元（与规则价不符）
→ 引擎建议：13.5 元/包，status=warning
```

#### CR-242 · 校正价35.0

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省中医药大学附属第三医院（电力）（`ZY3-DIANLI`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：洗手服
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省中医药大学附属第三医院（电力）
包名：洗手服-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-243 · 校正价88.0

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省中医药大学附属第三医院（电力）（`ZY3-DIANLI`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **88 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：激光镜-4件 盒1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省中医药大学附属第三医院（电力）
包名：激光镜-4件 盒1-示例/Z7520
账单价：98 元（与规则价不符）
→ 引擎建议：88 元/包，status=warning
```

#### CR-244 · 等离子镜36

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江省中医药大学附属第三医院（电力）（`ZY3-DIANLI`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **36 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：等离子镜
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江省中医药大学附属第三医院（电力）
包名：等离子镜-示例/Z7520
账单价：46 元（与规则价不符）
→ 引擎建议：36 元/包，status=warning
```

### 5.52 客户 · 黑龙江中医药大学附属第一医院（`ZYY-D1`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：40 条（CR-245～CR-284）

#### CR-245 · 30度腹腔镜ground30.38

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | -1（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30.4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：30°腹腔镜、30度腹腔镜
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：30°腹腔镜-示例/Z7520
账单价：40.4 元（与规则价不符）
→ 引擎建议：30.4 元/包，status=warning
```

#### CR-246 · 宫腔镜镜头-3件按包52.74

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **52.8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：镜头-3件
- 科室 ∈ {宫腔镜}
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：镜头-3件-示例/Z7520
账单价：62.8 元（与规则价不符）
→ 引擎建议：52.8 元/包，status=warning
```

#### CR-247 · 附一低温双1件35元

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：双
- **排除**关键词：(双)、/(双)、双极、双极线
- 器械件数 ≥ 1
- 器械件数 ≤ 1
- 灭菌类型 = `LT`（HT=高温，LT=低温）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：双-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-248 · 30°腹腔镜组合价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30.4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：30°腹腔镜、30度腹腔镜
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：30°腹腔镜-示例/Z7520
账单价：40.4 元（与规则价不符）
→ 引擎建议：30.4 元/包，status=warning
```

#### CR-249 · 附一克氏针5合1含包材

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 5（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：克氏针、银质针、内热针、车针、拔髓针、扩大针、卷棉子
- 器械件数 ≤ 10

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：克氏针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-250 · 附一克氏针5合1免包材

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 6（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：克氏针、银质针、内热针、车针、拔髓针、扩大针、卷棉子
- 器械件数 ≥ 11
- 命中后 **不计包装/袋材费**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：克氏针-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-251 · 张景欣特器w12050

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **132 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：张景欣特器
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：张景欣特器-示例/Z7520
账单价：142 元（与规则价不符）
→ 引擎建议：132 元/包，status=warning
```

#### CR-252 · 王树人特器w12050

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **328 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：王树人特器-26
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：王树人特器-26-示例/Z7520
账单价：338 元（与规则价不符）
→ 引擎建议：328 元/包，status=warning
```

#### CR-253 · 换药包120布组合价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：换药包(120布)、换药包120
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：换药包(120布)-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

#### CR-254 · 孔巾包整包价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **27.97 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：孔巾包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：孔巾包-示例/Z7520
账单价：37.97 元（与规则价不符）
→ 引擎建议：27.97 元/包，status=warning
```

#### CR-255 · 腔镜包整包价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **27.97 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：腔镜包
- **排除**关键词：腹腔镜
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：腔镜包-示例/Z7520
账单价：37.97 元（与规则价不符）
→ 引擎建议：27.97 元/包，status=warning
```

#### CR-256 · 辅料包整包价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **27.97 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：辅料包
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：辅料包-示例/Z7520
账单价：37.97 元（与规则价不符）
→ 引擎建议：27.97 元/包，status=warning
```

#### CR-257 · 球内注药3件最低把价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.2 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：球内注药
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：球内注药-示例/Z7520
账单价：23.2 元（与规则价不符）
→ 引擎建议：13.2 元/包，status=warning
```

#### CR-258 · 冲洗头-120固定价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **310.4 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：冲洗头-120
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：冲洗头-120-示例/Z7520
账单价：320.4 元（与规则价不符）
→ 引擎建议：310.4 元/包，status=warning
```

#### CR-259 · 冲洗头-79固定价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **220 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：冲洗头-79
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：冲洗头-79-示例/Z7520
账单价：230 元（与规则价不符）
→ 引擎建议：220 元/包，status=warning
```

#### CR-260 · 冲洗头-82固定价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **237.6 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：冲洗头-82
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：冲洗头-82-示例/Z7520
账单价：247.6 元（与规则价不符）
→ 引擎建议：237.6 元/包，status=warning
```

#### CR-261 · 橄榄头-102固定价

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **257.6 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：橄榄头-102
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：橄榄头-102-示例/Z7520
账单价：267.6 元（与规则价不符）
→ 引擎建议：257.6 元/包，status=warning
```

#### CR-262 · 橄榄头5件算1件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 15（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：橄榄头
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：橄榄头-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-263 · 冲洗头5件算1件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FOLD` — 件数折算 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 16（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后先将器械数折算：≤5 件按 1 计费件；>5 件按 ceil(件数÷5) 计费件，再乘标准单价

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：冲洗头
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：冲洗头-15/Z7520，器械 15 件
→ 先按 FOLD 折算计费件数，再套高温纸塑 5.5 元/件 + 袋费
```

#### CR-264 · 低温器械保护盒

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 20（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：低温器械保护盒、保护盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：低温器械保护盒-示例/Z7520
账单价：18 元（与规则价不符）
→ 引擎建议：8 元/包，status=warning
```

#### CR-265 · 低温套5件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 30（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **70.33 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：低温灭菌（套）5件、低温灭菌(套)5件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：低温灭菌（套）5件-示例/Z7520
账单价：80.33 元（与规则价不符）
→ 引擎建议：70.33 元/包，status=warning
```

#### CR-266 · 低温套10件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 31（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **131.87 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：低温灭菌（套）10件、低温灭菌 (套)10件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：低温灭菌（套）10件-示例/Z7520
账单价：141.87 元（与规则价不符）
→ 引擎建议：131.87 元/包，status=warning
```

#### CR-267 · 低温套20件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 32（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **239.76 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：低温灭菌（套）20件、低温灭菌 (套)20件
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：低温灭菌（套）20件-示例/Z7520
账单价：249.76 元（与规则价不符）
→ 引擎建议：239.76 元/包，status=warning
```

#### CR-268 · 输尿管镜z3095两件35.16

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 35（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35.16 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：z3095
- 器械件数 ≥ 2
- 器械件数 ≤ 2
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：z3095-示例/Z7520
账单价：45.16 元（与规则价不符）
→ 引擎建议：35.16 元/包，status=warning
```

#### CR-269 · 保温杯-1Z2044

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 39（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22.38 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：保温杯-1Z2044
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：保温杯-1Z2044-示例/Z7520
账单价：32.38 元（与规则价不符）
→ 引擎建议：22.38 元/包，status=warning
```

#### CR-270 · 低温袋15cm

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 41（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **20 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：膀胱取石钳
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：膀胱取石钳-示例/Z7520
账单价：30 元（与规则价不符）
→ 引擎建议：20 元/包，status=warning
```

#### CR-271 · 纸塑袋10cm单把

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 50（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **6.39 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纸塑袋 10cm、纸塑袋10cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：纸塑袋 10cm-示例/Z7520
账单价：16.39 元（与规则价不符）
→ 引擎建议：6.39 元/包，status=warning
```

#### CR-272 · 纸塑袋15cm单把

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 51（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **8.79 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纸塑袋 15cm、纸塑袋15cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：纸塑袋 15cm-示例/Z7520
账单价：18.79 元（与规则价不符）
→ 引擎建议：8.79 元/包，status=warning
```

#### CR-273 · 纸塑袋20cm单把

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 52（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **10.39 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纸塑袋20cm、纸塑袋 20cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：纸塑袋20cm-示例/Z7520
账单价：20.39 元（与规则价不符）
→ 引擎建议：10.39 元/包，status=warning
```

#### CR-274 · 纸塑袋25cm单把

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 53（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **12.79 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纸塑袋 25cm、纸塑袋25cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：纸塑袋 25cm-示例/Z7520
账单价：22.79 元（与规则价不符）
→ 引擎建议：12.79 元/包，status=warning
```

#### CR-275 · 纸塑袋15cm两件套

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 54（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.19 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纸塑袋 15cm、纸塑袋15cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：纸塑袋 15cm-示例/Z7520
账单价：23.19 元（与规则价不符）
→ 引擎建议：13.19 元/包，status=warning
```

#### CR-276 · 纸塑袋10cm两件套

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 55（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **10.79 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：纸塑袋 10cm、纸塑袋10cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：纸塑袋 10cm-示例/Z7520
账单价：20.79 元（与规则价不符）
→ 引擎建议：10.79 元/包，status=warning
```

#### CR-277 · 敷料20x20

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 70（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **3.2 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：20cm*20cm、20×20
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：20cm*20cm-示例/Z7520
账单价：13.2 元（与规则价不符）
→ 引擎建议：3.2 元/包，status=warning
```

#### CR-278 · 敷料大20x20x15

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 71（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **2 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：大 （20cm*20cm*15cm）、20cm*20cm*15cm
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：大 （20cm*20cm*15cm）-示例/Z7520
账单价：12 元（与规则价不符）
→ 引擎建议：2 元/包，status=warning
```

#### CR-279 · 敷料大30x30x50

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 72（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **27.97 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：敷料大（30cm*30cm*50cm）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：敷料大（30cm*30cm*50cm）-示例/Z7520
账单价：37.97 元（与规则价不符）
→ 引擎建议：27.97 元/包，status=warning
```

#### CR-280 · 敷料小20x20x15

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 73（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **19.98 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：敷料小（20cm*20cm*15cm）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：敷料小（20cm*20cm*15cm）-示例/Z7520
账单价：29.98 元（与规则价不符）
→ 引擎建议：19.98 元/包，status=warning
```

#### CR-281 · 敷料中20x20x30

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 74（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **23.98 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：敷料中（20cm*20cm*30cm）
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：敷料中（20cm*20cm*30cm）-示例/Z7520
账单价：33.98 元（与规则价不符）
→ 引擎建议：23.98 元/包，status=warning
```

#### CR-282 · 外来器械4.4每件

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 80（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 4.4 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：外来
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：外来-5/Z7520，器械 5 件
账单价：17 元
→ 引擎建议：5×4.4=22 元（规则配置不计包装费），status=warning
```

#### CR-283 · 外来钉盒40

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 81（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **40 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：钉盒
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：钉盒-示例/Z7520
账单价：50 元（与规则价不符）
→ 引擎建议：40 元/包，status=warning
```

#### CR-284 · 外来生物监测120

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第一医院（`ZYY-D1`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 82（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **120 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：生物监测
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第一医院
包名：生物监测-示例/Z7520
账单价：130 元（与规则价不符）
→ 引擎建议：120 元/包，status=warning
```

### 5.53 客户 · 黑龙江中医药大学附属第二医院（哈南分院）（`ZYY-D2-HN`）

- 计价模式：`hybrid` — 混合（未命中走标准灭菌阶梯价）
- 启用规则：1 条（CR-285～CR-285）

#### CR-285 · 校正价16.5

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（哈南分院）（`ZYY-D2-HN`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `hybrid` — 混合（未命中走标准灭菌阶梯价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：粉刺针-3
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（哈南分院）
包名：粉刺针-3-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

### 5.54 客户 · 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`）

- 计价模式：`special_only` — 仅特色（未命中保留原价）
- 启用规则：15 条（CR-286～CR-300）

#### CR-286 · 附二南岗大衣无纺布35

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 3（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：大衣-无纺布
- 仅导出账单阶段生效（exportApply）
- 命中后 **不计包装/袋材费**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：大衣-无纺布-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-287 · 附二南岗大衣W9050

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：大衣
- 命中后 **不计包装/袋材费**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：大衣-示例/Z7520
账单价：40 元（与规则价不符）
→ 引擎建议：30 元/包，status=warning
```

#### CR-288 · 附二南岗小单w1250

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 4（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：小单
- 命中后 **不计包装/袋材费**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：小单-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-289 · 附二南岗刮勺探针5.5元/把

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `PRICE_PER_INSTRUMENT` — 按件计价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **计费件数 × 5.5 元/件**

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：刮勺探针
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：刮勺探针-5/Z7520，器械 5 件
账单价：22.5 元
→ 引擎建议：5×5.5=27.5 元（规则配置不计包装费），status=warning
```

#### CR-290 · 附二南岗机扩针7盒

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 8（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **16.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：机扩针-7盒1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：机扩针-7盒1-示例/Z7520
账单价：26.5 元（与规则价不符）
→ 引擎建议：16.5 元/包，status=warning
```

#### CR-291 · 校正价22.0

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **22 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：剪刀-4、车针-17
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：剪刀-4-示例/Z7520
账单价：32 元（与规则价不符）
→ 引擎建议：22 元/包，status=warning
```

#### CR-292 · 附二南岗弯针2

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 10（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **13.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：弯针-2
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：弯针-2-示例/Z7520
账单价：23.5 元（与规则价不符）
→ 引擎建议：13.5 元/包，status=warning
```

#### CR-293 · 校正价30.0

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：大衣
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：大衣-示例/Z7520
账单价：40 元（与规则价不符）
→ 引擎建议：30 元/包，status=warning
```

#### CR-294 · 附二南岗椎间孔镜镜头

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 11（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：椎间孔镜镜头
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：椎间孔镜镜头-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-295 · 校正价35.0

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：小单
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：小单-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

#### CR-296 · 附二南岗关节镜30度

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 12（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **36 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：关节镜30°
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：关节镜30°-示例/Z7520
账单价：46 元（与规则价不符）
→ 引擎建议：36 元/包，status=warning
```

#### CR-297 · 校正价38.5

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **38.5 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：机扩针-6盒1
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：机扩针-6盒1-示例/Z7520
账单价：48.5 元（与规则价不符）
→ 引擎建议：38.5 元/包，status=warning
```

#### CR-298 · 附二南岗关节镜70度

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 13（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **28 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：关节镜70°
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：关节镜70°-示例/Z7520
账单价：38 元（与规则价不符）
→ 引擎建议：28 元/包，status=warning
```

#### CR-299 · PDF/期待价30

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 200（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **30 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：大衣
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：大衣-示例/Z7520
账单价：40 元（与规则价不符）
→ 引擎建议：30 元/包，status=warning
```

#### CR-300 · PDF/期待价35

| 属性 | 值 |
|------|-----|
| 客户 | 黑龙江中医药大学附属第二医院（南岗）（`ZYY-D2-NG`） |
| 规则类型 | `FIXED_PRICE` — 固定单价 |
| 客户计价模式 | `special_only` — 仅特色（未命中保留原价） |
| 优先级 | 201（越小越先匹配） |
| 状态 | **启用** |

**作用**

命中后建议单价 = **35 元/包**（固定，不随件数变）

**生效条件**（须全部满足）

- 包名/类型/包材合并文本 **包含任一关键词**：小单
- 命中后 **不计包装/袋材费**
- 命中后 **不计客户折扣**

**示例**

```
医院：黑龙江中医药大学附属第二医院（南岗）
包名：小单-示例/Z7520
账单价：45 元（与规则价不符）
→ 引擎建议：35 元/包，status=warning
```

## 第 6 章 · 特殊收费(11) 业务分类（SC11-*）

> Excel 源文件 [`特殊收费(11).xlsx`](source/特殊收费(11).xlsx) 共 **107** 条语义条目（53 院级 + 22 通用 + 32 阶梯），
> 编译落库后对应第 5 章 CR-* 与第 2 章 STD-*。下列 **18** 种 SC11 类型为业务口径分类编号。

### SC11-T01 · 按件×单价 + 无纺布加价

**Registry 条目数**：3

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：冰城环钻包 7 件

**示例**：
```
7×5.5+3=41.5 元（PER_INSTRUMENT + EXTRA_FEE）
```

### SC11-T02 · 缝合针 1 件×5.5 + 标准包材

**Registry 条目数**：2

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：电机厂缝合针 1 件/15cm

**示例**：
```
5.5+2.5=8 元或固定 8 元
```

### SC11-T03 · 「双」包少件按件+包材

**Registry 条目数**：1

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：电机厂双包 ≤2 件

**示例**：
```
n×5.5 + 纸塑袋价
```

### SC11-T03b · 低温「双」固定 35 元

**Registry 条目数**：1

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：通用低温双包

**示例**：
```
35 元/包（FIXED_PRICE, LT）
```

### SC11-T04 · 小件 5 合 1 + 包材（≤10 件）

**Registry 条目数**：20

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：方南南 P 钻 8 件

**示例**：
```
ceil(8/5)×5.5+袋费
```

### SC11-T04b · 5 合 1 但单价 5.6

**Registry 条目数**：1

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：东北农大根管锉 12 件

**示例**：
```
ceil(12/5)×5.6+袋费
```

### SC11-T05 · 小件 5 合 1，>10 件免包材

**Registry 条目数**：8

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：克氏针 15 件

**示例**：
```
ceil(15/5)×5.5=16.5
```

### SC11-T06 · 10 合 1 + 包材

**Registry 条目数**：1

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：祖研排针 15 件

**示例**：
```
ceil(15/10)×5.5+袋费=16.5
```

### SC11-T07 · 敷料包 W 码固定价

**Registry 条目数**：3

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：驱血带 W90

**示例**：
```
30 元/包
```

### SC11-T08 · 包级固定单价

**Registry 条目数**：20

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：九州方盘

**示例**：
```
5.5 元/包（与件数无关）
```

### SC11-T09 · 纸塑 ≥20cm 固定价

**Registry 条目数**：2

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：工程大学孔巾 20cm

**示例**：
```
4 元/包
```

### SC11-T10 · 纸塑 <20cm 固定价

**Registry 条目数**：1

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：通用敷料 15cm

**示例**：
```
2.5 元/包
```

### SC11-T11 · 低温小件 ≤5 按 1 件+包材

**Registry 条目数**：3

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：海员胶帽 3 件

**示例**：
```
1 件低温包装价
```

### SC11-T12 · 低温强制 1 件一包

**Registry 条目数**：1

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：春语塑料管

**示例**：
```
标准低温包材价
```

### SC11-T13 · 标准价上加收

**Registry 条目数**：5

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：总工会 12°镜头

**示例**：
```
标准价 +8 元
```

### SC11-T14 · 双层袋 + 按件 5.5

**Registry 条目数**：1

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：索菲面吸针 ≥3 件

**示例**：
```
n×5.5（skipPackaging）
```

### SC11-T15 · 不论件数一口价

**Registry 条目数**：2

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：软镜 ETO/低温

**示例**：
```
300 元/包
```

### SC11-T16 · 环氧/低温件数阶梯+宽度 addon

**Registry 条目数**：32

**作用**：将 Excel 自然语言规则映射为 seed `ruleType` 组合，供引擎编译执行。

**典型生效场景**：低温纸塑 5 件/20cm

**示例**：
```
88 元（标准模板）
```

---

## 附录 A · 已停用客户规则（55 条）

<details><summary>点击展开</summary>

- `BINGCHENG-YM` · ≥3件按件5.5元 · 按件计价
- `BINGCHENG-YM` · 整形包54.5 · 固定单价
- `BINGCHENG-YM` · 整形包58 · 固定单价
- `BINGCHENG-YM` · 环钻27.5 · 固定单价
- `BINGCHENG-YM` · 环钻包30.5 · 固定单价
- `BINGCHENG-YM` · 脂充包54.5 · 固定单价
- `GUOYAO-2` · 校正价8.0 · 固定单价
- `GUOYAO-2` · 电机厂双按件5.5 · 按件计价
- `GUOYAO-2` · 电机厂指针5合1 · 件数折算
- `HRB-2ND` · 校正价5.5 · 固定单价
- `HRB-HIT` · 哈工大export 换药碗13 · 固定单价
- `HRB-HIT` · 哈工大export 种植盒23件27.5 · 固定单价
- `HRB-HIT` · 哈工大换药碗13 · 固定单价
- `HRB-HIT` · 哈工大种植盒23件27.5 · 固定单价
- `HRB-HIT` · 哈工大胆囊包10件165 · 固定单价
- `HRB-SD-MB` · 松电口腔科针类5件算1件 · 件数折算
- `HRB-SD-MB` · 松电车针5件算1件 · 件数折算
- `HRB-WY` · 市五W605060 export11 · 固定单价
- `HRB-WY` · 市五W705060 export16.5 · 固定单价
- `HRB-WY` · 市五W905060 export35 · 固定单价
- `HRB-WY` · 市五W906050 export35 · 固定单价
- `HRB-WY` · 市五Z1026 export88 · 固定单价
- `HRB-WY` · 市五export 胸腔镜121 · 固定单价
- `HRB-WY` · 市五export 骨四咬钳16.5 · 固定单价
- `HRB-WY` · 市五胸腔镜121 · 固定单价
- `HRB-WY-EM` · 校正价132.0 · 固定单价
- `HRB-WY-EM` · 校正价16.5 · 固定单价
- `HRB-WY-EM` · 校正价22.0 · 固定单价
- `HRB-WY-EM` · 校正价44.0 · 固定单价
- `HRB-WY-EM` · 校正价88.0 · 固定单价
- `HRB-WY-EM` · 校正价99.0 · 固定单价
- `NEAU-YY` · 东北农大口腔科针类5件算1件 · 件数折算
- `NEAU-YY` · 东北农大车针5件算1件 · 件数折算
- `NG-FUCHAN` · 南岗妇产export 扩棒16.5 · 固定单价
- `WCSRMYY` · 手术室腹腔镜器械(三号)17元/件 · 按件计价
- `WCSRMYY` · 手术室腹腔镜器械包187元/包 · 固定单价
- `ZUYAN-NG` · 排针10盘算1件 · 件数折算
- `ZUYAN-NG` · 校正价8.0 · 固定单价
- `ZUYAN-NG` · 祖研南岗export 弯盘13 · 固定单价
- `ZUYAN-NG` · 祖研南岗弯盘13 · 固定单价
- `ZUYAN-SF` · 三辅探针瘘管刨刀包 · 固定单价
- `ZYY-D1` · 30度腹腔镜组合价golden30.4 · 固定单价
- `ZYY-D1` · 低温袋10cm · 固定单价
- `ZYY-D1` · 低温袋20cm · 固定单价
- `ZYY-D1` · 低温袋25cm · 固定单价
- `ZYY-D1` · 低温袋30cm · 固定单价
- `ZYY-D1` · 外二换药包120布golden21.99 · 固定单价
- `ZYY-D1` · 外二换药包按包21.99 · 固定单价
- `ZYY-D1` · 王树人特器26golden328 · 固定单价
- `ZYY-D2-NG` · 附二南岗export 关节镜30度44 · 固定单价
- `ZYY-D2-NG` · 附二南岗export 关节镜70度44 · 固定单价
- `ZYY-D2-NG` · 附二南岗export 刮勺探针22 · 固定单价
- `ZYY-D2-NG` · 附二南岗export 机扩针7盒16.5 · 固定单价
- `ZYY-D2-NG` · 附二南岗export 椎间孔镜35 · 固定单价
- `ZYY-D2-NG` · 附二南岗刮勺探针4 · 固定单价

</details>
