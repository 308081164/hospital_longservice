<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.backend.mapper.MenuMapper">

    <resultMap id="MenuResultMap" type="com.hospital.backend.entity.Menu">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="menuType" column="menu_type"/>
        <result property="icon" column="icon"/>
        <result property="path" column="path"/>
        <result property="order" column="menu_order"/>
        <result property="parentId" column="parent_id"/>
        <result property="isHidden" column="is_hidden"/>
        <result property="component" column="component"/>
        <result property="keepalive" column="keepalive"/>
        <result property="redirect" column="redirect"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.hospital.backend.entity.Menu" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_menu (name, menu_type, icon, path, menu_order, parent_id, is_hidden, component, keepalive, redirect, created_at, updated_at)
        VALUES (#{name}, #{menuType}, #{icon}, #{path}, #{order}, #{parentId}, #{isHidden}, #{component}, #{keepalive}, #{redirect}, NOW(), NOW())
    </insert>

    <select id="selectById" resultMap="MenuResultMap">
        SELECT * FROM sys_menu WHERE id = #{id}
    </select>

    <select id="selectAllOrderByOrder" resultMap="MenuResultMap">
        SELECT * FROM sys_menu ORDER BY menu_order ASC
    </select>

    <select id="selectAll" resultMap="MenuResultMap">
        SELECT * FROM sys_menu
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM sys_menu
    </select>

    <select id="selectByPath" resultMap="MenuResultMap">
        SELECT * FROM sys_menu WHERE path = #{path} LIMIT 1
    </select>

    <update id="update" parameterType="com.hospital.backend.entity.Menu">
        UPDATE sys_menu
        SET name = #{name},
            menu_type = #{menuType},
            icon = #{icon},
            path = #{path},
            menu_order = #{order},
            parent_id = #{parentId},
            is_hidden = #{isHidden},
            component = #{component},
            keepalive = #{keepalive},
            redirect = #{redirect},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
