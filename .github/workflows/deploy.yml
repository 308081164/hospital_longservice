# push 到 main 时：校验迁移清单 → 构建并推送镜像 → SSH 部署 → 强制重建 backend 执行 SQL 迁移 → 等待健康后重启前端
name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  # 禁止取消进行中的部署：历史事故（89bc9893）中取消发生在「frontend 已 stop/rm、尚未 recreate」
  # 窗口内，造成部分部署。改为排队串行，后续 push 等待前一个部署完整结束。
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify DB migrate manifest
        run: bash scripts/check-migrate-manifest.sh

      - name: Verify billing rules manifest fresh
        run: bash scripts/check-manifest-fresh.sh

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Run backend unit tests (golden rows + pricing)
        working-directory: backend
        run: mvn -q test -Dtest=PricingEngineTest#goldenRowsRegression,PricingRuleCompilerIntegrationTest,CustomerServiceImplTest,RuleEntrySemanticsTest,PricingRuleCompilerSemanticTest,SpecialCharge11CoverageTest,PricingEngineSpecialRulesTest,PricingEngineStandardPathTest,PricingEngineBillingModeTest,RuleTypeCoverageGateTest,RuleFidelityRegressionTest

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Build frontend (sanity)
        working-directory: frontend
        run: |
          pnpm install --frozen-lockfile
          npx vite build

      - name: Set image names
        id: images
        run: |
          OWNER="${{ github.repository_owner }}"
          OWNER_LC=$(echo "$OWNER" | tr '[:upper:]' '[:lower:]')
          echo "backend=$REGISTRY/$OWNER_LC/hospital-backend:latest" >> $GITHUB_OUTPUT
          echo "frontend=$REGISTRY/$OWNER_LC/hospital-frontend:latest" >> $GITHUB_OUTPUT
          # 不可变 SHA 标签：部署只认 SHA 标签，杜绝 :latest 拉取漂移/缓存旧镜像
          echo "backend_sha=$REGISTRY/$OWNER_LC/hospital-backend:${GITHUB_SHA}" >> $GITHUB_OUTPUT
          echo "frontend_sha=$REGISTRY/$OWNER_LC/hospital-frontend:${GITHUB_SHA}" >> $GITHUB_OUTPUT

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backend
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 45
          max_attempts: 4
          retry_wait_seconds: 45
          warning_on_retry: true
          command: >
            docker buildx build --push
            --build-arg GIT_SHA="${{ github.sha }}"
            --build-arg BUILD_TIME="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            -t "${{ steps.images.outputs.backend }}"
            -t "${{ steps.images.outputs.backend_sha }}"
            ./backend

      - name: Build and push frontend
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 45
          max_attempts: 4
          retry_wait_seconds: 45
          warning_on_retry: true
          command: >
            docker buildx build --push
            --build-arg APP_BUILD_ID="${{ github.sha }}"
            -t "${{ steps.images.outputs.frontend }}"
            -t "${{ steps.images.outputs.frontend_sha }}"
            ./frontend

      - name: Deploy to server
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_PORT: ${{ secrets.SSH_PORT || 22 }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH || '/mnt/newdisk/app/Hospital' }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "${SSH_PORT}" -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -p "${SSH_PORT}" \
            "${SSH_USER}@${SSH_HOST}" \
            "mkdir -p '${DEPLOY_PATH}/deploy' && \
             if [ -d '${DEPLOY_PATH}/deploy/nginx-frontend-shared-net.conf' ]; then \
               rm -rf '${DEPLOY_PATH}/deploy/nginx-frontend-shared-net.conf'; \
             fi"
          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -P "${SSH_PORT}" \
            docker-compose.prod.yml \
            "${SSH_USER}@${SSH_HOST}:${DEPLOY_PATH}/"
          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -P "${SSH_PORT}" -r \
            backend \
            "${SSH_USER}@${SSH_HOST}:${DEPLOY_PATH}/"
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -p "${SSH_PORT}" \
            "${SSH_USER}@${SSH_HOST}" "mkdir -p '${DEPLOY_PATH}/deploy/sql'"
          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -P "${SSH_PORT}" \
            deploy/nginx-hospital.conf \
            deploy/nginx-frontend-shared-net.conf \
            deploy/verify-billing-on-server.sh \
            deploy/reapply-billing-manifest-on-server.sh \
            deploy/reapply-billing-p0-6-on-server.sh \
            deploy/apply-p0-6-billing-sql.sh \
            deploy/mysql-hospital-cli.sh \
            deploy/verify-billing-api-on-server.sh \
            deploy/run-prod-verify.sh \
            deploy/prod-smoke-docker.sh \
            "${SSH_USER}@${SSH_HOST}:${DEPLOY_PATH}/deploy/"
          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -P "${SSH_PORT}" \
            deploy/sql/p0-6-billing-toggle.sql \
            "${SSH_USER}@${SSH_HOST}:${DEPLOY_PATH}/deploy/sql/"

      - name: Pull service images on server (runner SSH)
        env:
          IMAGE_BACKEND: ${{ steps.images.outputs.backend_sha }}
          IMAGE_FRONTEND: ${{ steps.images.outputs.frontend_sha }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          GHCR_USER: ${{ secrets.GHCR_USERNAME }}
          GHCR_TOKEN: ${{ secrets.GHCR_READ_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
        run: |
          set -e
          PORT="${SSH_PORT:-22}"
          DPATH="${DEPLOY_PATH:-/mnt/newdisk/app/Hospital}"
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/deploy_pull_key
          chmod 600 ~/.ssh/deploy_pull_key
          ssh-keyscan -p "$PORT" -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          echo "DEPLOY_STAGE=pull SHA=${GITHUB_SHA:-unknown}"

          {
            echo 'set -e'
            echo "cd $(printf %q "$DPATH")"
            echo "echo DEPLOY_STAGE=pull SHA=$(printf %q "${GITHUB_SHA:-unknown}")"
            if [ -n "${GHCR_TOKEN:-}" ] && [ -n "${GHCR_USER:-}" ]; then
              echo "echo $(printf %q "$GHCR_TOKEN") | docker login ghcr.io -u $(printf %q "$GHCR_USER") --password-stdin"
            fi
            echo "export IMAGE_BACKEND=$(printf %q "$IMAGE_BACKEND")"
            echo "export IMAGE_FRONTEND=$(printf %q "$IMAGE_FRONTEND")"
            echo "docker compose -f docker-compose.prod.yml pull"
            echo "echo DEPLOY_STAGE=pull OK"
          } | ssh -i ~/.ssh/deploy_pull_key \
              -o StrictHostKeyChecking=no \
              -o ServerAliveInterval=30 \
              -o ServerAliveCountMax=360 \
              -p "$PORT" \
              "$SSH_USER@$SSH_HOST" bash -s

      - name: Deploy stack on server (mysql + backend)
        uses: appleboy/ssh-action@v1.2.5
        env:
          IMAGE_BACKEND: ${{ steps.images.outputs.backend_sha }}
          IMAGE_FRONTEND: ${{ steps.images.outputs.frontend_sha }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          GHCR_USER: ${{ secrets.GHCR_USERNAME }}
          GHCR_TOKEN: ${{ secrets.GHCR_READ_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 2h
          timeout: 2m
          envs: IMAGE_BACKEND,IMAGE_FRONTEND,DEPLOY_PATH,GHCR_USER,GHCR_TOKEN,GITHUB_SHA
          script: |
            set -e
            DPATH="${DEPLOY_PATH:-/mnt/newdisk/app/Hospital}"
            cd "$DPATH"
            echo "DEPLOY_SCRIPT_TAG=hospital-v1 SHA=${GITHUB_SHA:-unknown}"

            if [ ! -f .env ]; then
              echo "错误: 服务器 $DPATH/.env 不存在，请先上传 .env 后再部署。"
              exit 1
            fi

            if [ -n "$GHCR_TOKEN" ] && [ -n "$GHCR_USER" ]; then
              echo "$GHCR_TOKEN" | docker login ghcr.io -u "$GHCR_USER" --password-stdin
            fi
            CI_IMAGE_BACKEND="${IMAGE_BACKEND}"
            CI_IMAGE_FRONTEND="${IMAGE_FRONTEND}"
            set -a && source .env && set +a
            export IMAGE_BACKEND="${CI_IMAGE_BACKEND}"
            export IMAGE_FRONTEND="${CI_IMAGE_FRONTEND}"

            # MySQL 仅保证运行（避免每次 force-recreate 导致空窗）。
            # 注意：不再提前 stop/rm frontend —— 旧 frontend 一直服务到「Recreate frontend」步骤，
            # 消除「backend 失败导致 frontend 也被停掉」的额外停机窗口。
            docker compose -f docker-compose.prod.yml up -d mysql
            echo "DEPLOY_STAGE=mysql_container_started"
            for i in $(seq 1 30); do
              if docker compose -f docker-compose.prod.yml ps mysql | grep -q '(healthy)'; then
                echo "mysql healthy（第 ${i} 轮）"
                break
              fi
              sleep 2
            done
            if [ -f deploy/reapply-billing-manifest-on-server.sh ]; then
              echo "DEPLOY_STAGE=billing_manifest_reconcile_deferred"
              echo "billingEnabled 将在 backend 启动后由 manifest reconciler 同步"
            fi
            docker compose -f docker-compose.prod.yml up -d --no-deps --force-recreate backend
            echo "DEPLOY_STAGE=backend_container_started"

      - name: Wait for backend health (runner SSH poll)
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          set -e
          PORT="${SSH_PORT:-22}"
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/deploy_wait_key
          chmod 600 ~/.ssh/deploy_wait_key
          ssh-keyscan -p "$PORT" -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          echo "DEPLOY_SCRIPT_TAG=hospital-wait SHA=${GITHUB_SHA:-unknown}"
          echo "等待宿主机 127.0.0.1:8853/api/v1/base/health（最多约 600s）"

          run_ssh () {
            ssh -i ~/.ssh/deploy_wait_key -o StrictHostKeyChecking=no -p "$PORT" "$SSH_USER@$SSH_HOST" "$@"
          }

          chk='curl -sS --connect-timeout 4 -o /dev/null http://127.0.0.1:8853/api/v1/base/health'

          for round in $(seq 1 60); do
            if run_ssh "$chk"; then
              echo "backend 健康检查通过（第 ${round} 轮）"
              exit 0
            fi
            echo "[wait] 第 ${round} 轮 / 60，8853 仍不可达"
            if [ "$((round % 3))" -eq 0 ]; then
              echo "---- backend 最近日志 ----"
              run_ssh "docker logs --tail 100 hospital-backend 2>&1" || true
            fi
            sleep 10
          done

          echo "==== 8853 在预期时间内仍不可访问 ===="
          run_ssh "docker logs --tail 300 hospital-backend 2>&1" || true
          exit 1

      - name: Assert backend version parity (hard gate)
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
          GITHUB_SHA: ${{ github.sha }}
          EXPECTED_IMAGE: ${{ steps.images.outputs.backend_sha }}
        run: |
          set -e
          PORT="${SSH_PORT:-22}"
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/deploy_parity_key
          chmod 600 ~/.ssh/deploy_parity_key
          ssh-keyscan -p "$PORT" -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

          run_ssh () {
            ssh -i ~/.ssh/deploy_parity_key -o StrictHostKeyChecking=no -p "$PORT" "$SSH_USER@$SSH_HOST" "$@"
          }

          echo "==> 校验生产 backend 版本 == 本次提交 ${GITHUB_SHA}"
          VER_BODY="$(run_ssh "curl -sS --connect-timeout 5 http://127.0.0.1:8853/api/v1/base/version")"
          echo "$VER_BODY"
          ACTUAL_SHA="$(printf '%s' "$VER_BODY" | python3 -c 'import sys,json; print((json.load(sys.stdin).get("data") or {}).get("gitSha") or "")')"
          if [ "$ACTUAL_SHA" != "$GITHUB_SHA" ]; then
            echo "错误: 生产 backend gitSha=${ACTUAL_SHA:-未知} != 期望 ${GITHUB_SHA}（旧容器/旧镜像仍在服务）" >&2
            run_ssh "docker inspect hospital-backend --format 'container_image={{.Config.Image}} image_id={{.Image}}'" || true
            exit 1
          fi

          echo "==> 校验 backend 容器镜像 == ${EXPECTED_IMAGE}"
          CONTAINER_IMAGE="$(run_ssh "docker inspect hospital-backend --format '{{.Config.Image}}'")"
          if [ "$CONTAINER_IMAGE" != "$EXPECTED_IMAGE" ]; then
            echo "错误: backend 容器镜像=${CONTAINER_IMAGE} != 期望 ${EXPECTED_IMAGE}" >&2
            exit 1
          fi
          echo "backend 版本 parity OK（${ACTUAL_SHA}）"

      - name: Recreate frontend on server
        uses: appleboy/ssh-action@v1.2.5
        env:
          IMAGE_BACKEND: ${{ steps.images.outputs.backend_sha }}
          IMAGE_FRONTEND: ${{ steps.images.outputs.frontend_sha }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          GHCR_USER: ${{ secrets.GHCR_USERNAME }}
          GHCR_TOKEN: ${{ secrets.GHCR_READ_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 45m
          timeout: 2m
          envs: IMAGE_BACKEND,IMAGE_FRONTEND,DEPLOY_PATH,GHCR_USER,GHCR_TOKEN,GITHUB_SHA
          script: |
            set -e
            DPATH="${DEPLOY_PATH:-/mnt/newdisk/app/Hospital}"
            cd "$DPATH"
            echo "DEPLOY_SCRIPT_TAG=hospital-frontend SHA=${GITHUB_SHA:-unknown}"
            if [ -n "$GHCR_TOKEN" ] && [ -n "$GHCR_USER" ]; then
              echo "$GHCR_TOKEN" | docker login ghcr.io -u "$GHCR_USER" --password-stdin
            fi
            CI_IMAGE_BACKEND="${IMAGE_BACKEND}"
            CI_IMAGE_FRONTEND="${IMAGE_FRONTEND}"
            set -a && source .env && set +a
            export IMAGE_BACKEND="${CI_IMAGE_BACKEND}"
            export IMAGE_FRONTEND="${CI_IMAGE_FRONTEND}"
            if [ ! -f deploy/nginx-frontend-shared-net.conf ]; then
              echo "错误: deploy/nginx-frontend-shared-net.conf 不存在（SCP 路径错误或未同步）"
              ls -la deploy/ 2>/dev/null || true
              exit 1
            fi
            # 此刻才替换 frontend（backend 已通过版本 parity 硬闸门）
            docker compose -f docker-compose.prod.yml stop frontend 2>/dev/null || true
            docker compose -f docker-compose.prod.yml rm -f frontend 2>/dev/null || true
            docker compose -f docker-compose.prod.yml up -d --no-deps --force-recreate frontend
            ACTUAL_FE_IMAGE="$(docker inspect hospital-frontend --format '{{.Config.Image}}')"
            if [ "$ACTUAL_FE_IMAGE" != "$IMAGE_FRONTEND" ]; then
              echo "错误: frontend 容器镜像=${ACTUAL_FE_IMAGE} != 期望 ${IMAGE_FRONTEND}" >&2
              exit 1
            fi
            echo "frontend 镜像 parity OK（${ACTUAL_FE_IMAGE}）"

      - name: Sync billing deploy scripts to server (before reapply)
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_PORT: ${{ secrets.SSH_PORT || 22 }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH || '/mnt/newdisk/app/Hospital' }}
        run: |
          set -e
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/deploy_billing_key
          chmod 600 ~/.ssh/deploy_billing_key
          ssh-keyscan -p "${SSH_PORT}" -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          ssh -i ~/.ssh/deploy_billing_key -o StrictHostKeyChecking=no -p "${SSH_PORT}" \
            "${SSH_USER}@${SSH_HOST}" "mkdir -p '${DEPLOY_PATH}/deploy/sql'"
          scp -i ~/.ssh/deploy_billing_key -o StrictHostKeyChecking=no -P "${SSH_PORT}" \
            deploy/verify-billing-on-server.sh \
            deploy/reapply-billing-manifest-on-server.sh \
            deploy/reapply-billing-p0-6-on-server.sh \
            deploy/apply-p0-6-billing-sql.sh \
            deploy/mysql-hospital-cli.sh \
            deploy/verify-billing-api-on-server.sh \
            deploy/run-prod-verify.sh \
            deploy/prod-smoke-docker.sh \
            "${SSH_USER}@${SSH_HOST}:${DEPLOY_PATH}/deploy/"
          scp -i ~/.ssh/deploy_billing_key -o StrictHostKeyChecking=no -P "${SSH_PORT}" \
            deploy/sql/p0-6-billing-toggle.sql \
            "${SSH_USER}@${SSH_HOST}:${DEPLOY_PATH}/deploy/sql/"

      - name: Reconcile billing manifest and verify on server
        uses: appleboy/ssh-action@v1.2.5
        env:
          IMAGE_BACKEND: ${{ steps.images.outputs.backend_sha }}
          IMAGE_FRONTEND: ${{ steps.images.outputs.frontend_sha }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 15m
          envs: IMAGE_BACKEND,IMAGE_FRONTEND
          script: |
            set -e
            DPATH="${{ secrets.DEPLOY_PATH || '/mnt/newdisk/app/Hospital' }}"
            cd "$DPATH"
            MANIFEST="backend/src/main/resources/billing-seeds/billing-rules-manifest.json"
            if [ ! -f "$MANIFEST" ]; then
              echo "错误: $MANIFEST 未同步到服务器" >&2
              ls -la backend/src/main/resources/billing-seeds/ 2>/dev/null || true
              exit 1
            fi
            chmod +x deploy/verify-billing-on-server.sh deploy/reapply-billing-manifest-on-server.sh \
              deploy/verify-billing-api-on-server.sh deploy/mysql-hospital-cli.sh 2>/dev/null || true
            echo "==> deploy 目录（billing 脚本）"
            ls -la deploy/*.sh "$MANIFEST" 2>/dev/null || ls -la deploy/ || true
            echo "==> backend 连库诊断"
            bash deploy/mysql-hospital-cli.sh --print-target 2>/dev/null || true
            docker inspect hospital-backend --format '{{range .Config.Env}}{{println .}}{{end}}' 2>/dev/null | grep '^MYSQL_' || true
            bash deploy/reapply-billing-manifest-on-server.sh || {
              echo "==== manifest reconcile 失败，backend 日志 ===="
              docker logs hospital-backend 2>&1 | tail -80 || true
              exit 1
            }

      - name: Post-deploy CLI smoke
        uses: appleboy/ssh-action@v1.2.5
        env:
          GITHUB_SHA: ${{ github.sha }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 10m
          envs: GITHUB_SHA
          script: |
            set -e
            DPATH="${{ secrets.DEPLOY_PATH || '/mnt/newdisk/app/Hospital' }}"
            cd "$DPATH"
            chmod +x deploy/run-prod-verify.sh deploy/prod-smoke-docker.sh 2>/dev/null || true
            bash deploy/run-prod-verify.sh smoke --json --expect-sha "$GITHUB_SHA"

  post-deploy-parity-gate:
    needs: build-and-push
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Prod parity gate (read-only)
        env:
          PROD_HOST: ${{ secrets.SSH_HOST }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_PORT: ${{ secrets.SSH_PORT || 22 }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH || '/mnt/newdisk/app/Hospital' }}
          SMOKE_USER: ${{ secrets.SMOKE_USER }}
          SMOKE_PASS: ${{ secrets.SMOKE_PASS }}
          SMOKE_EXPECT_SHA: ${{ github.sha }}
        run: |
          chmod +x bin/hospital-cli scripts/run_prod_parity_gate.sh
          bash scripts/run_prod_parity_gate.sh

      - name: Upload calibration artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: job_baseline_prod_calibration
          path: 测试用例/job_baseline_prod_calibration.json
          if-no-files-found: warn

      - name: Upload rules parity report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: billing_rules_parity_report
          path: 测试用例/billing_rules_parity_report.json
          if-no-files-found: warn
